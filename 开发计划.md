# 开发计划

> 状态说明:✅ 已完成 ｜ 🔄 进行中 ｜ 📋 待规划 ｜ ⚠️ 已知问题

本项目为「可视化网页宏录制 + 回放 + 数据提取 + 导出 Excel」桌面工具(MVP 第一版)。
技术栈:Node + TypeScript + Electron + Playwright + exceljs,本地 JSON 存储。

## 模块:内置浏览器与录制

### 核心功能

- ✅ Electron 桌面窗口(地址栏、打开网页、开始/停止录制、运行/保存/加载宏、导出 Excel、日志区)
- ✅ 内置浏览器使用 `<webview>`(Chromium),可浏览任意网页
- ✅ 录制器(`webview-preload.ts`)监听 click / input / press / scroll 并生成步骤
- ✅ 稳定选择器生成(`selector-generator.ts`):data-testid → id → name → aria-label → role+文本 → class → css path → xpath;css path 的每段在可用时携带稳定 class(如 `li.next`),`:nth-of-type` 仅在 class 仍无法区分同标签兄弟时才用,避免「录制后页面增减兄弟元素致选择器命中数变化」
- ✅ click 步骤附语义指纹(`buildFingerprint`:tag/text/aria/href/最近稳定祖先 anchor),随宏 JSON 保存,供回放时主选择器命中 ≠1 通用重定位
- ✅ 录制经 `sendToHost` 回传宿主,渲染层实时累积步骤并展示
- ✅ 导航后自动「重新武装」录制器(保证翻页后操作仍被记录)
- ✅ 新标签页归一:`<webview allowpopups>`(关键前置开关,缺则 Electron 源头丢弃弹窗、handler 不触发)+ 主进程 `did-attach-webview`+`setWindowOpenHandler` 拒绝弹窗并改在同一 webview 内 `loadURL`,录制连续不断、后续操作正常记录;回放端 `context.on('page')` 跟随弹窗切换活动页保持一致
- ✅ 步骤行右键菜单:「标记翻页操作」可将步骤标记为翻页特殊标识并设总页数 N(取消标记/修改页数),标记行带高亮+「翻页 · 共N页」徽标(`pagination`/`pageCount` 字段随宏 JSON 保存)
- ✅ 步骤行右键菜单:「在此后添加滚动到底部」插入 `{type:'scroll-bottom'}` 专用步骤(用于触发无限滚动懒加载),`insertScrollBottom` 仿 `insertPause`;需多次加载就插多条
- ✅ 步骤行右键菜单:「在此后添加等待页面加载完成」插入 `{type:'wait-for-load'}` 专用步骤,`insertWaitForLoad` 仿 `insertScrollBottom`;回放到此等 `load` 事件(整页加载完毕)后再继续
- ✅ 步骤行右键菜单:「修改填写内容」仅对 `fill` 步骤出现,就地编辑 `value`(填写内容)字段,无需重录/改 JSON。`showFillValueInput` 仿 `showPauseReasonInput`(菜单内转文本输入框,预填当前 value,回车/确定生效),与 pause.reason 不同——**不 trim、不删字段、允许空串**(填写内容可能含前后空格且语义上必填);改后 `renderSteps()` 即刷新、点「保存宏」落盘。选择器修正仍由既有「重新点选此步骤的选择器」覆盖
- ✅ 元素拾取器(picker)+「等待元素出现」步骤:步骤行右键「在此后添加等待元素出现(点选)」→ 进入拾取模式,在 `<webview>` 里 hover 高亮 + 点选目标元素 → `webview-preload.ts` 用 `generateSelector` 生成选择器,`sendToHost('picker-result',…)` 回传,renderer 插入 `{type:'waitForSelector',selector}` 步骤(复用既有 `waitForSelector` 类型与回放)。拾取点击被 `preventDefault` 拦截不跳转、`if(picking)return` 不录成 click;拾取期间临时挂起录制、结束恢复;ESC 取消;导航后 `dom-ready` 兜底清理悬挂位置。开关通道 `toggle-picker`(仿 `toggle-recording`)。**已解耦为通用「取选择器+回调」服务**:renderer 侧用 `requestPick(onPicked)` 进入拾取、`pendingPick` 回调槽取代写死的 `pickerInsertAt`,`picker-result` 回来后交给发起方登记的回调(用途与拾取分离),为后续消费者(步骤选择器修正 / AI 规则各 selector 点选填充)铺路;现有「等待元素出现」「等待元素可点击」均是它的消费者,行为零变化
- ✅ 步骤板块 UI 打磨:步骤行 hover 高亮(淡灰底+左侧蓝竖条+平滑过渡),翻页标记行 hover 底色协调加深,翻页徽标升级为渐变 chip(`»` 前缀+阴影+字重),右键菜单项/输入框统一 hover 与 focus 反馈
- ✅ 连续滚动步骤折叠展示(纯前端,`renderer.ts`+`index.html` CSS):无限滚动录制易产生大量连续 `scroll` 步骤淹没视野。`renderSteps()` 由 `forEach` 改 `while` 分组扫描,探测连续 ≥2 条可分组滚动(`isGroupableScroll`:`type==='scroll'&&!pagination`,翻页标记滚动单独成行不折叠),默认只显示组首 + 行尾「连续滚动 N 条 ▾」可点击标识(`.scroll-group-toggle`),点击展开全部子项(`.scroll-group-item` 缩进淡色)/收起。折叠态用 `expandedScrollGroups`(`WeakSet<Step>`,按组首对象引用为锚,非下标,插入/删除/拖拽后仍命中、加载新宏自动 GC)。每行 div 创建逻辑抽 `createStepLine(step,i)`(组首/子项/普通行共用,事件仍绑真实下标,右键/拖拽/徽标零改动)。**实时适配**:分组每次 render 按当前 `steps` 重算,已展开组中间插入新步骤 → 拆成「前段(组首引用不变仍展开)+ 新步骤 + 后段(新组首默认折叠)」,现有 `addStep`/`insertXxx`/`moveStep` 本就调 `renderSteps` 故自动生效。数据(`steps`/`buildMacro`)与回放完全不变
- ✅ 提取规则面板可折叠、默认收起(`.collapsible.collapsed`,点标题 `classList.toggle('collapsed')` 展开/收起,箭头指示状态):该框多由 AI 生成、用户极少手改,收起省空间并防误操作;AI 生成后保持收起不自动展开
- ✅ 右侧各板块统一可收起:在原有「提取规则/插件/浏览器登录态」基础上,补齐「AI 提取(`#ai-panel`/`#ai-title`)」「录制步骤(`#steps-panel`/`#steps-title`)」两块的 `collapsible` 折叠;通用折叠规则补全各 body(`#extract`/`.browser-body`/`.ai-body`/`.plugin-body`/`#steps`)的 `display:none`(顺带修复插件面板原先点标题不真正收起的遗漏);步骤面板收起时 `.steps-panel.collapsed{flex:0 0 auto;min-height:0}` 不再占弹性高度。点各自标题 `classList.toggle('collapsed')`
- ✅ 步骤行拖拽排序:每个 `.step-line` 设 `draggable`,HTML5 拖拽(dragstart/dragover/drop/dragend)上下拖动调整顺序,拖动时半透明 + 蓝色插入指示线;复用 `steps` 数组 + `renderSteps()` 重绘,排序结果随「保存宏」落盘;与右键菜单并存,纯前端(`renderer.ts` 的 `moveStep`)
- ✅ 录制步骤撤销/重做(Ctrl+Z 撤销 / Ctrl+Shift+Z·Ctrl+Y 重做,通用):`renderSteps()` 顶部 `captureHistoryOnRender()` 自动 diff `steps` JSON 快照记账(所有 mutation 都紧跟 renderSteps、非改动型渲染不入栈),`undoSteps`/`redoSteps` 反序列化快照重渲染,`applyingHistory` 防自记账、新改动清 redo;document `keydown` 监听焦点在可编辑控件时放行原生撤销。覆盖录制/排序/删除/插入/改内容/重新点选/AI 校正/加载宏等一切 steps 改动;会话内内存栈上限 100,不持久化。纯 `renderer.ts`+`index.html`
- ✅ 主页面左右宽度可拖动调整:`.browser-panel` 与 `.sidebar` 间插入分隔条 `#main-divider`(`col-resize` 光标+hover/拖动高亮),`renderer.ts` 的 `setupMainDivider` 监听 mousedown/mousemove/mouseup 实时改写 `.sidebar` 的 `flex`/`width`(右栏限 [240px, 75%·主区宽]),拖拽期间 `.main.resizing webview{pointer-events:none}` 遮罩防 webview 吞事件;宽度存 `localStorage`(`macro.sidebarWidth`)下次启动恢复
- ✅ 浏览器板块视觉隔离:webview 外包 `.browser-panel`(整圈边框+圆角,`.main` 加 padding/gap 与工具栏/侧栏/日志区拉开间隙),板块头部 `.browser-header` 蓝色左强调条 + 内联 Chromium SVG 图标 + 「Chromium · 内置」名称徽标,标明录制/预览所用浏览器内核

### 待办

- 📋 录制 `waitForSelector` 的自动判定(当前不自动录制,见已知问题)
- 📋 步骤可视化编辑(删除 / 拖拽排序已支持)
- 📋 录制点击时区分「会导航的点击」并智能插入等待
- ✅ 通用拾取器消费者①:步骤行右键「重新点选此步骤的选择器」修正点错/不准的 click/fill/waitForSelector(复用 `requestPick`,对象引用定位,click 同步更新 fingerprint)
- 📋 通用拾取器的后续消费者②:AI 提取面板 listSelector/字段/actionSelector/detailFields 点选填充(免手敲 JSON)

## 模块:回放引擎(MacroRunner)

### 核心功能

- ✅ 读取 JSON 宏,Playwright 启动 Chromium(headed)逐步执行
- ✅ 每种 step 独立处理方法(goto / click / fill / press / scroll / waitForSelector)
- ✅ 每步执行前后打印中文日志(第 N/总 步)
- ✅ 出错截图保存到 `errors/`,返回结构化错误(步骤索引 / 类型 / selector / URL / 原因)
- ✅ 回放全局默认超时(默认 60s,`MACRO_TIMEOUT` 毫秒可覆盖):`page.setDefaultTimeout` / `setDefaultNavigationTimeout`,解决点击后慢页导航等待超时
- ✅ 翻页步骤处理:正常回放跳过被标记的翻页步骤;收集所有翻页步骤构造「翻页序列」(`turnPage` 闭包按文档顺序执行,执行后 `waitForLoadState('domcontentloaded')` 等整页导航稳定再交回提取,防半载页少采/旧分页器误判),总页数取各步 `pageCount` 最大值,传入提取流程驱动翻页
- ✅ click 通用降级:`handleClick` 先 `locator(selector).count()`,唯一命中走主路径;命中 0/多个/非法时若 step 带语义指纹则 `relocateByFingerprint`(anchor 缩小 → 文本精确 → aria → href,取首个可见可用元素)通用重定位;无指纹/重定位失败回退原生 click 沿用出错路径。翻页 click 自动受益,无翻页专用代码
- ✅ 人工介入暂停(`pause` 步骤):回放到该步阻塞,有头浏览器停住等用户在窗口里手动操作(登录/验证码/扫码),点「继续」恢复;依赖倒置——`MacroRunner` 构造注入 `onPause` 回调(core 层不依赖 Electron,无回调默认放行),主进程用 `runId` 隔离的 `resume-macro` 信号恢复;可选 `timeout` 防无人值守永久挂死,超时落入现有出错截图流程;窗口关闭中断回放(`wc.once('destroyed')` reject)
- ✅ 滚动到底部(`scroll-bottom` 步骤):普通顺序步骤,回放到此 `handleScrollBottom` 取页面实际高度 `window.scrollTo(0,document.body.scrollHeight)` 滚到底 + 固定等 1s 懒加载就绪;用于无限滚动页(录不出固定座标),需多次加载就插多条
- ✅ 等待页面加载完成(`wait-for-load` 步骤):普通顺序步骤,回放到此 `handleWaitForLoad` 调 `page.waitForLoadState('load')` 等整页(DOM+所有资源)加载完毕再继续;超时只告警不致命(防轮询型站点 `load` 永不触发挂死),沿用全局默认超时
- ✅ 等待元素可点击(`waitForClickable` 步骤):比 `waitForSelector` 的 visible 更强,要求元素**可交互**(尺寸非零、非 disabled/aria-disabled、视口内时中心点未被遮罩遮挡)。`handleWaitForClickable` 用 `page.waitForFunction` 在页面内做**纯只读判定**、轮询到通过或超时——**不用 trial click**(后者会 scroll into view 改变滚动位置,对本项目 `scroll-bottom`/无限滚动敏感);零副作用(不滚动/不派发事件/不改 DOM)。超时致命(同 waitForSelector 强前置语义);视口外元素降级为只判尺寸+disabled。用于「透明遮罩盖住内容」「disabled 按钮变 enabled」等下一步非点击的同步场景(下一步若是 click,Playwright click 自带 actionability auto-wait 已兜住,无需本步骤)

- ✅ 登录态复用(三条可叠加 UI 开关,默认关,存 `browser-config.json`):①持久化回放 profile——Playwright 改 `launchPersistentContext(userDataDir)`,跨次回放复用同一目录(含 cookie/localStorage),一次登录长期有效;②录制→回放 cookie 注入——回放前从 webview 默认 session 导出 cookies,转 Playwright 格式后 `addCookies` 注入;③录制→回放 localStorage 注入(`injectRecordingLocalStorage`/`#bc-localstorage`)——主进程持有 guest webContents,`executeJavaScript` 读「录制 webview 当前页 origin」的 localStorage,回放端独立 `addInitScript` 按 origin 精准注入(限当前页 origin)。`MacroRunner` 第 4 参 `SessionOptions{userDataDir?,cookies?,localStorage?}`(core 不依赖 Electron,来源/转换在主进程),目录可在侧栏「浏览器登录态」面板勾选/选目录
- ✅ 回放反检测加固(Playwright 自带「Chromium for Testing」易被反爬识别):①加固启动参数(`--disable-blink-features=AutomationControlled` 等 + `ignoreDefaultArgs:['--enable-automation']`);②内核优选回退链(`preferSystemChrome` 开时 `chrome`→`msedge`→捆绑 Chromium 逐个 try/catch 回退);③`addInitScript` 抹 `navigator.webdriver`、补 `window.chrome`/`permissions.query`/`languages`/`plugins`;④仅回退捆绑 Chromium 时规整 UA/locale/viewport。开关 `browser-config.json.useSystemChrome`(默认开)+ UI `#bc-chrome`。后续遇 Cloudflare/DataDome 深度指纹可升级 patchright

### 待办

- 📋 步骤级超时与重试策略可配置
- ⚠️ localStorage 注入仅覆盖录制 webview「当前页 origin」,跨多 origin 的 localStorage 暂不迁移(复杂多域登录靠持久 profile 兜底)

## 模块:数据提取与导出

### 核心功能

- ✅ `extract(page, config, pagination?, downloads?)` 支持 single / list / list-detail / list-action 四种模式(list-detail:列表页收基础字段,按 `detailLinkField` 所选字段取详情链接,再逐个进详情页抓详情字段合并成行)
- ✅ list-detail 详情入口改为「用户选字段」:`detailLinkField`(字段名,引用 `fields`)取代 AI 生成的 `detailLinkSelector`;`collectListDetailPage` 用所选字段的 selector 在项内取 href 绝对化(字段缺失/留空→取列表项自身);彻底移除 detailLinkSelector,旧宏需重新生成
- ✅ list-action 模式(列表逐项动作):遍历 `listSelector` 逐项点 `actionSelector`(留空点项本身),每点一项等这次下载开始(`waitForNext`)再点下一项,超时只告警继续;复用翻页上下文逐页处理;不产出数据行,下载结果经 `RunResult.downloads` 回传
- ✅ 通用下载捕获(context 级原语,对所有 mode 生效):回放 context 统一 `acceptDownloads:true`,`core/download-manager.ts` 的 `DownloadManager` 挂 `context.on('download')`,用 `suggestedFilename()` 落盘 `downloads/`,重名加 `(N)` 去重;暴露 `savedPaths`/`count()`/`waitForNext(timeoutMs)`。单个下载按钮等普通场景亦受益
- ✅ 多页采集:list / list-detail 接收翻页上下文(`{ totalPages, turnPage }`),按总页数逐页采集,每页采完执行翻页序列并等待 `listSelector` 就绪;无翻页时按单页处理(向后兼容)
- ✅ list-detail 先收完整列表再进详情:跨所有页把列表项+详情链接全部收集后,才统一逐个进详情页抓取(保证完整列表优先)
- ✅ 字段类型 text / html / attr / href / src,缺失返回空串(不截断数据)
- ✅ `exportToExcel(rows, path)` 首行字段名 + 数据行
- ✅ 导出 Excel 弹出保存对话框可选路径(默认目录 `exports/`、默认名 `result-{时间戳}.xlsx`),导出成功后 `shell.showItemInFolder` 自动打开所在文件夹并高亮该文件;取消导出返回 null 并提示

### 待办

- 📋 自动识别列表结构(免手动配置)
- 📋 CSV 导出

## 模块:后处理器(回放产出后的定制处理)

> 轻量「type → {manifest,handler}」注册表机制,非通用插件框架;**UI 的可选插件列表由注册表驱动(热插拔)**,为 list-action 下载等场景的定制下游处理提供可插拔扩展点,不动 mode 分发。

### 核心功能

- ✅ 后处理器注册表 `core/post-processors/index.ts`:`registerPostProcessor(manifest, handler)` + `runPostProcessors(specs, ctx)` + `listPostProcessors()`(按序执行、单个失败不致命、core 层不依赖 Electron);`Macro.postProcess?:PostProcessSpec[]`(`{type,options?}`)挂配置,`RunResult.postProcessed?:PostProcessResult[]`(`{type,output?,message}`)回传结果,`PostProcessorManifest`(`{type,label,description}`)驱动 UI
- ✅ 首个后处理器 `merge-zip-excel`(**多格式**):list-action 逐项下载得到一批表格(zip 内的 csv/xls/xlsx,或直接下载的裸表格),解压(`adm-zip`,从 entry 直取 Buffer)→ **SheetJS(`xlsx` 库)通用读** csv/xls/xlsx(取首个 sheet,`sheet_to_json({defval:'',raw:false})`)→ 堆叠合并(union 列、缺列填空,复用现有 `exportToExcel` 写盘)→ 产出 `exports/merged-<时间戳>.xlsx`;**csv 编码 UTF-8 主 + GBK 兜底**(`iconv-lite`,去 BOM);`options.addSourceColumn` 为真加「来源文件」列(默认关,无 UI 入口);只处理本次 `result.downloads`,不扫历史旧文件。SheetJS 装官方已修复版(CDN tarball,避开 npm 0.18.5 安全告警)
- ✅ 主进程串接:`main.ts` 的 `run-macro` 在回放成功且 `macro.postProcess` 非空时 `runPostProcessors`,`shell.showItemInFolder` 优先定位合并产物;IPC `list-plugins`→`listPostProcessors()`、`run-plugin`(弹文件夹选择→对其内文件直接跑插件,不回放),`preload` 暴露 `listPlugins()`/`runPlugin()`
- ✅ UI——**注册表驱动的可选插件列表(热插拔)**:侧栏 collapsible 面板 `#plugin-panel`(青绿 + 🧩 徽标),`#plugin-list` 由 `loadPlugins()` 拉 `listPlugins()` 动态渲染每个插件一行(`.plugin-row`:复选框 + 名称 + **「直接运行」按钮**,下方说明)。**两种触发**:(a) 勾选 → `buildMacro` 写 `macro.postProcess`(随宏保存)→ **随主「运行」执行**,结果走公共 `reportRunResult`,加载宏 `refreshPluginSelection` 回显;(b)「直接运行」`runPluginDirect` → **不跑宏**,经 IPC `run-plugin` 弹**文件多选**(限 zip/csv/xls/xlsx,默认 downloads/),对所选文件直接合并(可直接挑 zip 压缩包)。选择器属提取规则,插件只对文件/下载产物通用
- ✅ 离线自检 `scripts/verify-merge.mjs`(多格式:zip 内 xlsx + UTF-8/BOM csv + 裸 csv + GBK csv,断言合并 6 行、列并集 `[名称,价格,产地,状态]`、各编码中文不乱码,已过);真实样本实测 20 个 payment zip 合并 4506 行通过

### 待办

- 📋 更多后处理器(如下载文件改名、按规则去重、合并产物推送)——加一个自注册带 manifest 的模块即自动上架插件列表
- 📋 插件参数的图形化配置(当前插件项仅勾选启用,无参数 UI)

## 模块:AI 提取(对接 openclaw agent 生成规则)

### 核心功能

- ✅ openclaw 客户端 `core/openclaw-client.ts`:WebSocket 连本机 OpenClaw Gateway(`ws://127.0.0.1:18799`)+ Ed25519 签名认证(v3 payload)+ `chat.send`(`deliver:false`)取草稿,参考 aiAgentServicer 的 `test_ws_send.js` / `oclaw-client.js`
- ✅ 专用 agent `webextract`(`openclaw agents add` 创建,workspace 的 SOUL.md/IDENTITY.md 定位为「只输出 ExtractConfig JSON 的提取规则生成器」)
- ✅ 调用层 `core/ai-extract.ts`:profile 列表式,每档指定一个 openclaw agent 目标(`agentId` + `sessionKeyPrefix`),按 `profileId` 选用;单次请求即连即走
- ✅ HTML 发送前降噪(去 script/style/注释,不截断长度);从回复剥出 JSON(处理 ```json 围栏),解析为 ExtractConfig
- ✅ 配置文件 `ai-config.json`(项目根,首次运行自动生成);系统/提示词模板、超时、连接覆盖可配
- ✅ 自检脚本 `scripts/test-ai.mjs`(已实跑:对接 webextract,24s 产出合法 list 规则)
- ✅ IPC `ai-list-profiles` / `ai-generate-extract` + preload 暴露 + UI「AI 提取」区(agent 下拉 + 需求输入 + 生成按钮)
- ✅ 渲染层:抓当前 `<webview>` 页面 HTML → 调 openclaw agent → 规则回填「提取规则」框
- ✅ 目标 mode 选择:「AI 生成规则」旁加 mode 下拉(single/list/list-detail/list-action),所选模式作为「前提」注入提示词;list-detail 需「提取规则」框已有合法 list 规则,并把该 list 规则带给 agent 作基础增量补 detail;SOUL.md 已补 list-detail 结构定义
- ✅ list-detail 详情入口字段选择:选 list-detail 时显示 `#ai-detail-link-field` 下拉(选项来自现有 list 规则 `fields[].name`,`refreshDetailLinkFieldOptions`),用户选定字段生成时写入 `detailLinkField`(AI 不再产详情链接)
- ✅ list-detail 校验改为「在详情页验 detailFields」:规则在详情页生成(AI 需读详情页 HTML),`verifySelectors` 对 list-detail 只整页校验 `detailFields` 命中数,跳过 listSelector/list 字段/详情入口 href(它们属列表页、在生成 list 规则时已验过);消除「listSelector 0 命中→无效重生成」与「取不到 href」误报,修复回路改去修真正的 detailFields。详情入口字段改为信息说明(回放时列表页生效),不再校验
- ✅ list-action 的 AI 一等支持:`buildModeHint` 加 list-action 分支(直接喂结构 `{mode,listSelector,actionSelector}`,无需改 webextract 的 SOUL.md,不需 baseRules);`GenerateInput.mode` 与 UI 下拉、renderer 断言同步补 list-action
- ✅ list-detail 选项按前提条件自动禁用:`refreshAiModeOptions()` 依据 `parseValidListRules()` 实时切换该 option 的 `disabled`(禁用态文案提示「需先填合法 list 规则」),在规则框 `input` 及 AI 生成/加载宏/init 三处程序化赋值后刷新;替代原「选中后报错回退」的不友好交互
- ✅ AI 板块 UI 打磨:面板标题紫色强调条+圆点,引擎/模式下拉与需求框统一 focus 紫光环,生成按钮按下下沉+阴影过渡,生成中状态区显示旋转加载圆环(`setAiBusy` 切换 `ai-loading` class),生成结果状态文字成功转绿/失败转红(`ok`/`err` class)
- ✅ 上传 `ai-config.json`:AI 面板「上传配置」按钮,主进程弹文件框选 json → core `validateAiConfig` 严格校验格式(profiles 非空/各字段类型/id 唯一/defaultProfile 一致/openclaw.identity 齐全/cleanHtml 布尔等,失败返回中文原因)→ 通过才 `importAiConfig` 覆盖生效(覆盖前备份 `ai-config.json.bak`),即时刷新引擎下拉;校验失败不覆盖原文件。链路 core→main(`import-ai-config` IPC)→preload→renderer

### 待办

- 📋 生成后当场试提取 + 样例值预览(用户暂缓)
- 📋 隐藏 JSON、改为纯可视化字段列表(后续迭代)
- 📋 配置档/agent 目标的图形化设置界面(当前手改 ai-config.json)
- 📋 端到端 GUI 验证(`npm run dev` 真实网页点「AI 生成规则」)

## 模块:AI 校正选择器(对接 selector-fix agent 修复脆弱选择器)

### 核心功能

- ✅ 专用 agent `selector-fix`(`openclaw agents add` 创建,workspace `~/.openclaw/workspace-selector-fix`,SOUL.md/IDENTITY.md 定位「只输出 `{"selector":"..."}` 的选择器修复器」,完整〈选择器质量准则〉为单一可信源:禁用 FB Stylex `x…`/Twitter `r-…`/React `_r_·_R_·:r:·mount_` 随机 id,优先 data-*/语义 id/aria/文本/语义 class,善用稳定祖先作用域,可输出 `xpath=`)
- ✅ `ai-config.json` + `DEFAULT_CONFIG` 加 `selector-fix` profile(`agent:selector-fix:macro:selector`,timeout 90s)
- ✅ 客户端 `fixSelector`(`core/ai-extract.ts`,复用 `OpenclawClient`/`extractJson`/`SELECTOR_QUALITY_GUIDE`;入参 `{current,reason,elementHtml,ancestors,feedback?,sessionKey?}` → `{selector}`),webextract 的 `generateExtract` 零改动
- ✅ IPC `ai-fix-selector`(`main.ts`)+ `preload.aiFixSelector`
- ✅ renderer 校正回路(基于实时录制 webview DOM):`locateAndSnapshot`(试选择器→失效用指纹定位→打临时标记取 outerHTML+祖先链)/`verifyFixed`(恰好命中 1 个且是被标记元素,`xpath=` 走 `document.evaluate`)/`clearFixMarker`/`fixStepSelector`(最多 3 轮,带反馈复用 sessionKey),入口:**仅步骤右键单步**(原工具栏 `#ai-fix-all` 全局批量按钮已移除:跨页面录制的元素批量校正大多跳过、实用性低;保留 `fixStepSelector`/agent/IPC)
- ✅ 方案 A:单步校正改为**离线、与当前页无关**——录制时 `buildElementContext(el)` 抓元素上下文 `{outerHTML,ancestors,contextHtml(带 data-macro-cap 标记的邻域子树)}` 存宏同名旁车 `<宏名>.captures.json`(宏本体不变);`fixStepSelector` 有上下文走 `fixWithCapture`(喂上下文给 `aiFixSelector`、`verifyAgainstCapture` 用 DOMParser 离线验证),无则回退 `fixWithLiveDom`(旧宏)。上下文按内存 `cid`+`Map` 关联(抗 undo);`buildMacro` 剥 cid、`buildCaptures`/`relinkCaptures` 存取旁车。解决「元素在别的页面、当前页找不到」的结构性天花板
- ✅ 非 click 步骤补语义指纹(`FillStep`/`WaitForSelectorStep`/`WaitForClickableStep` 加 `fingerprint`,preload fill 提交补 `buildFingerprint`,renderer insert 保存 picker 回传的 fingerprint),使失效选择器可重定位(仅新录制生效)
- ✅ 自检脚本 `scripts/test-selector-fix.mjs`(实跑:把 `#_r_3_ > …button.x1lliihq` 校正成 `button[data-testid="open-registration-form"]`)

### 待办

- 📋 旧宏(fb.json)非 click 步骤无指纹,失效选择器在当前页也不命中则只能跳过/手动重选(需重录补指纹)
- 📋 校正依赖当前 webview 已加载对应页面/状态(跨导航步骤需切页后再校正)
- 📋 端到端 GUI 验证(`npm run dev` 打开 FB 登录页加载 fb.json 点批量校正)

## 基础设施 & 工程质量

### 已完成

- ✅ TypeScript 双 tsconfig 构建(主进程 CommonJS + 渲染进程 ESM),纯 tsc 无打包器
- ✅ `scripts/copy-assets.mjs` 跨平台拷贝静态资源
- ✅ `postinstall` 自动下载 Playwright Chromium
- ✅ 主/渲染进程通过 IPC + contextBridge 通信,Playwright 不暴露给渲染进程
- ✅ README、示例宏 `examples/demo-macro.json`
- ✅ 健壮启动脚本 `scripts/start.mjs`(剥离 ELECTRON_RUN_AS_NODE,跨 shell/环境可启动)
- ✅ 启动加载 UI:主进程 `backgroundColor` + `show:false`/`ready-to-show` 延迟显示,渲染层内联加载遮罩(`#boot-overlay`,复用 `ai-spin` 动画),`init()` 改 async 等配置加载完淡出,消除启动白屏
- ✅ 打包浏览器缓存复用:`copy-playwright-browser.mjs` 用 `build/ms-playwright/.pw-version` 版本标记幂等,playwright 版本未变时跳过下载、复用本地已下的 Chromium(多次打包 0 下载),版本变更才清空重下
- ✅ 无头自检脚本 `scripts/verify-core.mjs` + `MACRO_HEADLESS` 环境变量(已实跑验证 20 行提取 + Excel 导出)

### 待办

- ✅ electron-builder 打包为可分发安装包(NSIS,自带 Chromium,`npm run dist` 一键出包,详见 `打包指南.md`)
- 📋 单元测试(selector-generator / extractor)
- 📋 安装包代码签名(消除 SmartScreen「未知发布者」提示)
- ✅ AI 连接信息可内联到 `ai-config.json`(`openclaw` 段支持 url/token/identity,目标机无需铺 `~/.openclaw`)
- 📋 AI 提取共享身份自动下发(目前仍需手动把 identity 填进目标机 `ai-config.json`,或为同事单独签发 device 身份)
- 📋 Docker 化无头回放/提取/导出引擎(录制仍在本地;本期暂缓)

## 已知问题 & 历史修复

| 日期 | 模块 | 问题 | 状态 |
| -- | -- | -- | -- |
| 2026-07-02 | 录制/拾取 | 拾取模式无进入提示且 ESC 需先点浏览器获焦才生效:①ESC 监听只在 webview guest 内(`onPickerKey`),焦点在主窗口时按 ESC 无效;②进入后主界面零变化,用户不知已进入、可能误点。修复(仅前端 `renderer.ts`+`index.html`,不碰 preload/core):进入拾取即 `webview.focus()` 把焦点交给内嵌浏览器 + `.app` 加 `.picking` 类(工具栏/侧栏/日志区 `pointer-events:none`+变暗、浏览器面板高亮描边、顶部弹蓝色横幅「拾取模式进行中…按 ESC 取消」+取消按钮);新增 `enterPickingUI/exitPickingUI/cancelPick` 三辅助 + document 级 ESC 监听(主窗口有焦点时也能退出)。两条退出路径(guest 内 ESC 走 preload→`picker-result`;主窗口 ESC 走 `cancelPick`→`toggle-picker false`)均以 `pendingPick` 为闸门,幂等无重复。`npm run build` 双 tsconfig 通过 | ✅ 已修复 |
| 2026-06-22 | 启动 | 环境含 `ELECTRON_RUN_AS_NODE=1` 时 `electron .` 退化为 Node,app 为 undefined | ✅ 已修复(scripts/start.mjs 子进程剥离该变量) |
| 2026-06-22 | 录制 | `waitForSelector` 暂不自动录制,依赖手动添加 / Playwright auto-wait | ⚠️ 已知限制 |
| 2026-06-23 | AI 提取 | 初版用本机 claude/codex CLI 生成规则(claude 实跑通过) | 🔄 已被 openclaw 方案取代 |
| 2026-06-23 | AI 提取 | 改为对接 openclaw agent(WS+Ed25519),创建专用 webextract agent;整链实跑通过(24s 出规则) | ✅ 已完成 |
| 2026-06-23 | 回放引擎 | 点击后慢页导航等待("waiting for scheduled navigations")默认 30s 不足致 click 超时 | ✅ 已修复(全局默认超时提至 60s,`MACRO_TIMEOUT` 可调) |
| 2026-06-23 | AI 提取 | 新增目标 mode 选择(single/list/list-detail)+ 提示词注入模式前提;list-detail 带现有 list 规则增量补全;SOUL.md 补 list-detail 结构 | ✅ 已完成 |
| 2026-06-24 | 录制/回放/提取 | 翻页标记功能:步骤右键标记翻页+设总页数,回放跳过、提取时驱动翻页多页采集;list-detail 改为先收完整列表再进详情。已实跑验证(list 3页60行、list-detail 2页40项先收后详) | ✅ 已完成 |
| 2026-06-24 | UI | AI 提取板块与录制步骤板块样式/交互美化(focus 光环、按钮按下、加载动画、状态分色、步骤行 hover、翻页标记/徽标质感升级),纯样式+少量 class 切换,`npm run build` 通过 | ✅ 已完成 |
| 2026-06-24 | 导出/UI | 导出 Excel 改为弹保存对话框选路径+导出后 `shell.showItemInFolder` 打开文件夹高亮文件(取消返回 null);提取规则面板改为可折叠默认收起,`npm run build` 通过 | ✅ 已完成 |
| 2026-06-24 | AI 提取 | list-detail 模式选项改为前提不满足时直接禁用(`refreshAiModeOptions` 实时联动+禁用文案),移除原选后报错回退逻辑,`npm run build` 通过 | ✅ 已完成 |
| 2026-06-24 | 回放/UI | 新增人工介入暂停 `pause` 步骤:右键「在此前/此后插入暂停」或工具栏「插入暂停」标记,回放到此弹模态框等用户手动操作后点继续;`MacroRunner` 注入 `onPause`、主进程 `runId` 隔离 resume、可选 timeout 超时;core 层 3 用例验证(onPause 调用/超时抛错/默认放行)+ 无头回归 20 行通过 | ✅ 已完成 |
| 2026-06-24 | 回放/UI | 登录态复用:持久化回放 profile(`launchPersistentContext`)+ 录制 webview cookie 注入(`addCookies`),侧栏「浏览器登录态」面板开关/选目录,配置存 `browser-config.json`;`MacroRunner` 加 `SessionOptions` 第 4 参(向后兼容)。默认关回归 20 行通过 + 持久化分支实跑 20 行且目录生成 | ✅ 已完成 |
| 2026-06-25 | 打包 | 补 `npm run dist`/`pack:browser` 脚本,electron-builder 出 NSIS 安装包(285MB,自带 Chromium),实跑 win-unpacked 验证浏览器进包 | ✅ 已完成 |
| 2026-06-25 | AI 提取 | `ai-config.json` 原为懒生成(首次用 AI 才写),安装后找不到;改为启动即 `loadAiConfig()` 生成并打日志路径。实跑验证首次启动即生成 | ✅ 已修复 |
| 2026-06-25 | 文档 | `打包指南.md` userData 路径误写为 `%APPDATA%\网页宏录制工具\`,实际取 package.json name=`macro-recorder`;尝试 `setName`/`setPath` 改中文名打包态无效,回退并修正文档为 `%APPDATA%\macro-recorder\` | ✅ 已修复 |
| 2026-06-25 | AI 提取 | 原 `openclaw` 段只能配 url/token,身份写死读 `~/.openclaw`,目标机无法只靠 ai-config.json 用 AI。改为 `openclaw` 段支持内联 `identity`,`loadOpenclawConfig` 优先用配置值、缺项才回退本机文件(向后兼容)。空段回退/内联优先/无本机文件三场景均验证通过 | ✅ 已完成 |
| 2026-06-25 | 录制/回放 | 点击「新标签页打开」链接(target=_blank / window.open)录制不生效:webview 默认丢弃弹窗,新页操作无从记录。录制端 `main.ts` 加 `did-attach-webview`+`setWindowOpenHandler` 拒绝新窗、改在同一 webview 内 `loadURL`(点击照常录制、跳转后录制器自动重武装);回放端 `macro-runner.ts` 用「活动页」可变引用+`context.on('page')` 跟随弹窗,提取/翻页/错误截图改用活动页。`npm run build` 通过 | ✅ 已修复 |
| 2026-06-25 | 录制 | 上条修复后用 163 首页实测「世界杯」链接仍无反应:根因是 `<webview>` 缺 `allowpopups` 属性,Electron 在源头丢弃弹窗、`setWindowOpenHandler` 根本不触发。`index.html` 给 webview 加 `allowpopups`(布尔属性,attach 时读取),handler 方可生效。`npm run build` 通过 | ✅ 已修复 |
| 2026-06-25 | 打包/回放 | 打包版在干净目标机回放第0步报 `Executable doesn't exist at ...ms-playwright\chromium-1228\chrome.exe`。根因:`main.ts` 顶部 import macro-runner→playwright 在设 `PLAYWRIGHT_BROWSERS_PATH` 之前执行,playwright 初始化时已锁死默认缓存路径(浏览器实已正确打进 `resources/ms-playwright`,版本号也对)。修复:抽出 `src/main/runtime-bootstrap.ts` 作为 main.ts 第一个 import,在 playwright 初始化前设好环境变量。编译产物 require 顺序验证(bootstrap 先于 macro-runner)+ 无头回归 20 行通过 | ✅ 已修复 |
| 2026-06-25 | 录制/UI | 录制步骤板块每一项支持鼠标上下拖动排序(HTML5 拖拽 + 蓝色插入指示线,`moveStep` 改 `steps` 顺序随保存落盘,与右键菜单并存),`npm run build` 通过 | ✅ 已完成 |
| 2026-06-25 | AI 提取 | AI 板块新增「上传配置」按钮:选 `ai-config.json` → `validateAiConfig` 严格校验格式(profiles 非空/字段类型/id 唯一/defaultProfile 一致/openclaw.identity/cleanHtml 等,失败回中文原因)→ 通过才覆盖生效(先备份 `.bak`)并刷新下拉,校验失败不覆盖。core→main→preload→renderer 全链路,`npm run build` 通过 | ✅ 已完成 |
| 2026-06-25 | 启动/UI | 程序刚打开白屏:窗口在内容绘制前即显示且无加载提示。主进程 `BrowserWindow` 加 `backgroundColor:'#f3f4f6'`+`show:false`,`ready-to-show` 再 `show()`;`index.html` 加内联加载遮罩 `#boot-overlay`(居中旋转动画,复用 `ai-spin`),`renderer.ts` 的 `init()` 改 async,`await Promise.allSettled` 配置加载后淡出移除遮罩。`npm run build` 通过 | ✅ 已修复 |
| 2026-06-25 | 打包 | 每次打包都重新下载 ~690MB Chromium:`copy-playwright-browser.mjs` 无条件 `rmSync` 后重装。改为版本标记幂等——写 `build/ms-playwright/.pw-version`,版本匹配且 chrome.exe 在位则跳过下载复用缓存,仅首次/版本变更才清空重下。`npm run build` 通过 | ✅ 已修复 |
| 2026-06-26 | 回放引擎 | 回放用 Playwright 自带「Chromium for Testing」,`navigator.webdriver`/`--enable-automation`/缺 Chrome 品牌组件易被网站反爬识别。加固:①统一加 `--disable-blink-features=AutomationControlled` 等 args + `ignoreDefaultArgs:['--enable-automation']`;②内核优选回退链 `chrome`→`msedge`→捆绑 Chromium(`useSystemChrome` 默认开,try/catch 逐个回退并记日志);③`addInitScript` 抹 webdriver、补 chrome/permissions/languages/plugins;④仅捆绑回退时规整 UA/locale/viewport。新增 `browser-config.json.useSystemChrome` + UI `#bc-chrome`,全链路 plumbing。`npm run build` 通过 | ✅ 已修复 |
| 2026-06-26 | 提取/回放 | 新需求「列表逐项点按钮、每点一次下载一个文件」现有 single/list/list-detail 都满足不了(只读不点,且项目零下载处理)。以「通用不特供」两块能力实现:①通用下载捕获(context 级 `acceptDownloads`+新 `download-manager.ts` 的 `DownloadManager` 监听 download 落盘 downloads/、suggestedFilename 去重、`waitForNext` 节流);②新 `list-action` 提取 mode(逐项点 `actionSelector`,复用翻页,产出下载文件经 `RunResult.downloads` 回传)。贯通 `MacroRunner` 加 `downloadDir` 参、`extract` 加 `downloads` 参、`main.ts` 加 downloadsDir+`shell.showItemInFolder`、UI 下拉+结果状态;AI 一等支持(`buildModeHint` 加 list-action 分支)。新增 `examples/demo-list-action.json`。`npm run build` 通过 | ✅ 已完成 |
| 2026-06-26 | 录制/UI | 主页面左右栏宽度写死(右栏固定 360px)无法调整。在 `.browser-panel` 与 `.sidebar` 间加可拖动分隔条 `#main-divider`,`setupMainDivider` 监听鼠标拖动实时改右栏 `flex`/`width`(限 [240px,75%]),拖拽期间遮罩 webview 防吞事件,宽度存 `localStorage` 启动恢复。`npm run build` 通过 | ✅ 已完成 |
| 2026-06-26 | 录制 | 点「开始录制」会无条件 `steps=[]` 清空已录步骤,停止后想续录则现场全失。改为保留现场:删除清空逻辑,goto 智能去重补充(steps 为空补首个 goto;非空仅当当前页 URL 与最后一条 goto 不同才补,避免重复 goto)。`npm run build` 通过 | ✅ 已修复 |
| 2026-06-26 | 回放/UI | 部分站点登录 token 存 localStorage 而非 cookie,仅注入 cookie 回放仍未登录。新增「回放前注入录制 localStorage」开关(`injectRecordingLocalStorage`/`#bc-localstorage`,默认关):主进程 `did-attach-webview` 持有 guest webContents,`buildSessionOptions` 经 `captureRecordingLocalStorage()` 用 `executeJavaScript` 读录制 webview 当前页 origin 的 localStorage(仅 http(s)、非空),组装 `SessionOptions.localStorage`;回放端 `macro-runner.ts` 在 cookie 注入后用独立 `addInitScript`(导航前)按 `location.origin` 精准注入,逐条 setItem try/catch。限当前页 origin。`npm run build` 通过 | ✅ 已完成 |
| 2026-06-26 | 提取/list-action | 逐项点击后日志卡在「开始逐项点击」无输出(假卡死):`extractListAction` 循环内零日志,且 `click` 继承全局 60s 超时、坏选择器静默等满才报错,`waitForNext` 再静默 30s;另 `waitForNext` 在 click 后才注册存在漏接快下载的竞态。修复:循环内加逐项进度日志、动作点击用专属 `actionTimeout`(默认 30s)快速暴露坏选择器、下载等待改为点击前注册。`npm run build` 通过 | ✅ 已修复 |
| 2026-06-26 | 提取/list-action | 「未找到可点击的按钮」只输出一行无法判断原因(element-plus 选择器全项 0 匹配)。`extractor.ts` 新增只读诊断 `diagnoseMissingTarget`,在 0 匹配分支输出:①项内 `<button>` 总数及各 class/文本(上限 8 个)②选择器按空格分段累积探测、报告首处归零层级③末段结构性伪类(`:first-of-type` 等)剥离重测,命中则提示伪类元凶。全程 try/catch 不影响回放,日志限长。`npm run build` 通过 | ✅ 已完成 |
| 2026-06-26 | AI 提取 | AI 生成的选择器零验证、盲生成常踩动态类名/框架克隆 DOM/脆弱伪类(实测某 element-plus 报表页选择器全项 0 命中)。通用型加固:①`ai-extract.ts` 新增 `SELECTOR_QUALITY_GUIDE` 通用准则(优先 data-*/id/文本、禁用动态类名、慎用结构伪类、注意固定列克隆子树)无条件注入 message,不依赖既有 ai-config.json;②`GenerateInput` 加 `feedback`/`sessionKey`,`GenerateResult` 回传 sessionKey,支持多轮复用同一 agent 会话修复;③renderer 新增 `verifySelectors` 在录制 webview 内实测各选择器命中数(镜像 extractor 运行时语义:项内字段/动作以首个列表项为根测),生成流程改为「生成→实测→未命中带反馈重生成」自检回路(最多 2 次修复,失败不致命仍填最后一版并提示人工核对)。`npm run build` 通过 | ✅ 已完成 |
| 2026-06-29 | AI 提取/提取 | list-detail 不进详情页:webextract agent 把 `detailLinkSelector` 选成 `<head>` 的 `link[rel=canonical]`,运行时在「项内」找致 0 命中、详情 URL 空、退化为 list;自检回路因「项内任意一个命中即可」放行坏宏。改造:不再由 AI 指定详情链接,`ListDetailExtractConfig` 用 `detailLinkField`(引用 fields 字段名)取代 `detailLinkSelector`,`collectListDetailPage` 用所选字段 selector 取 href;UI 选 list-detail 时显示字段下拉供选,`buildModeHint` 去掉详情链接、SOUL.md 契约同步删除;`verifySelectors`/`evaluateChecks` 改为对所选字段非致命校验 href。彻底移除 detailLinkSelector 不做旧宏迁移,`macros/webscraper.json` 按要求保留。`npm run build` 通过 | ✅ 已修复 |
| 2026-06-29 | 录制/回放 | 翻页选择器脆弱:在分页器首页(仅 `<li class="next">`、无 Previous)录制 Next,生成的 `…ul > li > a` 因当时 `li` 无同标签兄弟未带定位锚点;翻页后出现 `<li class="previous">`,该选择器同时命中 Previous+Next 两个 `<a>`,严格模式报错或误点 Previous 往回翻。**通用**双层修复(非分页器专用):①录制端 `buildCssPath` 每段携带稳定 class(`li.next`)、`:nth-of-type` 仅在 class 不足以区分同标签兄弟时才用,新录制根治;②运行端 click 附语义指纹(`buildFingerprint`)+ `handleClick` 主选择器命中 ≠1 时 `relocateByFingerprint` 通用重定位(anchor→文本→aria→href)。旧宏靠改动①后重录一次修复。另修一处**翻页竞态**:`turnPage` 在执行翻页步骤后 `waitForLoadState('domcontentloaded')`,避免「在半载页采集少采、或读到旧分页器误判命中数」。`scripts/verify-pagination.mjs` 实跑验证:脆弱选择器 `.pager li a`(page2 命中 2)+ 指纹 anchor 重定位,quotes.toscrape.com 采满 3 页 30 行、日志出现「已用语义指纹(anchor)重定位」。`npm run build` 通过 | ✅ 已修复 |
| 2026-06-29 | AI 提取 | list-detail 生成时误报「取不到有效 href」并触发无效重生成:规则在**详情页**生成(AI 需读详情页 HTML 出 detailFields),而 `verifySelectors` 仍在详情页上验列表页选择器——listSelector 命中 0 致 evaluateChecks 判失败、空转 2 轮重生成,详情入口字段 `noItem` 误报 href,真正的 detailFields 反而没验。改:`verifySelectors` 对 list-detail 改为「整页只验 detailFields 命中」、置空 listSelector 跳过列表页选择器(它们在生成 list 规则时已验过),移除 `wantHref/hasHref` 死代码插桩与 href 误报块(改为 info 说明详情入口在回放/列表页生效)。另删 `buildModeHint` 里对最终规则零影响的「请勿输出 detailLinkSelector」冗余提示词(renderer 已无条件 `delete detailLinkSelector` 兜底)。运行端 href 取值不改。`npm run build` 通过 | ✅ 已修复 |
| 2026-06-29 | 后处理器 | 新需求「list-action 下载的 zip 内各一个 excel,合并成一张总表」属定制非通用,项目原本无任何 hook/pipeline、mode 分发是硬编码 if/else。不搭重型插件框架(过度设计)、不写死埋进 mode 分支(难找难扩),改加**轻量后处理器注册表**:`Macro.postProcess`+`RunResult.postProcessed` 类型、`core/post-processors/`(`index.ts` 注册表 + `merge-zip-excel.ts` 首个 handler,`adm-zip` 解压 + `exceljs` 读 xlsx + 复用 `exportToExcel` 堆叠合并 union 列)、`main.ts` 回放后串接。离线自检 `scripts/verify-merge.mjs` 实跑通过(3 表合并 6 行、列并集正确)。`npm run build` 通过 | ✅ 已完成 |
| 2026-06-29 | 后处理器/UI | 首版把合并做成 list-action 模式下的「下载后自动合并」复选框、挂在通用 AI 提取面板里,用户反馈「像通用型开关,不是定制入口」。改版:移除复选框,新增**独立「插件」面板** `#plugin-merge-panel`(青绿强调 + 🧩 徽标,视觉独立成块),含选择器输入 + 加来源列 + 「从提取规则导入」(复用 AI 的 list-action 选择器) + 「运行并合并」自包含跑完整流程;抽出公共 `reportRunResult` 与「运行」按钮共用结果日志。后端(注册表/handler/主进程/类型/自检)不变。`npm run build` + 离线自检通过 | ✅ 已完成 |
| 2026-06-29 | 后处理器/UI | 上版「独立插件面板」仍带选择器输入(与提取规则/宏重复)和独立「运行并合并」按钮(与主「运行」重复),用户指出「这不是插件,插件应是可勾选的列表、热插拔」。重做为**注册表驱动的可选插件列表**:注册表改存 `{manifest,handler}` 并加 `listPostProcessors`;`merge-zip-excel` 注册改传 manifest(label/description);`main.ts` 加 `list-plugins` IPC、`preload` 加 `listPlugins`;UI 拆掉选择器/导入/运行并合并,改 `#plugin-list` 由 `loadPlugins()` 动态渲染可勾选插件项,勾选写入 `macro.postProcess` 随主「运行」执行、随宏保存、加载回显;选择器留在提取规则,插件只对下载产物通用。新增插件只需 core 加自注册模块即自动上架(前端零改动)。`npm run build` + 离线自检通过 | ✅ 已完成 |
| 2026-06-29 | 后处理器/UI | 给插件加「直接运行」:不跑宏、直接合并文件。`main.ts` 加 IPC `run-plugin`,`preload` 加 `runPlugin`;插件行改 `.plugin-row`(左勾选+名称、右「直接运行」按钮),renderer `runPluginDirect` 调用并打印结果。勾选随宏运行(a)与直接运行(b)两种触发并存。**选择方式**:初版弹「选文件夹」,按用户要求改为弹**文件多选**(`openFile`+`multiSelections`,filters 限 `zip/csv/xls/xlsx/xlsm`,默认 downloads/),可直接挑 zip 压缩包,与插件「解压 zip…」简介一致;所选文件路径作 `downloads` 传 `runPostProcessors`。`npm run build` 通过 | ✅ 已完成 |
| 2026-06-29 | 后处理器 | 实跑发现 `merge-zip-excel` 只认 zip 内 `.xlsx`,而真实下载的 zip 内是 `.csv`(UTF-8/BOM、英文表头、含中文值)→ 全部「未找到 .xlsx 表格,已跳过」,合并为空。用户反馈「格式太单一」。改造:读取改用 **SheetJS(`xlsx` 库,装 CDN patched 0.20.3 避旧版告警)** 通吃 **csv/xls/xlsx**,**既认 zip 内条目也认裸下载文件**;csv 用 `iconv-lite` **UTF-8 主 + GBK 兜底**解码(去 BOM)再交 SheetJS 解析;写出仍用 `exportToExcel`。移除 exceljs 读取与 `cellToString`/`readXlsxBuffer`。`scripts/verify-merge.mjs` 扩为多格式用例(xlsx+UTF8/BOM csv+裸 csv+GBK csv,断言行数/列并集/各编码中文不乱码);真实样本实测 20 个 payment zip 合并 4506 行、表头与「支付已通知」完好。`npm run build` 通过 | ✅ 已修复 |
| 2026-06-29 | 提取/翻页 | list-action 配翻页后实测「2 页只有第 2 页执行了动作」。根因:三种翻页模式的循环都只在 `turnPage()` 后 `waitForSelector(listSelector)`,**首页处理之前无任何等待**,list-action 首页列表 AJAX 未就绪即 `items.count()=0` 整页被跳过(只剩末页因翻页后有等待才生效)。**通用化**修复(三模式共享、翻页标记机制不动、不加任何开关/面板):`extractor.ts` 抽 `waitListReady`(每页处理前等列表就绪,修首页)+ `turnPageAndSettle`(翻页后比对首行文本变化、等内容真正切换,消除读旧页),`list`/`list-detail`/`list-action` 循环各接两处;等待超时统一 `PAGE_SETTLE_TIMEOUT=30000` 且 catch 放行(真空页非致命)。`verify-pagination`(list 3 页 30 行)+ `verify-merge` 回归通过,`npm run build` 通过;bcpay live list-action 待用户手动复跑 | ✅ 已修复 |
| 2026-06-29 | 提取/重构 | 上条修复暴露三处翻页循环骨架完全复制粘贴(同一「首页缺等待」缺陷三发、修复三改、漏改即模式分裂)。抽公共高阶函数 `paginatedCollect(page,listSelector,pagination,processPage)` 收敛「逐页等就绪→处理本页→翻页等切换」骨架为单一事实来源,`list`/`list-detail` 阶段一/`list-action` 三处改为传「处理本页」回调,累加(rows/collected/clicked)留外层闭包。**纯结构重构、零行为变化**(等待/日志/翻页顺序逐字一致),新增提取模式只写回调、接线想漏都漏不了。`verify-pagination` 仍 3 页 30 行(首末行不变)+ `verify-merge` 通过,`npm run build` 通过 | ✅ 已完成 |
| 2026-06-30 | 录制/回放 | 无限滚动页录不出(录到的 `scroll` 是固定座标、翻页是「点下一页」置换型,均不适配)。新增专用步骤 `{type:'scroll-bottom'}`:步骤右键「在此后添加滚动到底部」插入(`insertScrollBottom`),回放执行到此 `handleScrollBottom` 滚到页面实际底部(`window.scrollTo(0,scrollHeight)`)+ 等 1s 懒加载;普通顺序步骤(正常回放即执行,需多次加载插多条)。`macro-types`/`executeStep`/两处 `describeStep` 同步,`npm run build` 通过 | ✅ 已完成 |
| 2026-06-30 | 回放 | 「滚动到底部」对 fixed/内部可滚动容器(`overflow:auto/scroll` 的 div)无效:`handleScrollBottom` 只滚 window,而真正滚动发生在内部元素时 window 不动→空操作、懒加载不触发。修复:改为 window 滚到底 + 扫描所有 `overflowY:auto/scroll/overlay` 且 scrollHeight>clientHeight 的容器各自 `scrollTop=scrollHeight`(设 scrollTop 派发 scroll 事件,兼容 scroll 监听型与 IntersectionObserver 哨兵型懒加载),`logInfo` 输出命中容器数便于诊断。仅改 `handleScrollBottom` 一函数,`npm run build` 通过 | ✅ 已修复 |
| 2026-06-30 | 录制/拾取 | 新增元素拾取器 +「等待元素出现」步骤:右键「在此后添加等待元素出现(点选)」进入拾取模式,webview 内 hover 高亮 + 点选目标元素,`webview-preload.ts` 用 `generateSelector` 生成选择器回传(`toggle-picker`/`picker-result` 通道),插入 `{type:'waitForSelector',selector}`(复用既有类型与回放,超时致命);拦截点击不跳转、`if(picking)return` 不录成 click、拾取期间挂起录制、ESC 取消、导航 `dom-ready` 兜底。`npm run build` 通过 | ✅ 已完成 |
| 2026-06-30 | 录制/拾取 | 拾取生成的选择器混入临时高亮类 `__macro_picker_hover__`(如 `div.col-sm-8.col-md-9.__macro_picker_hover__`、`ul.__macro_picker_hover__`),该类仅拾取时存在→回放命中 0、`waitForSelector` 超时失败。根因:`onPickerClick` 在被点元素仍带高亮类时即 `generateSelector`,而 `clearHover` 在其后的 `exitPicker` 内才执行;`isStableClass` 又未过滤该类。修复:生成选择器**前**先 `clearHover()`+`t.classList.remove(PICKER_CLS)`(根治在 picker 侧,不耦合 selector-generator)。旧坏步骤需删除重拾。`npm run build` 通过 | ✅ 已修复 |
| 2026-06-30 | 录制/拾取 | 拾取器不够通用:preload 侧已通用(回传干净 `{selector,fingerprint}`),但 renderer 侧把结果写死灌进 `insertWaitForSelector`(`startPicker`→`pickerInsertAt`→只能生成 waitForSelector),缺「发起方意图」抽象。**纯重构解耦**(仅机制、零行为变化、不碰 preload/IPC):`pickerInsertAt`→通用回调槽 `pendingPick:PickedHandler`、`startPicker`→`requestPick(onPicked)`、`picker-result` 改调发起方登记的回调、`dom-ready` 兜底同步;现有右键「等待元素出现」改为 `requestPick(sel=>insertWaitForSelector(at,sel))` 唯一消费者(用户感知一致)。renderer 保持自包含(handler 的 fingerprint 用 `unknown` 不引 core 类型)。为后续消费者(步骤选择器修正/AI 各 selector 点选填充)铺路。`npm run build` 通过 | ✅ 已完成 |
| 2026-07-01 | 录制/拾取 | 兑现拾取器解耦——加首个新消费者:步骤行右键「重新点选此步骤的选择器」,修正录制点错/AI 不准的 click/fill/waitForSelector(此前只能删了重录/手敲 JSON)。仅带 `selector` 的步骤显示该项,复用 `requestPick` 全部机制(拾取/挂起录制/ESC/导航兜底);回调用**对象引用+`indexOf`** 定位(拾取异步期间排序变化仍命中、被删则安全放弃),click 步骤**同步更新 fingerprint**(否则回放重定位依据旧元素走偏)。单文件 `renderer.ts`,不碰 preload/IPC/回放。`npm run build` 通过 | ✅ 已完成 |
| 2026-06-30 | 录制/UI | 无限滚动录制易产生大量连续 `scroll` 步骤平铺挤占视野(如第 8~11 步全是 scroll)。`renderer.ts` 的 `renderSteps` 由 `forEach` 改 `while` 分组扫描:连续 ≥2 条可分组滚动(`isGroupableScroll`,翻页标记滚动除外)默认只显示组首 + 行尾「连续滚动 N 条 ▾」可点击标识,点击展开/收起子项(缩进淡色);折叠态用 `WeakSet<Step>` 按组首对象引用追踪(非下标,插入/删除/拖拽后仍命中)。每行 div 创建抽 `createStepLine(step,i)` 共用(右键/拖拽/徽标零改动)。已展开组中间插入新步骤实时拆成「前段仍展开+新步骤+后段折叠」。纯前端、数据与回放不变,`npm run build` 通过 | ✅ 已完成 |
| 2026-07-01 | 回放/等待可点击 | `waitForClickable` 对 **XPath 选择器误超时**:判定函数在页面内用 `document.querySelector`(只认 CSS),而 `generateSelector` 对纯文本元素(如 `<p>`)产出 `xpath=` 前缀选择器 → querySelector 抛错被 catch 吞成「未就绪」→ 60s 超时失败。对比「等待元素出现」走 `page.waitForSelector`(认 xpath)正常,锤实根因是两步选择器解析器不同。修复:判定函数「找元素」步加引擎分诊——`xpath=` 前缀走 `document.evaluate`(纯只读,零副作用不变),否则原 `querySelector`(CSS 老路逐字不变),加 `nodeType!==1` 守卫;下游只读检查一字未改,每轮轮询重解析对重渲染免疫。`npm run build` + `verify-pagination`(3 页 30 行)回归通过 | ✅ 已修复 |
| 2026-07-01 | 回放/拾取 | 澄清「等待元素出现」实为 Playwright 默认 `state:'visible'`(只等出现且可见,不等可交互),补一个更强的**等待元素可点击**步骤覆盖「出现≠可点击」场景(透明遮罩盖住/disabled 未就绪/SPA hydration 未完成)。新增 `{type:'waitForClickable',selector,timeout?}`:步骤右键「在此后添加等待元素可点击(点选)」复用拾取器 `requestPick`;`handleWaitForClickable` 用 `page.waitForFunction` **纯只读判定**(尺寸/可见/非disabled/`elementFromPoint` 遮挡检测)轮询到通过或超时。**关键设计取舍**:用户要求零副作用,故**不用** Playwright `trial click`(其 actionability 会 scroll into view 改变滚动位置、可能误触无限滚动懒加载),改 evaluate 只读判定——不滚动/不派发事件/不碰渲染;视口外元素降级为只判尺寸+disabled。超时致命(同 waitForSelector 强前置)。9 处照 waitForSelector 模式接线(types/executeStep/两处 describeStep/菜单/insert),preload 与 index.html 零改动。`executeStep` 的 `never` 穷尽检查兜底。`npm run build` 双 tsconfig 通过 | ✅ 已完成 |
| 2026-07-02 | 录制/选择器 | Facebook 等站点录制的 click 选择器是一长串 `div.x1ey2m1c.x78zum5.xdt5ytf…` 垃圾类拼接(又长又脆)。根因:FB 用 **Stylex** 生成原子化 CSS 类(`x` 开头短哈希,每次发版变),而 `selector-generator.ts` 的 `isStableClass` 三条过滤规则(≥16 长哈希、`\d{4,}`、`css-/sc-/jsx-/emotion-` 前缀)都拦不住它们(长度仅 6~9),遂被当稳定类逐层拼进 `buildCssPath`。**通用**修复(非 FB 专用、对 Twitter/X 等原子化站点同样受益):`isStableClass` 追加两条——① `^x[a-z0-9]{4,}$` 且含数字(FB Stylex);② `^r-[a-z0-9]{5,}$` 且含数字(Twitter/X)。两条均强制含数字,纯词类名/带连字符语义类(`x-axis`/`col-md-6`)天然不匹配,不误伤。过滤后元素无稳定类则回退纯结构 CSS path(不含垃圾类,短得多),回放有 `buildFingerprint` 指纹兜底。`isStableClass` 加 `export`;新增 `scripts/verify-selector-class.mjs`(断言 14 原子类被过滤、9 语义类含 next/previous 保留)。`npm run build` + 该自检 + `verify-pagination`(3 页 30 行,指纹重定位仍生效)回归通过 | ✅ 已修复 |
| 2026-07-02 | 回放/等待 | 点击触发新浏览器窗口后,下一步「等待元素出现」的元素在新窗口里 → 回放卡死;日志停在「第 3 步…执行中」后**晚约 1 秒**才打「检测到新标签页弹窗,已切换…」。根因:主循环 `executeStep(activePage,...)` 传入**当下**的 `activePage`,点击后立即进入下一步、而新窗口 `context.on('page')` 事件尚未发火,等待步骤绑定旧页等一个永不出现的元素直至超时(切换机制没问题,只是时机晚)。用户在该步前插「人工介入暂停」可绕过。修复(仅 `macro-runner.ts`,复用「切换会发生、只是晚」这一事实):新增 helper `raceWaitAcrossPopup`——对「当前页」与「随后 `context.waitForEvent('page')` 弹出的新页」竞态执行同一等待函数,先满足者胜出,`finally` 抑制未采纳分支迟到 rejection;`handleWaitForSelector`/`handleWaitForClickable` 加 `context?` 参并包进 helper(可点击判定 predicate 一字未改),`executeStep` 加末参 `context?` 并透传、主循环与 `turnPage`(用捕获的 `runContext`)传入。**仅改等待类步骤**(点击等零改动、零额外延迟);致命语义不变;新窗口开出后既有 `on('page')` 照常更新 `activePage`,后续步骤自然在新页继续。旧宏零迁移、无类型/UI/preload 改动。新增 `scripts/verify-popup-wait.mjs`(target=_blank 两页、page2 延迟 1.5s 插入 `#target`,断言 `ok=true`,日志实测复现「执行中→检测到弹窗→2s 后完成」)+ `verify-pagination`/`verify-merge` 回归通过,`npm run build` 通过 | ✅ 已修复 |
| 2026-07-02 | 录制/选择器 | 07-02 上一条正则加固后,`fb.json` 仍有大量随机类(FB 无数字 Stylex 原子类 `xeuugli`/`xstzfhl` 因规则「强制含数字」漏网)与随机 id(React/Relay `#_r_3_`/`#_R_1cl...`,`isStableClass` 只管 class、id 从不校验)。用户否决「继续堆正则」,改**借助 AI**。新建专用 `selector-fix` openclaw agent(SOUL.md 单一可信源:禁 Stylex/Twitter 原子类与 React 随机 id、优先 data-*/语义 id/aria/文本、善用稳定祖先作用域、可 `xpath=`)+ 校正回路:`fixSelector`(`ai-extract.ts`,复用 OpenclawClient/extractJson/GUIDE)、IPC `ai-fix-selector`+`preload.aiFixSelector`、renderer `locateAndSnapshot`(实时 webview DOM 定位:试选择器→失效用指纹→打临时标记取 outerHTML+祖先链)/`verifyFixed`(恰好命中 1 且是被标记元素,xpath 走 evaluate)/`clearFixMarker`/`fixStepSelector`(≤3 轮带反馈复用 sessionKey),入口工具栏 `#ai-fix-all` 批量 + 步骤右键单步;非 click 步骤补 `fingerprint`(types+preload fill+renderer insert)使失效选择器可重定位(仅新录制)。**未改 `selector-generator.ts` 任何过滤规则**。`node scripts/test-selector-fix.mjs` 实跑:24s 把 `#_r_3_ > …button.x1lliihq` 校正成 `button[data-testid="open-registration-form"]`;`verify-merge`/`verify-pagination` 回归 + `npm run build` 通过 | ✅ 已完成 |
| 2026-07-02 | 录制/UI | 用户实测反馈:全局批量「AI 校正选择器」不实用(校正基于当前 webview 实时 DOM,而宏的步骤常录自不同页面,批量跑大多「跳过」);另需通用撤销。**①移除**工具栏 `#ai-fix-all` 全局批量按钮 + `fixAllSelectors`/`setFixBusy`/其监听(仅保留步骤右键单步校正,`fixStepSelector`/agent/IPC 全留)。**②新增录制步骤 Ctrl+Z 撤销 / Ctrl+Shift+Z·Ctrl+Y 重做**:不写逐操作逆操作,而在 `renderSteps()` 顶部 `captureHistoryOnRender()` 自动 diff `steps` JSON 快照记账(所有 mutation 都紧跟 renderSteps、非改动型渲染不入栈,天然通用覆盖录制/排序/删除/插入/改内容/重新点选/AI 校正/加载宏);`undoSteps`/`redoSteps` 反序列化重渲染、`applyingHistory` 防自记账、新改动清 redo;document keydown 焦点在可编辑控件时放行原生撤销;会话内栈上限 100。纯 `renderer.ts`+`index.html`,不碰 core/主进程/preload。`npm run build` + `verify-merge`/`verify-pagination` 回归通过 | ✅ 已完成 |
| 2026-07-03 | AI 校正/录制 | 单步 AI 校正仍有「页面不匹配」结构性缺陷:`locateAndSnapshot` 在**当前 webview 实时 DOM** 找元素,而宏步骤常录自不同页面(登录后/深层/瞬时),元素不在当前页就跳过、手动复现困难。**方案 A(录制时抓上下文,离线校正)**:`selector-generator.ts` 加 `buildElementContext(el)`→`{outerHTML,ancestors,contextHtml}`(contextHtml=邻域子树,采集时临时打 `data-macro-cap` 标记读完即除,供离线识别目标);`webview-preload.ts` 三处采集点随 step 回传上下文;存宏同名旁车 `<宏名>.captures.json`(`macro-types` 加 `StepCapture`/`MacroCaptures`,`macro-store` 加 `saveMacroCaptures`/`loadMacroCaptures`,`main.ts` save/load 接旁车,宏 JSON 不变);`renderer.ts` 用内存 `cid`+`Map<cid,上下文>`(抗 undo)关联,`buildMacro` 剥 cid、`buildCaptures`/`relinkCaptures` 存取,`fixStepSelector` 有上下文走 `fixWithCapture`(离线喂上下文+`verifyAgainstCapture` DOMParser 验证)、无则回退 `fixWithLiveDom`。仅新录制有上下文(旧宏回退实时);离线验证为邻域子树内唯一(非严格整页,靠 agent 锚点准则+回放兜底);旁车已 gitignore。`node scripts/verify-captures.mjs`(新增,旁车往返)+ `verify-merge`/`verify-pagination`/`verify-selector-class` 回归 + `npm run build` 通过 | ✅ 已完成 |
| 2026-07-03 | AI 校正/录制 | 方案 A 落地后实测某 FB click 步骤:AI 三轮给的选择器(`//div[@role='button'][.//span[text=X]]` 等)其实都对却被离线验证全否。根因①`verifyAgainstCapture`/`verifyFixed` 要求 `matches[0]===target`(严格同一元素),而录制目标是无意义内层 `span`,AI 正确锚定包含它的 `div[role=button]`(点外层按钮与点里层 span 对点击等效);②`resolveClickable` 只上溯 4 层,FB 的 `div[role=button]` 在更上面没被录成目标。修复:**A. 点击步骤放宽验证**——两验证器加 `acceptClickable` 参 + `matchHitsTarget`(`match===target` 或 可点击祖先 `match.contains(target)&&isActionable` 或 内后代 `target.contains(match)`;`isActionable`=a/button/role∈交互角色集),用可点击白名单挡掉大容器 div;fill/wait 仍严格同一。**B. `resolveClickable` 4→8**,新录制直接记住语义按钮。仅 `renderer.ts`+`webview-preload.ts`,`npm run build`+四项回归通过 | ✅ 已修复 |
