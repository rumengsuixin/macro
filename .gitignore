node_modules/
dist/

# 运行时产物
exports/
errors/
macros/
downloads/
timelines/
dumps/
browser-profile/
# 运行时配置:现全部收在 dataRoot/config/(首次运行自动生成;不含 git 追踪的 config-templates/)。
# 根目录旧位置的遗留文件(旧版本生成)一并忽略,防误提交(尤其 ai-config.json 可能含凭据)。
/config/
/ai-config.json
/browser-config.json
/request-rules.json
/bank-integrate.json
/merge-config.json
/replay-profile.json
/hooks.json

# 打包产物
release/
build/ms-playwright/

# 选择器上下文旁车(离线 AI 校正用,随宏生成,勿入库)
*.captures.json

# 日志与系统文件
*.log
.DS_Store
Thumbs.db
