# 项目本地文档记录

> 本文件记录本项目生成的本地文档与关键信息,便于上下文清空后快速恢复。

## 工作铁律

- **【铁律】修改本项目前必须先 commit**:在对本项目进行任何改动前,必须先对项目执行一次 commit(保存当前状态),再开始改动。

## 本地文档清单

- `README.md` — 安装、运行、使用步骤、宏 DSL 与提取规则说明
- `YouTube上传流程分析.html` — YouTube Studio 视频上传三阶段(①`/upload/studio` 会话初始化 start→②`upload.youtube.com/?upload_id` 字节上传+finalize→③`/youtubei/v1/upload/createvideo`)完整逆向流程(2026-07-16,基于 `timelines/timeline-replay-20260716-115136713.jsonl` 真实抓包,样本 B.mp4/28056038B):厘清 **`frontendUploadId`(前端在 ① 请求体生成的 `innertube_studio:<UUID>:0`)与 `scottyResourceId`(服务端在 ① 响应头 `x-goog-upload-header-scotty-resource-id` 返回,376 字符,早于字节上传即分配)在同一次 start 里配对绑定**、原封不动带入 createvideo(逐字相等已证);含 `X-Goog-Upload-*` 协议头速查、feedback 轮询(continuation 编码 frontendUploadId)、createvideo 的 attestation/sessionInfo/consistencyToken 令牌说明,及对「复用资源跳过重传」的结论(**body 可改·资源对可跨会话复用·必须成对同源否则 400 INVALID_ARGUMENT**;待验证:同一对能否多建/一份内容能否秒传拿多对/blob TTL;三种工作流 A反复发同视频·B成对轮换需改代码·C单个复用静态set即可)
- `开发计划.md` — 模块状态、待办、已知问题(每次任务后持续维护)
- `功能开发计划.md` — 欠缺功能路线图(独立文档,2026-07-06):基于六维度调研(交互录制/数据加工/自动化编排/反爬对抗/可靠性监控/宏资产管理)梳理缺口,按 P0/P1/P2 排优先级;P0 首推参数化(数据驱动)
- `xlsxIntgration融合设计.html` — 与外部项目 `D:\git_object\xlsxIntgration`(实为 Python 3.8 的银行流水整合/支付对账工具集,非 Node)的融合评估与设计(2026-07-13):结论=代码级合并不可行、集成式可行;方案=把 xlsxIntgration 各业务线做成 macro 的**后处理插件**(新增 `bank-integrate.ts` + `python-bridge.ts` 子进程 spawn Python,复用 `registerPostProcessor`「文件进→xlsx 出」契约),xlsxIntgration 仅需 `config.py` 加 `BANK_INPUT_DIR`/`BANK_OUTPUT_DIR` 环境变量覆盖(向后兼容)。分阶段:PoC 代号1→对账线→用 macro 录制替换代号4/BC/EPIN 手写 CDP 抓取。**当前仅设计文档,未实施代码**;配套计划文件 `C:\Users\Administrator\.claude\plans\d-git-object-xlsxintgration-tingly-badger.md`
- `examples/demo-macro.json` — 演示宏(books.toscrape.com,采集 标题 / 价格 / 链接)
- `examples/demo-list-action.json` — 演示宏(list-action 模式:列表逐项点击下载按钮,占位选择器需按实际页面替换)
- `ai-config.json` — AI 提取配置(项目根,首次运行自动生成):openclaw agent 目标 profile 列表 + 系统/提示词模板
- `browser-config.json` — 浏览器登录态复用配置(项目根,首次设置时生成,已 gitignore):`persistProfile`/`userDataDir`/`injectRecordingSession`/`injectRecordingLocalStorage`/`useSystemChrome`(反检测·默认开)
- `browser-profile/` — 持久化回放 profile 默认目录(已 gitignore)
- `request-rules.json` — 请求改写 + 重发 + 响应头改写 + 请求头改写 + 真拦截 + 请求体落盘 + 请求体替换 + 只记录支路(项目根/打包 userData,首次运行自动生成 `enabled:false` inert 模板,已 gitignore):`{enabled,rules[],resends?[…,replaceWithFile?],responseRules?[],requestHeaderRules?[{urlPattern,when?,setHeaders?,removeHeaders?}],blocks?[{urlPattern,method?}],dumps?[{urlPattern,method?,extension?}],bodyReplaces?[{urlPattern,method?,replaceWithFile}],record?:{enabled,urlPattern?,includeBody?}}`;命中的 POST 请求按规则改写 body 顶层字段(执行顺序 set→append→remove);`requestHeaderRules` 命中且 `when` 满足即改**原始请求的请求头**(setHeaders 覆盖/removeHeaders 删,大小写不敏感;回放端 route.continue({headers})/route.fetch({headers});content-length 自动剥离、cookie/host 改不了;区别于 resends[].setHeaders 改重发副本头);`blocks` 命中即**硬阻断不发出**(route.abort);`dumps` 命中即把**完整二进制请求体**(CDP postDataEntries)写成一个文件到 `dumps/`(缺省 mp4);`bodyReplaces` 命中即用 `replaceWithFile`(本地绝对路径)的**完整字节整体替换请求体**(CDP `Fetch.continueRequest({postData})`,能替换 File/Blob 上传体、Content-Length 网络栈重算)。**某条 `resends` 项也可配 `replaceWithFile`(本地绝对路径)**——设了则重发请求整体用该文件完整字节作 body(忽略 set/append/remove、content-type 保留触发请求原值),经 base64→页面内 `Uint8Array` 重建、能发任意二进制(如换视频),仅回放端。dumps/bodyReplaces 均仅回放端、受 `enabled` 总开关管。**〔2026-07-16 起〕请求拦截(改写/重发/响应头/真拦截/请求体落盘/请求体替换)现仅在回放阶段生效——录制端已不再挂 CDP 拦截器**(`record` 只记录支路两端仍可用);回放端每次运行开始重新加载最新 + 运行中 `fs.watchFile` 热更新。**数组值直接写真 JSON 数组 `["x"]`(勿写成字符串 `"['x']"`);`append` 往数组字段追加并去重**。**`record` 段是独立于 `enabled` 的「只记录不修改」支路**(见下「录制/回放两端只记录不修改支路」),开它即拦截所有请求写时间线、不改写。示例见 `examples/request-rules.example.json`
- `dumps/dump-<毫秒戳>-<序号>.<后缀>` — 「请求体落盘」支路的**完整二进制请求体**文件(项目根/打包 userData 的 `dumps/`,已 gitignore):命中 `dumps` 规则的请求,其请求体从首字节到末字节经 **CDP Fetch `postDataEntries`(base64 重组,能抓 File/Blob 上传体)** `fs.writeFileSync` 落盘(缺省 `.mp4`),用于抓取上传型接口的字节体(如把 YouTube 视频上传请求体存成 mp4)。由 `request-rules.json` 的 `dumps` 段开启,仅回放端生效
- `examples/request-rules.example.json` — 请求改写规则示例(含字段说明,拷到 `request-rules.json` 改用)
- `拦截器规则配置手册.html` — `request-rules.json` 全字段速查手册(2026-07-20,面向配置查阅,非工程说明):把拦截器 8 个顶层键(`enabled`/`rules`/`resends`/`responseRules`/`blocks`/`dumps`/`bodyReplaces`/`record`)的**每个可配置字段**整理成表格(字段/类型/必填or可选/作用/限制),含通用概念(CDP glob 匹配、bodyType 嗅探优先级、set→append→remove 语义、物理分开原因、防递归标记头、Windows 路径转义、归一化静默丢弃非法项)、「我想做 X→用哪个支路」速查表、各支路代码示例、最小骨架 + 排查清单。数据源=`src/core/macro-types.ts`(类型)+`src/storage/request-rules-store.ts`(归一化校验)+`examples/request-rules.example.json`,标注了「仅回放端/两端」「受 enabled 管/独立开关」等生效范围徽章
- `scripts/test-ai.mjs` — AI 自检脚本(验证对接 openclaw agent 整链能否跑通)
- `scripts/verify-pagination.mjs` — 翻页健壮性自检脚本(脆弱选择器 + 语义指纹 anchor 重定位,quotes.toscrape.com 实跑采满 3 页 30 行;需网络)
- `scripts/verify-merge.mjs` — 合并后处理器离线自检(本地造 xlsx→打 zip→跑 merge-zip-excel,断言行数/列并集;不需网络,需先 `npm run build`)
- `bank-integrate.json` — 银行整合桥接配置(项目根/打包 userData,首次运行自动生成模板,已 gitignore):`{timeoutMs?,modes:{type→{executable}}}`;模板默认 seed **全 5 个代号**、`executable` 按 `process.platform` 指向 xlsxIntgration 打包**可执行文件**(win `dist\银行流水整合\<代号中文名>.exe`/mac `dist/bank-integration/<代号英文名>`),覆盖 `bank-integrate-domestic/overseas/order-match/payout/collection-payout`;插件据此 spawn 对应 exe(无脚本参)。产物回收改为取输出目录最新 xlsx(`findProducedXlsx`),故 `summaryFile` 死字段已删
- `scripts/verify-bank-integrate.mjs` — 银行整合端到端自检(**5 代号各真跑 exe**:代号1/2/3/5/6 各拷 xlsxIntgration `data/input/N` 现成样本→桥接 spawn 对应 exe→断言 exports 产出 xlsx 可读、含多工作表;某代号 exe/样本缺失则跳过非失败;需先 `npm run build` + 该 exe 已打包)
- `scripts/verify-selector-class.mjs` — 选择器稳定类判定离线自检(断言 `isStableClass` 过滤 FB Stylex/Twitter 原子类、保留语义类含 next/previous;需先 `npm run build`,不需网络)
- `scripts/verify-popup-wait.mjs` — 新窗口等待竞态离线自检(本地造两 html:page1 有 target=_blank 链接、page2 延迟 1.5s 才插入 `#target`;宏 goto→click 开新窗→waitForSelector `#target`,断言 `ok=true` 即等待步骤跟随了新窗口;需先 `npm run build`,`MACRO_HEADLESS=1` 运行,不需网络)
- `scripts/test-selector-fix.mjs` — 选择器校正自检脚本(造「脆弱选择器 + 稳定锚点」上下文调 `fixSelector`,断言返回选择器非空且不含随机 id/原子类/nth-of-type;需先 `npm run build` 且 Gateway 运行 + 已建 selector-fix agent)
- `scripts/verify-captures.mjs` — 旁车上下文读写离线自检(`saveMacroCaptures`/`loadMacroCaptures` 往返一致、空则删旧旁车不留壳;需先 `npm run build`,不需网络)
- `scripts/verify-stepurls.mjs` — 回放回填来源 URL 自检(quotes 首页点作者链接进详情页,断言 `RunResult.stepUrls` 里 click 步来源=首页、其后 waitForSelector 步来源=详情页;验证「按 URL 分组」对旧宏的回放回填救援;需网络 + `MACRO_HEADLESS=1`,需先 `npm run build`)
- `scripts/verify-cancel.mjs` — 运行中停止离线自检(造「永不出现的元素 + 30s 超时 waitForSelector」宏,run() 期间 800ms 后调 `runner.cancel()`,断言远早于超时返回且 `cancelled=true`;需先 `npm run build`,`MACRO_HEADLESS=1` 运行,不需网络)
- `scripts/verify-request-rewrite.mjs` — 请求改写纯逻辑离线自检(断言 `globToRegExp`/`decideBodyType`/`rewritePostBody`:CDP glob 匹配、类型判定优先级、JSON/form 的 set/**append 追加去重**/remove、set 数组值原样保留;需先 `npm run build`,不需网络、不启 Electron)
- `scripts/verify-request-intercept.mjs`(+ `scripts/_intercept-e2e.cjs`)— 请求改写**真跑 Electron 端到端**自检(起本地 echo 服务同源提供测试页避 CORS + webview 发 POST,挂真实 `RequestInterceptor`,断言服务端收到改写后 body;真跑通 CDP `Fetch.enable`/`getRequestPostData`/`continueRequest(base64)` 链路;需先 `npm run build`,不需网络,需能以应用方式启动 Electron)
- `scripts/verify-replay-intercept.mjs` — **回放端**请求改写**真跑 Playwright 端到端**自检(起本地 echo 服务同源提供测试页 + 页面自动 `fetch('POST /echo')`,挂真实 `MacroRunner`+`sessionOptions.requestRules`,跑 `[goto,waitForSelector #done]`,断言服务端实收改写后 body `{page:1,size:100,injected:"yes"}` 且无 debug;真跑通 `context.route`→`request.postData()`→`route.continue({postData})`(普通字符串非 base64)链路;需先 `npm run build`,不需网络;本机缺 headless_shell 时用 `PLAYWRIGHT_BROWSERS_PATH=<repo>/build/ms-playwright MACRO_HEADLESS=1` 走捆绑 Chromium)
- `scripts/verify-timeline-recorder.mjs` — 请求时间线记录器**纯逻辑离线**自检(断言 `TimelineRecorder`:请求+响应两行同 id 可 join、字段齐备、`matches` 按 urlPattern 命中/`setPattern` 热更新、**~1MB reqBody 读回逐字相等护禁止截断**、N 次写=N 行 append 语义、懒建目录、落盘失败熔断不抛;需先 `npm run build`,不需网络、不启 Electron)
- `scripts/verify-timeline-replay.mjs` — **回放端**只记录支路**真跑 Playwright 端到端**自检(本地 echo 服务 + 页面自动 `POST /echo`,session 故意 `enabled:false,rules:[]` 仅开 `record.enabled` 证记录独立于改写门槛,`MacroRunner` 第 6 参传临时 timelinesDir,跑 `[goto,waitForSelector #done]`,断言 `timeline-replay-*.jsonl` 含 `/echo` 的 request 行(POST、reqBody 未截断)+ 同 id response 行(status=200/timingMs 为数/mimeType 有值);真跑通 `context.on('request'/'response')`→JSONL 落盘;需先 `npm run build`,`MACRO_HEADLESS=1`,缺 headless_shell 时加 `PLAYWRIGHT_BROWSERS_PATH=<repo>/build/ms-playwright`)
- `scripts/verify-timeline-record.mjs`(+ `scripts/_timeline-record-e2e.cjs`)— **录制端**只记录支路**真跑 Electron 端到端**自检(本地 echo 服务同源测试页 + webview 发 `POST /echo`,临时规则 `enabled:false,rules:[]` 仅 `record.enabled`,挂真实 `RequestInterceptor(rulesPath, timelinesDir)`,断言 `timeline-record-*.jsonl` 含 request 行(POST、body 未截断)+ 同 id response 行(status=200/timingMs 为数/mimeType);真跑通 CDP `Network.enable`/`requestWillBeSent`/`getRequestPostData`/`loadingFinished` 链路;需先 `npm run build`,不需网络,需能以应用方式启动 Electron)
- `timelines/timeline-<record|replay>-<毫秒戳>.jsonl` — 「只记录不修改」支路的**请求时间线**(项目根/打包 userData 的 `timelines/`,已 gitignore):每行一条 JSON,`{t,phase,kind:'request'|'response',id,method,url,reqHeaders?,reqBody?,status?,timingMs?,respHeaders?,mimeType?,error?}`,请求+响应两行同 `id` 关联。由 `request-rules.json` 的 `record` 段开启
- `scripts/verify-hotreload.mjs` — **回放端运行中热更新**真跑 Playwright E2E(本地 echo 服务 + 页面链式 POST + `pause`/注入 `onPause` 保活;初始改写+记录全关,收到第 3 个 POST 后 `runner.updateRequestRules` 切全开,断言前段未改写/未记录、后段已改写/已记录;需先 `npm run build`,`MACRO_HEADLESS=1`,缺 headless_shell 前置 `PLAYWRIGHT_BROWSERS_PATH=<repo>/build/ms-playwright`)
- `scripts/verify-resend.mjs` — **回放端「重发型」拦截**真跑 Playwright E2E(本地 echo 服务 + 页面只发 1 个 `POST /trigger`;session `enabled:true,rules:[],resends:[{urlPattern,delayMs:1500,set:{resent_flag}}]`,宏 `[goto,pause]`+`onPause` 轮询「原始+重发各 1」保活;断言恰 1 原始(无标记头/无 resent_flag)+ ≥1 重发(带 `x-macro-resend` 头 && resent_flag=yes && page 仍=1 证基于原 body 改参)+ 延时 ≈1500ms + 重发数==repeat 不增长(证防递归);需先 `npm run build`,`MACRO_HEADLESS=1`,缺 headless_shell 前置 `PLAYWRIGHT_BROWSERS_PATH=<repo>/build/ms-playwright` 或 `npx playwright install chromium-headless-shell`)
- `scripts/verify-dump-body.mjs` — **回放端「请求体落盘」CDP Fetch** 真跑 Playwright E2E(本地 echo 服务;**两用例**:页面发 `new Blob([...])` body(模拟 File/文件上传,如视频)+ `Uint8Array` body,二进制体=全部 256 种字节值各 3 次共 768 字节含大量无效 UTF-8;session `enabled:true,rules:[],dumps:[{urlPattern:'*/upload*',extension:'mp4'}]`,`MacroRunner` 第 7 参传临时 dumpsDir,跑 `[goto,waitForSelector #done]`;断言两用例各落 1 个 `dump-*.mp4` 且与原始 Buffer **逐字节相等**、反证 string(UTF-8)路径会损坏——**Blob 用例锁住升级价值**:旧 postDataBuffer 实现对 Blob 返回 null 必失败,新 CDP `postDataEntries` 实现通过;需先 `npm run build`,`MACRO_HEADLESS=1`,缺 headless_shell 前置 `PLAYWRIGHT_BROWSERS_PATH=<repo>/build/ms-playwright` 或 `npx playwright install chromium-headless-shell`)
- `scripts/verify-body-replace.mjs` — **回放端「请求体整体替换(拦截替换)」CDP Fetch** 真跑 Playwright E2E(本地服务;页面发 **Blob** body 内容 A(256 字节)、临时造替换文件 B(600 字节、内容/大小都不同);session `enabled:true,rules:[],bodyReplaces:[{urlPattern:'*/upload*',replaceWithFile:B}]`,跑 `[goto,waitForSelector #done]`;断言**服务端实收 == B 逐字节相等**(整体替换生效、对 Blob 有效)、与 A 不等、`content-length` 头 = B 字节数(大小变化时长度正确重算);证 CDP `Fetch.continueRequest({postData})` 替换 Blob 上传体;需先 `npm run build`,`MACRO_HEADLESS=1`,缺 headless_shell 前置 `PLAYWRIGHT_BROWSERS_PATH=<repo>/build/ms-playwright` 或 `npx playwright install chromium-headless-shell`)
- `scripts/verify-resend-headers.mjs` — **回放端「重发型」改重发请求头(setHeaders/removeHeaders)** 真跑 Playwright E2E(本地 echo 服务 + 页面发 1 个 `POST /trigger` 带两个自定义头 `x-inherited`(将被删)/`x-keep`(应继承);session `enabled:true,rules:[],resends:[{urlPattern:'*/trigger*',delayMs:1200,setHeaders:{'x-custom':'injected'},removeHeaders:['x-inherited']}]`;服务端整份保存 `req.headers` 按 `x-macro-resend` 头分原始/重发;断言 setHeaders 生效(重发头含 `x-custom=injected`)+ 继承生效(重发头含 `x-keep`,证 removeHeaders 断言非空跑)+ removeHeaders 生效(重发头**不含** `x-inherited`,本会被继承却被删)+ 标记头 `x-macro-resend=1` 受保护 + 原始请求不受影响(仍含 `x-inherited`、不含 `x-custom`)+ 延时/防递归;需先 `npm run build`,`MACRO_HEADLESS=1`,缺 headless_shell 前置 `PLAYWRIGHT_BROWSERS_PATH=<repo>/build/ms-playwright` 或走系统缓存 `%LOCALAPPDATA%\ms-playwright`)
- `scripts/verify-resend-replace.mjs` — **回放端「重发 + 本地文件整体替换 body」** 真跑 Playwright E2E(本地 echo 服务 + 页面只发 1 个 `POST /trigger`(JSON `{page:1}`);临时造替换文件 B(600 字节全字节值,含无效 UTF-8);session `enabled:true,rules:[],resends:[{urlPattern:'*/trigger*',delayMs:800,replaceWithFile:B}]`(无 set/append/remove),宏 `[goto,pause]`+`onPause` 轮询「原始+重发各 1」保活;服务端存**原始 Buffer**(不 JSON.parse)按 `x-macro-resend` 头分类;断言恰 1 原始且其字节 != B(原触发未被动)+ ≥1 重发 && **重发体 == B 逐字节相等**(证 resend 用文件字节整体作 body、经 base64→页面内 `Uint8Array` 重建、能发任意二进制)+ 延时 ≥500ms;需先 `npm run build`,`MACRO_HEADLESS=1`,缺 headless_shell 前置 `PLAYWRIGHT_BROWSERS_PATH=<repo>/build/ms-playwright` 或走系统缓存 `%LOCALAPPDATA%\ms-playwright`)
- `scripts/verify-response-trigger.mjs` — **「重发响应条件触发」纯逻辑离线自检**(断言 `responseTriggerMet`/`getJsonByPath`/`triggerNeedsBody`/`headersAllEqual`:status/headers/bodyJson 三组 **AND**、JSON 嵌套点路径取值、`JSON.parse` 失败/路径缺失/读不到体 → 不命中、头名大小写不敏感、`String()` 化值比较、空触发器恒真;并回归重构后的 `responseConditionMet` 行为不变;需先 `npm run build`,不需网络、不启 Electron)
- `scripts/verify-response-trigger-replay.mjs` — **回放端「响应条件触发重发(捕获-重放模型)」真跑 Playwright E2E**(本地服务;页面加载后**先** `POST /payload`(body `{seq:"B"}`,被 `urlPattern:'*/payload*'` 捕获)、**再** `GET /status?case=pending` 与 `GET /status?case=done`;`/status` 响应复刻 **YouTube 上传反馈那种深层嵌套+异构数组结构**(`continuationContents[0].uploadFeedbackItemContinuation.contents[0].transferProgressBar.fractionCompleted`,done=1/「已上传 100%。」、pending=0.5),响应头 `x-ready:1`;session `resends:[{urlPattern:'*/payload*',responseTrigger:{triggerUrl:'*/status*',status:200,headers:{'x-ready':'1'},bodyContains:['uploadFeedbackItemContinuation','"fractionCompleted":1']},delayMs:800,repeat:1}]`(**用 `bodyContains` 原文子串免路径匹配**,证点路径写不出的嵌套数组结构也能触发),宏 `[goto,pause]`+`onPause` 轮询「1 原始+1 重发」保活;断言 `/payload` 恰 1 原始 + 恰 1 重发(body `seq=B` 证**重发的是捕获的 urlPattern 请求**)+ pending(`fractionCompleted:0.5`)不触发(bodyContains 门槛)+ 延时≈800ms + 重发数==1 不增长(防递归);需先 `npm run build`,`MACRO_HEADLESS=1`,缺 headless_shell 前置 `PLAYWRIGHT_BROWSERS_PATH=<repo>/build/ms-playwright` 或走系统缓存 `%LOCALAPPDATA%\ms-playwright`)
- `<宏名>.captures.json` — 宏的**选择器上下文旁车**(与宏同目录,离线 AI 校正用:录制时抓的 `{outerHTML,ancestors,contextHtml}` 同序对齐宏 steps;宏本体不含;已 gitignore)
- `scripts/make-icon.mjs` — 生成应用图标(纯 Node zlib 编码 PNG + 手工封装多尺寸 ICO + PNG-based ICNS,无图像依赖);产物 `assets/icon.png`/`assets/icon.ico`(Windows)/`assets/icon.icns`(macOS,32/64/128/256/512/1024;🐟 二爪鱼,均已入库)。改图标改其中几何/颜色常量后 `node scripts/make-icon.mjs` 重跑
- `打包指南.md` — Windows 安装包打包说明(自带 Chromium,`npm run dist` 一键打包,产物在 `release/`);安装态用户数据在 `%APPDATA%\macro-recorder\`,`ai-config.json` 首次启动自动生成。**第十节新增 macOS 版打包**(走 GitHub Actions,`.dmg`/`.zip` 未签名,含独立工具 Mac 对接步骤)
- `.github/workflows/build-mac.yml` — 在 macOS runner 上打 macro 的 Mac 版(`npm run dist:mac` → `.dmg`/`.zip`,自带 Chromium,未签名 `CSC_IDENTITY_AUTO_DISCOVERY=false`);触发:**push 到 `master` 分支自动触发**(2026-07-14 加,参考 xlsxIntgration,免反复手动打 tag)/ 手动 workflow_dispatch(选 arm64/x64)/ 推 `v*` tag,默认 arm64/macos-14。本机 Windows 打不了 Mac 包,统一走此 CI。**〔2026-07-14 Mac arm64 已实测端到端打通〕** 首次真跑连撞 3 坑后成功产出 arm64 `.dmg`/`.zip`:①`copy-playwright-browser.mjs` 的 `findChromeBinary` 硬编码 `chrome-mac`,arm64 实为 `chrome-mac-arm64`→动态匹配 `chrome-mac*` 前缀;②该目录内 app 名不是 `Chromium.app` 而是 **`Google Chrome for Testing.app`**→不硬编码,改在 `chrome-mac*` 下找任一 `*.app` 按 bundle 约定取同名可执行(加递归目录树失败诊断 `listTree`);③electron-builder 在 CI 隐式触发 publish 但无 `GH_TOKEN` 失败→`dist`/`dist:mac` 加 `--publish never`。安装包瘦身在 Mac 同样生效(arm64 删无头 shell 释放 192MB)。仅改 `copy-playwright-browser.mjs`/`package.json`/`build-mac.yml`,业务代码零改动;Windows 本机每轮 `pack:browser` 验证零回归。commit 2f827c0/644e0e1/3e941d1
- **〔2026-07-14 安装包瘦身〕** 打包做四项零风险瘦身(**Mac + Windows 两平台生效,业务代码零改动**):①`pack:browser`(`scripts/copy-playwright-browser.mjs` 新增 `pruneHeadlessShell()`)删无头 shell `chromium_headless_shell-*`——打包应用默认有头(`macro-runner.ts` 的 `MACRO_HEADLESS` 未设即 headless=false)只用完整版 `chromium-*`,无头 shell 纯占体积;设 `PACK_KEEP_HEADLESS_SHELL=1` 可保留(兜底「`PLAYWRIGHT_BROWSERS_PATH` 指向 build/ms-playwright 跑无头自检」的 fallback);开发自检走系统默认缓存 `%LOCALAPPDATA%\ms-playwright` 不受影响。②`package.json` → `build.electronLanguages:["zh-CN","en-US"]` 裁 electron 语言包(locales 55→2)。③④`build.files:["**/*","!**/*.map","!**/*.d.ts"]` 排除 asar 内依赖的 source map(exceljs 14MB 等)与类型定义(只作用于 asar,不影响 extraResources 的 Chromium)。**去掉了 compression:maximum**(electron-builder 官方证实对已压缩二进制「几乎不减体积、只拖慢构建」)。**仅改 `copy-playwright-browser.mjs`+`package.json`**,core/preload/renderer/主进程/宏 JSON/旁车/`build-mac.yml` 零改动。已验证:`npm run build` + `verify-merge`/`verify-captures` 回归 + `npm run dist`(Windows)实测**安装包 318MB**、三项均生效(`win-unpacked/locales/` 只剩 `zh-CN.pak`+`en-US.pak`、`resources/ms-playwright/` 无 `chromium_headless_shell`、app.asar 内 `.map`/`.d.ts` 为 0);推算优化前约 420MB,省约 100MB。详见 `打包指南.md` 第十一节
- 规划阶段计划文档:`C:\Users\Administrator\.claude\plans\glimmering-brewing-hoare.md`、`...\happy-wishing-hamming.md`(AI 提取)

## AI 提取要点(对接 openclaw agent)

- 方式:macro 作为 WebSocket 客户端连本机 OpenClaw Gateway(`ws://127.0.0.1:18799`),Ed25519 签名认证,`chat.send`(`deliver:false`)请求 agent 生成、收回草稿。**不再** spawn claude/codex。
- 客户端 `src/core/openclaw-client.ts`(参考 `D:\git_object\aiAgentServicer` 的 `test_ws_send.js`/`oclaw-client.js`);依赖 `ws` + Node 内置 `crypto`。
- 专用 agent:`webextract`(用 `openclaw agents add` 创建,workspace 在 `~/.openclaw/workspace-webextract`,SOUL.md 定位为「只输出 ExtractConfig JSON」)。另有 `selector-fix`(见下「AI 校正选择器」)。
- 配置在 `ai-config.json`:profile 指定 `agentId` + `sessionKeyPrefix`(实际 sessionKey = 前缀 + uuid),可配多个 agent 目标。
- 依赖:`~/.openclaw/openclaw.json`(端口/token)、`~/.openclaw/identity/device.json`(Ed25519 身份);Gateway 须运行。
- 已验证:自检对接 webextract,24s 产出合法 list 规则。
- agent 管理:`openclaw agents list` 查看,`openclaw agents delete webextract` 回滚。
- 选择器质量加固(通用,非专项):①`ai-extract.ts` 的 `SELECTOR_QUALITY_GUIDE` 常量现为**极简指针**(单行核心红线:优先稳定锚点 data-*/id/aria/文本、避免框架动态类名/结构伪类/隐藏克隆 DOM、actionSelector 须项内可点,末尾指向 agent 侧 SOUL.md〈选择器质量准则〉完整规范)在 `generateExtract` 组装 message 时**无条件注入**(systemPrompt 后、modeHint 前),不依赖既有 ai-config.json,老装机即时生效。**完整 5 条规范以 agent 侧 SOUL.md 为单一可信源**(避免两份全文重复漂移);客户端这行只作 agent 侧准则缺失/被改坏时的兜底红线。②**生成后自检回路**:`GenerateInput` 加 `feedback`/`sessionKey`,`GenerateResult` 回传 sessionKey(`chat.send` 仅凭 sessionKey 路由 agent,故重试须复用首轮 key 才能保留上下文);renderer 的 `verifySelectors` 在录制 webview 内用一段 `executeJavaScript` 实测各选择器命中数(listSelector 整页测,字段/actionSelector/detailLinkSelector 以**首个列表项**为根测,镜像 extractor 的 `item.locator(sel).first()`;非法选择器记 invalid),`evaluateChecks` 判定(listSelector 必>0、非法即失败、项内至少一个命中),未通过则带中文反馈复用同会话重生成,**最多 2 次修复**,仍失败不致命(填最后一版+提示人工核对)。DOM 与 AI 所见一致,无需回放浏览器、无新 IPC。
- AI 校正选择器(2026-07-02,不再堆正则、改走 AI:校正录制步骤里用了随机类名/id 的脆弱选择器):根因是 `selector-generator.ts` 的正则过滤(`isStableClass` 等)永远堆不完——FB Stylex 无数字原子类(`xeuugli`/`xstzfhl`)、React/Relay 随机 id(`#_r_3_`/`#_R_1cl...`)照样漏进已录宏(见 `macros/fb.json`)。**方案 = 新建专用 agent + 校正回路**,**未改 `selector-generator.ts` 任何规则**。①**新 agent `selector-fix`**(`openclaw agents add`,workspace `~/.openclaw/workspace-selector-fix`,SOUL.md 定位「只输出 `{"selector":"..."}` 的选择器修复器」,**完整〈选择器质量准则〉以该 SOUL.md 为单一可信源**:优先 data-*/语义 id/aria-label/文本/语义 class,禁用 FB Stylex `x…`/Twitter `r-…`/React `_r_`·`_R_`·`:r:`·`mount_` 随机 id,慎用 nth-of-type,善用稳定祖先做作用域,文本锚定可输出 `xpath=`)。`ai-config.json` 与 `DEFAULT_CONFIG` 各加 `selector-fix` profile(`sessionKeyPrefix:agent:selector-fix:macro:selector`,timeout 90s)。②**客户端 `fixSelector`**(加在 `ai-extract.ts`,复用同文件 `OpenclawClient`/`loadAiConfig`/`extractJson`/`SELECTOR_QUALITY_GUIDE`;入参 `{current,reason,elementHtml,ancestors,feedback?,sessionKey?}`,组装 message→`requestDraft`→取 `{selector}`;`resolveFixProfile` 默认回退 selector-fix)。webextract 的 `generateExtract` **零改动**。③**IPC `ai-fix-selector`**(`main.ts`)+ `preload.aiFixSelector`。④**renderer 校正回路**(`renderer.ts`,镜像 AI 提取的「生成→实测→反馈重生成」自愈回路,**基于实时录制 webview DOM**):`locateAndSnapshot(selector,fingerprint)` 用 `executeJavaScript` 先试当前选择器(唯一命中)、失效则用步骤已存指纹(aria→文本→href,要求唯一可见命中)定位,**先读 outerHTML(截断 2000)再给目标打临时标记 `data-macro-fix="1"`**,回传 outerHTML+祖先链摘要(≤6 层);`verifyFixed(selector)` 实测 AI 结果**恰好命中 1 个且是被标记元素**(`xpath=` 走 `document.evaluate`);`clearFixMarker` 于 `finally` 清标记;`fixStepSelector(index)` 跑最多 3 轮(不通过带「命中 K 个/非目标」中文反馈复用 sessionKey),通过则更新 `step.selector`(click 步骤保留原 fingerprint,元素身份未变),找不到元素则跳过并提示「先把对应页面加载到浏览器」。入口:步骤右键「AI 校正此步骤选择器」单步(紧邻「重新点选此步骤的选择器」)。**〔2026-07-02 后续〕原工具栏全局批量按钮 `#ai-fix-all` 已移除**——批量校正基于当前 webview 实时 DOM,而一个宏的步骤常录自不同页面,批量跑大多「跳过」实用性低;仅保留单步入口(用户先把浏览器导航到该步对应页面再校)。删除的是 `#ai-fix-all` 按钮 + `fixAllSelectors`/`setFixBusy`/其点击监听;`fixStepSelector`/`locateAndSnapshot`/`verifyFixed`/`clearFixMarker` 与 IPC/agent 全保留。⑤**给非 click 步骤补语义指纹**(旧选择器失效时才能重定位):`macro-types.ts` 的 `FillStep`/`WaitForSelectorStep`/`WaitForClickableStep` 各加可选 `fingerprint`;`webview-preload.ts` fill 提交处 `buildFingerprint(pendingFill.el)`;`renderer.ts` 的 `insertWaitForSelector`/`insertWaitForClickable` 保存 picker-result 已回传但原被丢弃的 fingerprint。**仅新录制生效**——`fb.json` 里旧的 fill/wait 步骤无指纹,其失效选择器若在当前页也不命中则只能跳过或手动重选;click 步骤有指纹可正常校正。**限制**:校正依赖当前 webview 已加载对应页面/状态(同「重新点选」UX);不把 DOM 快照写进宏(保持文件干净);改动经「保存宏」落盘。**已验证**:`node scripts/test-selector-fix.mjs` 对接 selector-fix,24s 把 `#_r_3_ > …button.x1lliihq` 校正成 `button[data-testid="open-registration-form"]`;`verify-merge`/`verify-pagination` 回归通过。agent 回滚 `openclaw agents delete selector-fix`。**〔2026-07-03 方案 A:校正改为离线、与当前页无关〕** 单步校正原用 `locateAndSnapshot` 在**当前** webview 实时 DOM 找元素——元素不在此刻页面上就跳过;而宏的步骤常录自不同页面(登录后/深层/瞬时),手动复现困难。根治=**在元素一定存在的时刻(录制时)把 DOM 上下文抓下来**,校正离线读它、不依赖当前页。①`selector-generator.ts` 新增 `buildElementContext(el)`→`{outerHTML(截2500), ancestors(祖先链≤6层), contextHtml}`:contextHtml=从 el 向上 ≤6 层里 outerHTML ≤`CTX_CAP=40000` 的**最高**祖先子树,采集时**临时给 el 打 `data-macro-cap` 标记**(读完立即移除,同步无副作用)以便离线识别目标;AI 只吃 outerHTML+ancestors,contextHtml 仅验证用。②`webview-preload.ts` 三处采集点(click/fill 提交/picker `onPickerClick`)`buildElementContext(el)` 随 step(`sendStep` 第 2 参 / `picker-result.context`)回传,**不塞进 step**。③上下文存**宏同名旁车 `<宏名>.captures.json`**(与宏 steps 同序对齐、每条带 type/selector 作加载校验;`macro-types.ts` 加 `StepCapture`/`CaptureEntry`/`MacroCaptures`;`macro-store.ts` 加 `saveMacroCaptures`/`loadMacroCaptures`/`captureSidecarPath`,空则删旧旁车不留壳;`main.ts` 的 `save-macro` 加 `captures` 第 2 参写旁车、`load-macro` 读旁车返回 `{macro,captures}`;`preload` 签名同步)。**宏 JSON 本体不变**。④`renderer.ts`:带 selector 的 step 创建时分配**内存字段 `cid`**、上下文入 `Map<cid,StepCapture>`(用 cid+Map 而非 WeakMap:undo/redo 走 JSON 重建对象,cid 随之保留、Map 独立于快照,故撤销后上下文仍在);`buildMacro` 用 `stripInternal` 剥 cid 保宏干净;`buildCaptures` 生成对齐旁车;加载 `relinkCaptures` 按同序+type/selector 校验回挂。校正 `fixStepSelector` 分流:**有上下文→`fixWithCapture`(离线,喂 cap.outerHTML/ancestors 给 `aiFixSelector`,`verifyAgainstCapture` 用 `DOMParser` 解析 contextHtml、断言 AI 选择器恰好命中被标记目标,≤3 轮带反馈复用 sessionKey,与当前页无关)**;**无上下文→`fixWithLiveDom`(原实时 `locateAndSnapshot`/`verifyFixed`/`clearFixMarker` 回退,旧宏不回归)**。**限制**:仅**新录制**(及本会话新建步骤)有上下文,旧宏(fb.json)走实时回退需先导航到对应页;离线验证是「邻域子树内唯一且命中目标」,非严格整页唯一(不存整页 HTML 控体积),页级唯一性靠 agent「优先文档级唯一锚点」准则 + 回放端兜底;旁车已 gitignore(`*.captures.json`)。**已验证**:`node scripts/verify-captures.mjs`(旁车读写往返一致、空不留壳)+ `verify-merge`/`verify-pagination`/`verify-selector-class` 回归 + `npm run build` 通过。**〔2026-07-03 修「验证器太严」〕** 实测某 FB click 步骤:AI 三轮给的选择器(`//div[@role='button'][.//span[text=X]]` 等)其实都对,却被离线验证全否——根因①验证器要求 `matches[0]===target`(严格同一元素),而录制目标是无意义内层 `span`,AI 正确锚定包含它的 `div[role=button]`(点外层按钮与点里层 span 对点击等效);②`resolveClickable` 只上溯 4 层,FB 的 `div[role=button]` 在更上面没被录成目标。修复:**A. 放宽验证器(点击步骤)**——`verifyAgainstCapture`/`verifyFixed` 加 `acceptClickable` 参,新增 `matchHitsTarget(match,target,acceptClickable)`:`match===target` 或(点击时)`match.contains(target)&&isActionable(match)`(可点击祖先,`isActionable`=tag a/button 或 role∈button/link/tab/menuitem/option/checkbox/radio/switch)或 `target.contains(match)`(内后代)即算命中;用「可点击」白名单把关,挡掉仅包含目标的大容器 div(如 `#login-panel-container`)。**fill/wait 仍严格同一**(picker 精确指向,`step.type==='click'` 才放宽)。**B. `resolveClickable` 深度 4→8**(`webview-preload.ts`),新录制直接记住语义按钮,让录制/标记/AI/回放四方对齐。仅改 `renderer.ts`+`webview-preload.ts`,不动 core/agent/旁车/宏。`npm run build`+四项回归通过。**〔2026-07-07 修「AI 校正产出易变状态属性致回放超时」〕** 实测某 FB fill 步骤 AI 校正返回 `input[aria-invalid="true"][autocomplete="off"][inputmode="text"][type="text"]`,回放一直超时。根因:`aria-invalid="true"` 是**易变状态属性**(字段校验错误态才有),录制那一刻字段恰为 invalid 态、`buildElementContext` 抓的 contextHtml 带着它,故 `verifyAgainstCapture` 拿录制态子树比对**通过**;但回放时字段干净、无该属性→命中 0 个→`page.fill` 超时(且 `handleFill` 无指纹兜底、毫无补救)。**本次只做「防止」层**(不改回放引擎、不改仓库外 SOUL.md):①`ai-extract.ts` 的 `SELECTOR_QUALITY_GUIDE` 追加红线,明令**禁止把随交互/校验实时变化的状态属性作选择条件**(aria-invalid/aria-expanded/aria-selected/aria-checked/aria-pressed/aria-busy/aria-disabled/aria-current 及元素 value)——此常量在 `fixSelector` 组装 message 时无条件注入(AI 提取 `generateExtract` 也共用,正向收益);②`renderer.ts` 新增模块级 `hasVolatileAttr(selector)`(正则 `/\[\s*@?(?:aria-(?:invalid|expanded|selected|checked|pressed|busy|disabled|current)|value)\b/i`,同覆盖 CSS `[attr` 与 xpath `[@attr`,只拦明确状态属性、不误杀 data-testid/aria-label/data-value/name/id),在 `fixWithCapture`/`fixWithLiveDom` 两回路里拿到 `res.selector` 后、验证前**确定性拦截**:含易变属性即判不通过、带中文反馈复用同 sessionKey 重挑(≤3 轮),仍不行沿用既有「保留原选择器请人工核对」。**取舍**:只拦明确易变状态属性,`input[autocomplete][inputmode][type]` 这类「静态但过泛」不在拦截范围(靠既有 count>1 唯一性校验兜);未加 `handleFill` 指纹兜底,故旧宏里已存的坏选择器需对该步重跑一次 AI 校正才会被新准则/拦截逼出稳定选择器。regex 单测(易变全拦/稳定全放)+ `npm run build` + `verify-captures`/`verify-merge`/`verify-cancel`/`verify-pagination`(30 行)回归通过。**仅改 `ai-extract.ts`+`renderer.ts`,不动 core 回放/preload/主进程/旁车/宏 JSON。**
- 录制步骤撤销/重做(2026-07-02,Ctrl+Z 撤销 / Ctrl+Shift+Z·Ctrl+Y 重做,**通用**覆盖一切改动 `steps` 的操作):**不给每种操作写逆操作**,而是维护 `steps` 的 JSON 快照历史栈,在 `renderSteps()` 顶部 `captureHistoryOnRender()` **自动 diff**——因为所有 mutation(录制新增/拖拽排序/删除/插入暂停·滚动·等待/改填写内容·暂停文案·翻页页数/重新点选/AI 校正/加载宏替换)都紧跟一次 `renderSteps()`,而非改动型渲染(滚动组展开/折叠)不改 `steps` 内容不入栈;故现有与未来任何步骤操作**自动被覆盖,无需逐点插桩**。`undoSteps`/`redoSteps` 反序列化快照+重渲染,`applyingHistory` 标志防撤销自身再记账,出现新改动即清 `redoStack`。document `keydown` 监听:焦点在**可编辑控件**(INPUT/TEXTAREA/contentEditable,即地址栏/宏名/需求框/JSON 框/内联对话框输入)时**放行给浏览器原生文本撤销**、不误伤;`<webview>` guest 内按键不冒泡到宿主 document,故浏览器里操作不误触。会话内内存栈(上限 100,重启清空、不持久化);撤销恢复整份 `steps`(反序列化为新对象),滚动分组展开态(`expandedScrollGroups` WeakSet 按对象身份)重置为折叠(轻微 UX)。**仅覆盖录制步骤 `steps`**,提取规则/插件勾选不在范围(各自文本框有原生撤销)。**纯 `renderer.ts`+`index.html` 改动**,不碰 core/主进程/preload。

## 项目要点

- xlsxIntgration「国内银行整合」融合为后处理插件(2026-07-13,阶段1 PoC:跨语言桥接闭环打通):把外部纯 Python 项目 `D:\git_object\xlsxIntgration`(银行流水整合/对账)的**代号1 国内银行整合**接入 macro,形态=**热插拔后处理插件 + 子进程 spawn Python**。**架构**:macro 收到一批合规命名银行文件(`{公司}-{银行}.xls/xlsx/csv`)→ 插件拷进临时 inputDir → `spawn(pythonExe,['整合1.py'],{cwd:projectRoot,env:{BANK_INPUT_DIR,BANK_OUTPUT_DIR}})` → Python 整合产出 `国内银行汇总.xlsx` → 回收到 macro `exports/国内银行汇总-<stamp>.xlsx` 并 `showItemInFolder` 定位。**复用既有热插拔机制,前端/preload/回放引擎零改动**。**改动**:①**xlsxIntgration 侧唯一改动**——`src/bank_integration/config.py` 的 `INPUT_DIR`/`OUTPUT_DIR` 改为 `Path(os.environ.get("BANK_INPUT_DIR"/"BANK_OUTPUT_DIR",原默认))`,不设环境变量时行为完全不变(向后兼容,已验证默认值不变);该仓库单独 commit(`13bd2c9`)。②macro 新增 3 文件(均在 `src/core/post-processors/`):`python-bridge.ts`(纯 Node `child_process.spawn`,完整累积 stdout/stderr **不截断**,**设 `PYTHONIOENCODING=utf-8`/`PYTHONUTF8=1` 避免 Windows GBK 控制台中文乱码**,超时 kill,Promise 永 resolve 用 `exitCode`/`timedOut` 表达成败,逐行 `onLog` 转发日志)、`bank-integrate-config.ts`(仿 `request-rules-store`:读 `bank-integrate.json`,不存在写 inert 模板 + 坏 JSON 逐字段回退;字段 `pythonExe`/`projectRoot`/`timeoutMs`/`modes{type→{entryScript,summaryFile}}`)、`bank-integrate.ts`(handler + 末尾自注册 `type:'bank-integrate-domestic'`,label「国内银行流水整合」;`os.tmpdir` 临时目录隔离**不污染 xlsxIntgration 的 `data/`**,退出码非0/超时/无产物→throw 被注册表 catch 成非致命 message,finally 清理)。③**配置如何送达 handler**:给 `PostProcessContext` 加通用可选字段 `dataRoot?:string`(macro userData 根),`main.ts` 的 `run-macro`/`run-plugin` 两处 ctx 组装填入;handler 据此定位 `bank-integrate.json`。`merge-zip-excel` 忽略该字段,既有行为不变。④`index.ts` 末尾 `import './bank-integrate';` 自注册;`.gitignore` 加 `bank-integrate.json`。**触发**:侧栏「附加处理」列表自动出现「国内银行流水整合」,可勾选随回放或「直接运行」多选银行文件(**无需新 IPC/UI**,热插拔机制自动上架)。**MVP 取舍**:仅代号1;输入文件名须已合规(下载名→合规名自动映射留后续);Python 走本机 venv(路径在 `bank-integrate.json` 配);未做 mac 打包/双平台分发。**改动仅 xlsxIntgration `config.py` + macro `post-processors/{python-bridge,bank-integrate-config,bank-integrate}.ts`(新)/`index.ts`/`main.ts`/`.gitignore` + `scripts/verify-bank-integrate.mjs`(新);macro 回放引擎/preload/renderer/宏 JSON/旁车/既有 merge-zip-excel 零改动**。已验证:`npm run build` + `node scripts/verify-bank-integrate.mjs`(**真跑 Python 端到端**:拷 `data/input/1` 现成 10 个合规样本 → 桥接 spawn → 产出 12 工作表汇总 xlsx、中文无乱码)+ `verify-merge`(既有插件回归)通过 + config.py 默认值不变已验证。设计文档见 `xlsxIntgration融合设计.html`
  - **〔2026-07-13 后续:改为依赖各平台可执行文件(不再依赖 Python 脚本+venv)〕** 用户本意是依赖打包好的可执行文件而非脚本。**xlsxIntgration 侧**:用新 config.py **重新打包** Windows exe(`venv\Scripts\python.exe -m PyInstaller bank_integration.spec --clean --noconfirm`,产物 `dist/银行流水整合/国内银行整合.exe`,gitignore 无需 commit;旧 exe 是旧 config.py 打的不认环境变量,故必重打)。**macro 侧**:①**`python-bridge.ts` 改名 `subprocess-bridge.ts`**(`runPython`→`runSubprocess`,语义泛化"跑任意子进程程序");**新增按行 Buffer 智能解码 `decodeSmart`**(utf-8 主 + GBK 兜底 via `iconv-lite`)——PyInstaller exe 的 stdout 在 Windows 是 GBK(`PYTHONIOENCODING` 对 frozen exe 不生效),不解码则日志中文乱码(实测复现并修复)。②**`bank-integrate-config.ts` 配置模型平台化**:去掉 `pythonExe`/`projectRoot`/`entryScript`,改 `{timeoutMs?,modes{type→{executable,summaryFile}}}`;`defaultExecutable(type)` 按 `process.platform` 给默认路径(win32→`dist\银行流水整合\国内银行整合.exe`、darwin→`dist/bank-integration/domestic_bank_integration`)。③**`bank-integrate.ts`**:spawn `mode.executable`(**无脚本参数**,exe 即代号1入口),`cwd`=exe 所在目录,仍用 `BANK_INPUT_DIR/OUTPUT_DIR` 临时目录隔离(frozen config.py 的 template 用 exe 旁的、input/output 由环境变量指向临时目录)。④删本机旧 `bank-integrate.json` 让新模型重生。**决策**:可执行文件走运行时外部路径(不捆绑进 macro 包);纯 exe 不保留 python 回退。**Mac 取舍**:本机 Windows 打不了 Mac 二进制(PyInstaller 不跨平台),darwin 分支代码已就绪、二进制待在 Mac 上跑 `build_mac.sh` 后填路径(`dist_mac` 现为源码+venv 形态)。**改动仅 macro `post-processors/{subprocess-bridge(改名自 python-bridge),bank-integrate-config,bank-integrate}.ts`+`verify-bank-integrate.mjs`(改真跑 exe) + xlsxIntgration 重新打包(无源码改动,config.py 已提交)**。已验证:`npm run build` + `verify-bank-integrate.mjs`(**真跑 exe 端到端**:10 样本产 12 工作表汇总 xlsx、中文日志经智能解码不乱码、环境变量目录隔离生效、无 python/venv 参与)+ `verify-merge` 回归通过
  - **〔2026-07-13 后续:补齐其余 4 代号,5 代号(1/2/3/5/6)全部端到端打通〕** 现象:UI「直接运行」`bank-integrate-overseas`(代号2)/`order-match`(代号3)等只输出「bank-integrate.json 未配置模式「xxx」」就"完成",实为假成功空转。**根因两层**:①**macro 配置层没跟上注册层**——`bank-integrate.ts` 末尾已注册 5 个代号共享 handler,但 `bank-integrate-config.ts` 的 `defaultExecutable()`/`templateConfig()` 只给 domestic 默认 exe,其余 4 个代号在 `cfg.modes` 里不存在 → handler 命中 `if(!mode)` 分支 **return**(非 throw)一句"未配置模式"、被注册表当"完成"。②**xlsxIntgration 侧输入目录不认环境变量**——只有代号1 `config.py` 读 `BANK_INPUT_DIR`;`config2/3/5/6.py` 的 `INPUT_DIR_2/3/5/6 = DATA_DIR/"input"/"N"` **写死不读 env**(输出 `OUTPUT_DIR` 共享、认 `BANK_OUTPUT_DIR`),故即使配上 exe,桥接把用户文件拷进临时 `BANK_INPUT_DIR` 后 exe 也读不到 → 功能仍坏。③代号6 连 Windows exe 都没打包(`bank_integration.spec` 只含代号1-5)。**修复(完整端到端)**:**xlsxIntgration 侧**——(a)`config2/3/5/6.py` 输入目录改 `Path(os.environ.get("BANK_INPUT_DIR", DATA_DIR/"input"/"N"))`(各 exe 是独立进程、只跑一个代号,共用同一 `BANK_INPUT_DIR` 名无冲突;不设 env 回退原默认,向后兼容已验证);(b)`bank_integration.spec` 加代号6(a6/pyz6/exe6「代收代付对账」+ COLLECT + `data/input/6`)并 `PyInstaller --clean` **重打包出 6 个 exe**(含新 `代收代付对账.exe`);该仓库单独 commit(config2/3/5/6+spec)。**macro 侧**——(c)`bank-integrate-config.ts` 的 `defaultExecutable` 扩成 5 代号平台映射表 `EXE_NAMES`(win 中文名/mac 英文名)、`templateConfig()` seed 全 5 代号;**删死字段 `summaryFile`**(handler 早已改用 `findProducedXlsx` 取输出目录最新 xlsx,兼容固定名/带日期名);因 `loadBankIntegrateConfig` 以模板 modes 为底再并入磁盘 JSON,**旧的只含 domestic 的磁盘 `bank-integrate.json` 自动获得其余 4 代号默认,无需删文件**;(d)`bank-integrate.ts:124` 注释 `MODE_EXES`→`EXE_NAMES`;`verify-bank-integrate.mjs` 已覆盖 5 代号无需再改。**改动仅 xlsxIntgration `config2/3/5/6.py`+`bank_integration.spec`(+重打包产物,gitignore) + macro `bank-integrate-config.ts`(主)/`bank-integrate.ts`(注释)/`CLAUDE.md`;handler 逻辑/`subprocess-bridge.ts`/`index.ts` 注册表/回放引擎/preload/renderer/主进程/宏 JSON/旁车零改动**。已验证:`npm run build` + `verify-bank-integrate.mjs`(**5/5 代号各真跑 exe 端到端**,断言各产出 xlsx 可读、≥1 工作表)+ `verify-merge` 回归通过
  - **〔2026-07-13 后续:银行整合/对账拆到独立「工具」板块(与「附加处理」分家)〕** 用户洞察:侧栏「🧩 附加处理」板块里只有 `merge-zip-excel`(批量下载表格合并)能和宏主流程对得上——它消费 `ctx.downloads`(宏 list-action 逐项下载产出的表格),勾选随宏回放自动执行有意义;而 **bank-integrate 5 代号是「独立工具」**——输入是**人工归集的合规命名银行流水/admin 主表/平台对账文件**,不可能由某个录制宏回放时下载产出,勾选随宏运行是**空转甚至误导**,只用得上「直接运行(选文件)」通道。**证据**:`main.ts` 里 `ctx.downloads` 两个来源——路径 A(`run-macro`,`result.downloads` 宏回放产物)/路径 B(`run-plugin`,用户文件多选);bank-integrate 只在 B 有意义。**方案(纯 UI 信息架构重组,后端零改动)**:①`PostProcessorManifest`(`macro-types.ts` + `renderer.ts` 本地副本)加 `standalone?:boolean`;②`bank-integrate.ts` 注册 5 代号时 `standalone:true`(merge 缺省 false);③`index.html` 新增 `#tool-panel`「🧰 独立工具」板块(珊瑚 `--accent` 色,置于 `#plugin-panel` 后)+ CSS(复用 `.plugin-row`/`.plugin-desc`/`.plugin-run-now`,修 `.tool-panel .plugin-desc` 去 24px 缩进因无复选框),**不加进 `.sidebar:not(.advanced)` 隐藏列表 → 基础视图常驻**(与归高级的 `#plugin-panel` 相反);④`renderer.ts` 的 `loadPlugins()` 按 `standalone` 分流:`!standalone`→`#plugin-list`(复选框+直接运行),`standalone`→`#tool-list`(**无复选框,只「运行」按钮**);两分支收敛进 `renderPluginRow(container,p,withCheckbox)`;⑤`main.ts` 的 `run-plugin` 对话框文案中性化(「选择要处理的文件」)+ 过滤器补 `pdf`(代号2 华美银行需)。**关键**:独立工具无复选框 → 天然不被 `selectedPluginTypes()` 收集、不写进 `macro.postProcess`,路径 A 对它自动关闭,`selectedPluginTypes`/`refreshPluginSelection` **零改动**。**UX 抉择(用户拍板)**:板块基础视图常驻 + 彻底去掉随宏勾选入口。**旧宏兼容**:此前若把 bank-integrate 勾进 postProcess,加载时找不到 checkbox 忽略、执行也只返回「没有可整合的文件」无害。**改动仅 `macro-types.ts`/`renderer.ts`/`index.html`/`main.ts` + `bank-integrate.ts`(注册加标记);注册表/5 个 handler/`subprocess-bridge`/`bank-integrate-config`/回放引擎/preload/宏 JSON/旁车零改动**。已验证:`npm run build` + `verify-merge`(merge 后端回归)+ `verify-bank-integrate.mjs`(**5/5 代号真跑 exe 端到端**回归)+ `listPostProcessors()` 实跑确认分流标记正确(merge=附加处理、5 代号=独立工具);Electron 窗口视觉/点击弹框留用户肉眼验收
  - **〔2026-07-14 后续:打通 macro 的 Mac 版 + 独立工具 Mac 对接〕** 用户要把整个 macro(Electron 应用)也打通 Mac 版、并让独立工具在 Mac 上对接 xlsxIntgration 的 **Mac 二进制**;二进制定位=**外部可配置路径**(与 Windows 一致,不捆绑进 macro 包);Mac 产物统一走 **GitHub Actions(macOS runner)**构建下载(本机 Windows,PyInstaller/Playwright 都不能跨平台)。**铁律:Windows 已可用,改动全做成「按平台分叉的增量」,Windows 分支逐字保留、零回归**。**探索确认**:平台差异已良好收敛——独立工具的 darwin 分支(`EXE_NAMES` 5 代号英文名、`defaultExecutable` darwin)**早已写好**,handler(`bank-integrate.ts`)/桥(`subprocess-bridge.ts`)完全跨平台;**运行时 Chromium 定位也已跨平台**(`runtime-bootstrap.ts` 用 `process.resourcesPath`,Mac 上解析到 `<app>.app/Contents/Resources/ms-playwright`);缺口全在**打包期**。**改动(6 处,均增量)**:①`bank-integrate-config.ts`——`DEFAULT_XLSX_ROOT` 由硬编码 Win 路径改 `defaultXlsxRoot()` 按平台(win32 保持 `D:\git_object\xlsxIntgration`、darwin 取 `os.homedir()`、其它留空),`defaultExecutable` darwin 分支**去 `dist/` 前缀**改 `<root>/bank-integration/<英文名>`(对齐 Actions artifact 真实层级:实测下载产物 `bank-integration/` 下 8 个英文名二进制 + 共享 `_internal/`,与 5 代号 darwin 名逐字对齐)。②`package.json`——加 `build.mac`(`target:[dmg,zip]`,`icon:assets/icon.icns`,`category`)+ `scripts.dist:mac`(`electron-builder --mac`);**`build.win`/`dist` 一字不动**。③`scripts/copy-playwright-browser.mjs`——平台门槛由「非 win32 报错」放宽为「允许 win32/darwin,其它报错」,`findChromeExe`→`findChromeBinary` 按平台定位主程序(win `chrome-win64/chrome.exe`、mac `chrome-mac/Chromium.app/Contents/MacOS/Chromium`);Win 分支路径逐字保留。④`scripts/make-icon.mjs`——新增纯 Node PNG-based **ICNS 编码器**(`ICNS_TYPES` ic11/12/07/08/09/10 → 32/64/128/256/512/1024,复用现有 `encodePNG`/`renderRGBA`),产出 `assets/icon.icns`(已入库,结构自洽校验通过);`icon.png` 仍 256 不变。⑤新增 `.github/workflows/build-mac.yml`——macOS runner(arm64→macos-14/x64→macos-13)跑 `npm run dist:mac` 上传 `.dmg`/`.zip`,`CSC_IDENTITY_AUTO_DISCOVERY=false`(无 Apple 账号,未签名),手动选架构或 `v*` tag 触发;`npm ci` 设 `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1` 避重复下载(由 `pack:browser` 统一下)。⑥文档:`打包指南.md` 加第十节(Mac 打包 + Gatekeeper 未签名绕过 + 独立工具 Mac 对接步骤)、`开发计划.md`/`CLAUDE.md` 登记。**架构取舍**:捆绑 Chromium 按 runner 架构单架构,故按目标 Mac 架构逐架构构建(不用 universal);默认 arm64(与 xlsxIntgration 的 `macos-14` 一致,推断目标是 Apple 芯片)。**Mac 操作注意**:.dmg 与 bank-integration 二进制均未签名,首次运行需 `xattr -cr`/右键打开 + 二进制 `chmod +x`;Win/Mac 的 `bank-integrate.json` 不通用(各自首次运行按平台重生成)。**不改动**:`bank-integrate.ts`/`subprocess-bridge.ts`/`runtime-bootstrap.ts`/`main.ts`(运行时)/core 回放/preload/renderer/宏 JSON/旁车/注册表/5 handler/`build.win`/`dist`(win)。**已验证(本机 Windows 能做的)**:`npm run build` + 平台分叉纯逻辑核对(模拟 `process.platform`:win32 路径逐字不变、darwin 产出 `bank-integration/<英文名>` 且名对齐真实产物)+ `verify-merge` + `verify-bank-integrate`(**5/5 代号真跑 Windows exe 端到端**)+ icon.icns 结构自洽(6 块/总长一致/各块合法 PNG)全过。**真正 Mac 端到端(.dmg 启动 + Playwright Mac Chromium + 独立工具选文件产 xlsx)待在 macOS Actions/Mac 上验收**
- 录制端拦截并改写 POST 请求体(2026-07-09,可控范围改请求参数):需求=录制阶段拦截某网站请求、在可控范围改其**参数**;经确认作用阶段=**录制端**、参数=**POST body**、形态=先「能改」即可。**关键约束**:录制用 Electron `<webview>`,而 Electron 的 `session.webRequest` 只能改 URL(query)/请求头、**改不了 POST body**(`uploadData` 只读),故录制端改请求体**唯一路径 = CDP**(`webContents.debugger` 挂 `Fetch` 域,请求发出前暂停→替换 body→放行)。**做法**:①新模块 `src/main/request-interceptor.ts` 的 `RequestInterceptor`——`attach(wc)` 走 `wc.debugger.attach('1.3')`(**try/catch**:DevTools 已开会抢占 debugger,记告警不致命)+ 从规则 `urlPattern` 生成 `Fetch.enable({patterns:[{urlPattern,requestStage:'Request'}]})`(**只暂停命中规则 URL 的请求**,降开销/不误伤);`Fetch.requestPaused` 里仅 POST 且命中规则才改写,取完整 body 用 `Fetch.getRequestPostData`(**遵守「禁止截断」铁律**,不用事件里可能被截断的 `request.postData`),改写后 `Fetch.continueRequest({requestId, postData: base64(newBody)})`(**CDP 要求 postData 为 base64**;Content-Length 由网络栈重算,同 Puppeteer 的 `request.continue({postData})`),**未命中/非 POST/解析失败/出错都必须 `continueRequest` 放行,否则页面卡死**;`detach` 关 `Fetch.disable`+`debugger.detach`+`fs.unwatchFile`。纯逻辑抽成 export 的 `globToRegExp`(CDP glob→正则:`*`→`.*`、`?`→`.`、`\`转义、`.`等正则元字符转义)/`decideBodyType`(规则显式 > Content-Type 嗅探 > body 首字符 `{` 判 json,兜底 form)/`rewritePostBody`(json:`JSON.parse`→合并 set/删 remove→`stringify`,**保留原始类型**;form:`URLSearchParams` set/delete→`toString`;无 set/remove 返回 null)**便于离线自测**。②规则存储 `src/storage/request-rules-store.ts`(仿 `browser-config-store.ts`)——`loadRequestRules`:不存在→**自动建 inert 模板**(`enabled:false`+示例规则)、坏 JSON→回退空、逐条 `normalizeRule` 校验(无 `urlPattern` 丢弃);类型 `RequestRule{urlPattern,bodyType?('json'|'form'),set?,remove?}`/`RequestRulesConfig{enabled,rules}` 放 `macro-types.ts`(与 `BrowserConfig` 同处)。③配置文件 `request-rules.json`(`dataRoot` 根,与 `browser-config.json` 同级,已 gitignore);**热更新**——`attach` 里 `fs.watchFile(configPath,{interval:1000})` 监听,改 `enabled`/规则即 `applyPatterns`(转 enable/disable/换 patterns,`enabledKey` 签名去重)即时生效无需重启。④接线:`main.ts` 顶部加 `requestRulesPath=path.join(dataRoot,'request-rules.json')`,`did-attach-webview` 里 `new RequestInterceptor(requestRulesPath).attach(guestContents)`、`guestContents.once('destroyed')` 里 `detach`。**MVP 取舍(用户答「先只要能改,形态随意」)**:无 UI(规则=编辑 `request-rules.json`,侧栏面板留后续);未覆盖 multipart/form-data、嵌套字段路径、query/请求头改写、回放端(Playwright `route()`)拦截;**已知限制**——CDP debugger 独占,用户手动开该 webview 的 DevTools 会顶掉拦截(触发 `detach`,记日志)。**改动仅 `src/main/request-interceptor.ts`(新)/`src/storage/request-rules-store.ts`(新)/`macro-types.ts`(加两类型)/`main.ts`(接线)/`.gitignore`;core 回放/preload/renderer/宏 JSON/旁车零改动**。已验证:`npm run build` + `node scripts/verify-request-rewrite.mjs`(纯逻辑全过)+ `node scripts/verify-request-intercept.mjs`(**真跑 Electron 端到端**:本地 echo 服务同源提供测试页避 CORS + webview 发 `POST /echo`,挂真实拦截器,断言服务端实收 `{page:1,size:100,injected:"yes"}`——即 size 被 set 覆盖、injected 新增、page 保留、debug 删除,真跑通 CDP 链路)+ `verify-captures`/`verify-merge` 回归通过(`verify-cancel` 因本机缺 Playwright headless_shell 浏览器二进制而失败,属预存环境问题、与本改动无关)
  - **〔2026-07-09 后续:支持数组值 + 新增 append 追加去重〕** 用户反馈两个卡点:①数组值(如 `encryptedVideoIds:["B5IvLw_ihbU"]`)不知怎么写——**其实 `set` 早就支持任意 JSON 值**(json 分支 `obj[k]=v` 原样保留类型、校验层不限制值类型),用户误写成字符串 `"['pe4-3g8q7o8']"`(带引号+单引号非合法 JSON),**正确写法=真数组 `["pe4-3g8q7o8"]`**;根因是示例/模板/文档无数组例子。②没有 append。**改进(仅顶层字段,与 set/remove 一致;不做嵌套点路径)**:`RequestRule` 加 `append?:Record<string,unknown>`(`macro-types.ts`);`rewritePostBody` 加 `appendEntries` + 空动作守卫纳入 append + 两个纯函数 helper——`appendJsonField`(value 数组则逐元素、否则单个;`obj[key]` 数组→复制/`undefined`→`[]`/单值→`[原值]`;`Set(base.map(JSON.stringify))` **去重**后 push)、`appendFormField`(`params.getAll(key)` 作已存在集合,`String` 化后 `params.append`、同值去重);**执行顺序 set→append→remove**;`rewriteBody` 日志补 `append=` 段(`request-interceptor.ts`);`normalizeRule` 仿 `set` 校验 `append`(非数组对象即原样保留)、`templateConfig` 补数组 set + append 演示(`request-rules-store.ts`);`examples/request-rules.example.json` 补 `_字段.append` 说明 + 数组值/append/form 三例 + 「数组值直接写真数组勿加引号」提示;`scripts/verify-request-rewrite.mjs` 加 `5)set 数组保留`+`6)json append 去重`(追加/新建/单值包数组/逐元素/去重/set 后 append)+`7)form append`+`8)只有 append 不返回 null`。**改动仅 `macro-types.ts`/`request-interceptor.ts`/`request-rules-store.ts`/`examples`/`scripts`/`CLAUDE.md` + 用户实时 `request-rules.json`(改正字符串→真数组、删重复死规则、加 append 写法注释);core 回放/CDP 拦截管线/preload/renderer/主进程/宏 JSON 零改动**。已验证:`npm run build` + `node scripts/verify-request-rewrite.mjs` 全过 + `verify-captures`/`verify-merge` 回归
  - **〔2026-07-10 回放端也支持拦截改写(Playwright route)〕** 需求=录制拦截外,**回放阶段操作时也拦截改写请求**。回放/提取用独立 Playwright Chromium(录制用 Electron webview),原先无任何请求路由代码。**做法(与录制端共用同一份规则/开关,用户确认)**:①**抽取改写纯逻辑到 core**——新建 `src/core/request-rewrite.ts`,把原在 `request-interceptor.ts` 的 `globToRegExp`/`decideBodyType`/`rewritePostBody`/`headerValue`/私有 `appendJsonField`/`appendFormField` 原样搬来,**新增可复用纯函数 `matchRule(rules,url)`**(录制端私有方法逻辑提出);`request-interceptor.ts` 改为从 core import + **再导出** `globToRegExp`/`decideBodyType`/`rewritePostBody`(保持导出面不变,`verify-request-rewrite.mjs` 仍从 `dist/main/request-interceptor.js` 取用零改动),调用点改 `matchRule(this.config.rules,url)`,删本地副本。**录制端行为一字不变**。②**回放端注册 route**——`macro-runner.ts` 新增私有 `installRequestRoutes(context)`:仅 `session.requestRules?.enabled && rules.length>0` 才 `context.route('**/*',handler)`(**全量拦截 + handler 内用 core `globToRegExp` 匹配**,不把 `urlPattern` 交给 Playwright——**其 glob 方言与 CDP 不同会致命中集漂移**);handler 内非 POST/未命中/无 body/无动作/异常都 `route.continue()` 放行(**铁律:每条请求必 continue 否则卡死**),命中且有改写则 `route.continue({postData:newBody})`(**Playwright 收普通字符串,非 CDP 的 base64**)。挂 context 上**自动覆盖初始页与后续所有 popup**;调用点在 `this.activeContext=context` + 取消检查之后、反检测 addInitScript 之前(**必须早于第一个 goto**),持久化/临时两分支合流后统一注册故都生效。③**数据注入(模式 B)**——`SessionOptions` 加 `requestRules?:RequestRulesConfig`;`main.ts` 的 `buildSessionOptions()` 复用已有 `requestRulesPath`(与录制端同一份)`loadRequestRules` 读入,`enabled && rules.length>0` 才塞 `options.requestRules`。**不做回放期运行中热更新**(每次 run-macro 都重新 `buildSessionOptions` → 天然按次加载最新)。④**自检**——新建 `scripts/verify-replay-intercept.mjs`(回放端真跑 Playwright route E2E)。**MVP 取舍**:与录制端同限(未覆盖 multipart/嵌套路径/query/请求头;非法 JSON 原样放行);未做分端开关(将来若需「只录制/只回放拦截」,`RequestRulesConfig` 加 `applyOnRecord?`/`applyOnReplay?` 缺省 true,两端门槛各加一条判断)。**改动仅 `src/core/request-rewrite.ts`(新)/`src/main/request-interceptor.ts`/`src/core/macro-types.ts`(SessionOptions 加字段)/`src/core/macro-runner.ts`/`src/main/main.ts`/`scripts/verify-replay-intercept.mjs`(新);宏 JSON/旁车/preload/renderer 零改动**。已验证:`npm run build` + `verify-request-rewrite`(纯逻辑全过,证录制端函数不变)+ `verify-replay-intercept`(回放端 E2E,服务端实收 `{page:1,size:100,injected:"yes"}`)+ `verify-request-intercept`(录制端 CDP E2E 回归)+ `verify-merge`/`verify-pagination`(30 行,route 未启用路径)回归通过
- 录制/回放两端「只记录不修改」支路(2026-07-10,拦截所有请求写时间线供分析):需求=在现有改写之外加一条**独立支路**——不修改任何请求,只拦截**所有请求(不限 method)** + 响应记录到时间线文件,供事后分析;即便改写关闭也要能记录。用户拍板:**两端都记录 · 记请求+响应 · 配置文件开关+JSONL**。**关键取舍**:记录走**被动观测**(录制端 CDP `Network` 域、回放端 `context.on('request'/'response'/'requestfailed')`),不暂停请求、不进改写路径——「支路互不影响」;命中改写规则的 URL,记录到的是**页面原始 body**(改写前),两端一致(当作特性)。**做法**:①**新建共享写入器 `src/core/timeline-recorder.ts`**(纯 fs/零 Electron/零 Playwright,类比 request-rewrite.ts 被两端 import):`TimelineRecorder(dir,phase,urlPattern?)`,统一 entry `{t(ISO),phase,kind:'request'|'response',id,method,url,reqHeaders?,reqBody?,status?,timingMs?,respHeaders?,mimeType?,error?}`;**每请求写两行**(request 到达即落、response 完成时落,同 `id` 关联);`fs.appendFileSync` JSONL(**全项目首次引入 append 写**);文件名 `timeline-<phase>-<毫秒戳>.jsonl`(毫秒级 fileStamp 避免两端同秒撞名);复用 `globToRegExp` 做 `matches`;`setPattern` 热更新;**熔断**(落盘失败置 disabled、后续静默不写不抛,记录失败绝不拖垮主流程);**body 完整不截断**。②**类型/存储**——`macro-types.ts` 加 `TimelineRecordConfig{enabled,urlPattern?,includeBody?}` + `RequestRulesConfig.record?`;`request-rules-store.ts` 加 `normalizeRecord`(config 级、非 per-rule)、`templateConfig` 补 inert `record`。③**录制端**——**扩展 `request-interceptor.ts`(不新建模块:debugger 单例已被它独占,Network 与 Fetch 可同会话共存)**:构造加第 2 参 `timelinesDir`;`debugger.on('message')` 加 `Network.*` 分支;`applyRecording()`(仿 applyPatterns,`recordKey` 签名去重,`Network.enable/disable`);`onNetworkEvent` 串 `requestWillBeSent`(id=CDP requestId)→`responseReceived`(回填 status/mimeType/headers)→`loadingFinished`(算 timingMs 写响应行)/`loadingFailed`;`emitRequestLine` 用 **`Network.getRequestPostData` 取完整 body**(遵守禁止截断,不用事件里可能截断的 postData);`detach` 加 `Network.disable`+清 pending。④**回放端**——`macro-runner.ts` 构造加第 6 参 `timelinesDir`(仿 downloadDir sibling 默认);`installTimelineRecording(context)` 在 `installRequestRoutes` 后、第一个 goto 前调用,**独立门槛 `record?.enabled`**(不看改写 enabled);`context.on('request')` 分配 `WeakMap<Request,{id,start}>` 写请求行(`req.postData()` 完整);**`context.on('response')`(而非 requestfinished)同步写响应行**——响应头到达即触发、早于 body 完成与 context 关闭,避开「宏结束后 context 立即关闭致异步 req.response() 来不及」竞态,timingMs 用 `Date.now()` 差值;`requestfailed` 写 error 行。⑤**接线 `main.ts`**——加 `timelinesDir` 常量+入 `ensureDirs`;录制端 `new RequestInterceptor(requestRulesPath, timelinesDir)`;回放端 `new MacroRunner(...,downloadsDir,timelinesDir)`;**`buildSessionOptions` 门槛放宽**为 `rewriteActive || recordActive`(record-only 即 `enabled:false` 也把 config 带给 runner;`installRequestRoutes` 与录制端 `applyPatterns` 的改写门槛不变——两支路彻底解耦)。⑥`.gitignore` 加 `timelines/`。**MVP 取舍**:响应只记 status/耗时/mimeType(不记响应体);未覆盖流式/SSE(loadingFinished 不发则无响应行);未做 UI(编辑 request-rules.json 的 record 段)。**改动仅 `timeline-recorder.ts`(新)/`macro-types.ts`/`request-rules-store.ts`/`request-interceptor.ts`/`macro-runner.ts`/`main.ts`/`.gitignore`/`examples`/3 个 verify 脚本;宏 JSON/旁车/preload/renderer/改写链路零改动**。已验证:`npm run build` + `verify-timeline-recorder`(纯逻辑:两行同 id/matches/**1MB body 不截断**/append/懒建/熔断全过)+ `verify-timeline-replay`(回放端 E2E,记录条数 2:request+response,证 record 独立于改写门槛)+ `verify-timeline-record`(录制端 CDP Network E2E,同样 request+response 完整)+ `verify-request-rewrite`/`verify-replay-intercept`/`verify-request-intercept`/`verify-merge` 回归全过
  - **〔2026-07-10 后续:回放端改写+记录运行中热更新〕** 原回放端两支路都在**每次运行开始那一刻把配置定死**(`installRequestRoutes` enabled=false 不注册 route、`installTimelineRecording` record 关不挂监听;配置仅 `buildSessionOptions` 读一次)——回放跑到一半改 `request-rules.json` 当次不生效。需求=**回放运行中也能开关改写/记录**(与录制端 `fs.watchFile` 对齐)。**做法(`macro-runner.ts` 主改 + `main.ts` 接线,`request-rewrite.ts`/`timeline-recorder.ts`/录制端零改动)**:①原 `installRequestRoutes`/`installTimelineRecording` 合并重构为 `setupRequestHandling(context)`(run() 里早于第一个 goto 调用):建**一次性** route handler(读实时 `this.rewriteRules`)+ **常挂** `context.on('request'/'response'/'requestfailed')` 三监听(读实时 `this.recorder` 决定写不写;`meta` WeakMap 记 id/start/**recorder**,响应写回同一 recorder 保证请求行响应行同文件)+ 应用初始配置。②新增 `applyReplayRewrite(cfg)`(**仿录制端 `applyPatterns`**:enabled 且有规则才 `context.route('**/*')`、否则 `context.unroute`——**改写关时零 route 开销守恒**;规则变则 handler 读新 `rewriteRules` 自动生效)、`applyReplayRecord(cfg)`(**仿 `applyRecording`**:关→开建**新** `TimelineRecorder`(新文件)、开→关置 null 停写、仅 pattern/includeBody 变则原地 `setPattern`)。③公有 `updateRequestRules(cfg)`(`if(!activeContext)return` 兜住晚触发;`void applyReplayRewrite().catch()` + `applyReplayRecord()`)。④`main.ts` 的 `run-macro` 里 `fs.watchFile(requestRulesPath,{interval:1000})`→`runner.updateRequestRules(loadRequestRules(...))`,随本次运行的 `listeners[] finally` `unwatchFile`(复刻录制端写法)。**关键取舍**:改写用 `route`(每请求绕 Node 有延迟)故 register/unroute 保零闲时开销;记录用被动 `context.on`(不拦截、开销可忽略)故常挂监听 + 标志。**限制**:route/unroute 只影响此后请求(中途开关不追溯在途请求);热更新延迟约 1s(watch interval);record 关→开一次 run 内多个时间线文件;`macro-types.ts` 仅 import 面(无类型变更)。已验证:`npm run build` + `verify-hotreload`(**新建**,证改写与记录都能 run 中 off→on:8 POST 前 3 未改/未记、后 5 已改/已记录 5<8)+ `verify-replay-intercept`/`verify-timeline-replay`(初始 enabled 路径不回归)+ `verify-cancel`/`verify-popup-wait`(回放引擎主流程)+ `verify-request-intercept`/`verify-timeline-record`(录制端未动)+ 纯逻辑/离线四项 全过
- 录制/回放两端「重发型」拦截支路(2026-07-15,在改写之外新增:命中某请求后延时改参重发一个新请求):需求=配置「某请求命中之后,间隔 n 秒,改某参数,主动重发一个新请求」,复用性高、两端可热开关。**与「改写」的本质区别**:改写就地改原请求;重发**不改原请求**(原请求照常放行),额外主动发一个新请求——这是项目两端第一次需要「主动发起 HTTP 请求」。**用户拍板**:重发次数=**可配置固定 N 次**(`repeat`+`intervalMs`,默认 1);触发频率=**每次命中都重发**(默认不去抖,保留可选 `dedupeMs` 逃生阀);热开关=**复用现有 `enabled` 总开关**(不引入独立开关)。**数据模型(工程决策)**:`RequestRule` 不动,新增独立 `ResendRule` + `RequestRulesConfig.resends?[]`(与改写 `rules[]` **物理分开**——因 `matchRule` 首命中即返回,混一个数组会让改写/重发互抢首命中;`normalizeRule` 校验契约也保持「改写-only」干净)。`ResendRule{urlPattern,delayMs?,targetUrl?,method?('POST'|'GET'),bodyType?,set?/append?/remove?,repeat?(clamp[1,100]),intervalMs?,dedupeMs?}`。**共享纯逻辑 `request-rewrite.ts`**:①`matchRule` 改**泛型** `<T extends {urlPattern}>`(向后兼容,改写侧仍推断 RequestRule、重发侧得 ResendRule);②新增 `RESEND_MARK_HEADER='x-macro-resend'`/`isResendOrigin`(防递归识别)/`resolveResendTarget`(空→触发URL、相对→绝对化)/`buildResendHeaders`(拷触发头保留 Authorization/CSRF、排除 host/content-length/connection/cookie/origin/referer/content-type、补 Content-Type + 标记头);**改参直接复用 `rewritePostBody`**(把 ResendRule 当规则视图喂入,零改动)。**回放端 `macro-runner.ts`**:`setupRequestHandling` 末尾加**常挂被动** `context.on('request')` 观察监听(靠 `resendWant` 标志 + `isResendOrigin` 防递归守卫 + `matchRule(resendRules)` 命中→`scheduleReplayResend`);新增 `applyReplayResend(cfg)`(仿 applyReplayRewrite/Record,`want=enabled&&resends.length>0`,关→清定时器)、`scheduleReplayResend`(去抖→算 target/bodyType/改参/头→`repeat` 次 `setTimeout`)、`fireReplayResend`(**页面内 fetch**:`activePage.evaluate(fetch)` 在当前活动页执行,DevTools Network 可见、带页面完整登录态;受 CSP/CORS 约束、无活动页则跳过。〔2026-07-16 由 APIRequestContext 改为页面内,以便在 DevTools 核对重发——见下方后续〕);`updateRequestRules` 加 `applyReplayResend`;`resendTimers:Set<Timeout>`+`clearResendTimers()` 在 `cancel()`/`run()` finally 清理。**录制端 `request-interceptor.ts`**:搭现有 Fetch 暂停便车——`applyPatterns` 泛化为「Fetch 拦截总管」(`active=rewriteActive||resendActive`,patterns=改写 URL ∪ 重发 URL);`onRequestPaused` 开头 `isResendOrigin` 防递归放行、命中 `resends` 则 `getPostData`(抽出复用,不动 `rewriteBody`)+ `scheduleResend`(原请求照常 continue);`fireResend` 用 **`wc.executeJavaScript('fetch(...)')`**(页面上下文,带 cookie/登录态/origin;值全 `JSON.stringify` 内插防注入);`resendTimers`+`clearResendTimers()` 在 `detach`/debugger 分离/热关闭清理。**存储 `request-rules-store.ts`**:抽 `applyActionFields`(bodyType/set/append/remove 校验,改写与重发共用 DRY)、新增 `normalizeResendRule`(数值字段非负校验、repeat clamp、method 枚举)、`loadRequestRules` 加 `resends` 归一化、`templateConfig` 加 inert 示例。**`main.ts`**:`buildSessionOptions` 门槛加 `resendActive=enabled&&resends.length>0`(纯重发场景也下发 config);录制端 attach、回放期 `fs.watchFile` **零改动**(传整份 config,新字段随之流入、热更新天然覆盖)。**防递归两道保险**:重发请求带 `x-macro-resend:1` 头,两端观察路径识别即整体跳过(回放端页面内 fetch 会经 route 与 on('request'),故**改写 route handler 与重发观察监听都加 `isResendOrigin` 守卫**兜住)。**限制**:两端都在页面内跑 fetch,跨域重发受 CSP/CORS 限制、需当前有活动页;v1 限 POST 触发。**〔2026-07-16 后续:回放端重发 APIRequestContext → 页面内 fetch〕** 原回放端用 `activeContext.request.fetch()`(APIRequestContext,页面外发),请求不归属任何页面 frame → **DevTools Network 面板看不到**(用户要能在 DevTools 核对重发)。改为 `this.activePage.evaluate(()=>fetch(...))` 在当前活动页执行(页面发起 → DevTools 可见 + 带页面完整上下文含 localStorage token/现算 CSRF)。**连带处理**:①`activePage` 原是 `run()` 闭包局部变量,新增实例字段 `this.activePage`(在 `activePage=page` 与 `context.on('page')` 两处同步、finally 置 null);②页面内 fetch **会经过改写 route**,故给 `rewriteHandler` 开头补 `isResendOrigin` 守卫(重发请求直接 continue 不被改写);③evaluate 用**序列化参数**传 `{url,method,headers,body}`(Playwright 自动序列化,非字符串拼接,免注入)。**取舍**:换来 DevTools 可见 + 上下文更全,代价=跨域受 CSP/CORS、依赖当前有活动页(页面已关/导航中则记日志跳过)。仅改 `macro-runner.ts`。已验证 `verify-resend`(日志 `已重发(页面内)`,8/8)+ `verify-replay-intercept`/`verify-hotreload`/`verify-request-rewrite` 回归全过**改动仅 `macro-types.ts`/`request-rewrite.ts`/`request-rules-store.ts`/`macro-runner.ts`/`request-interceptor.ts`/`main.ts`/`examples`/`scripts/verify-resend.mjs`(新);宏 JSON/旁车/preload/renderer/UI 零改动**。已验证:`npm run build` + **`verify-resend`(回放端 E2E,8/8:原始 `{page:1}`→重发 `{page:1,resent_flag:yes}`、延时 1521ms≈1500、重发数=1 防递归、独立于改写)** + `verify-request-rewrite`(纯逻辑证改写函数不变)/`verify-replay-intercept`/`verify-hotreload`/`verify-timeline-replay`/`verify-merge` 回归全过(录制端 CDP 端到端待 Electron 实跑,逻辑与已过 E2E 的回放端对称)
  - **〔2026-07-17 后续:resends 项可选「本地文件整体替换 body」〕** 需求=让 `resends` 也能像 `bodyReplaces` 一样用本地文件的完整字节整体作重发 body(不再只能 set 改字段),使重发既有编排能力(delayMs/targetUrl/repeat/dedupe)又能发任意二进制(YouTube 换视频)。**用户拍板**:①仅加到 resends(普通 rules 的「就地替换原请求 body」已由 `bodyReplaces` 覆盖,不重复);②`replaceWithFile` 存在则整体用文件字节、忽略 set/append/remove/bodyType(二进制无法字段级改写)。**架构**:请求拦截仅回放端生效、bodyReplace 本就只在回放端,故本功能只在**回放端 `macro-runner.ts`** 实现。**改动**:①`macro-types.ts` 的 `ResendRule` 加可选 `replaceWithFile?`;②`macro-runner.ts` 的 `scheduleReplayResend` 分流——文件路 `fs.readFileSync(rr.replaceWithFile)`→`toString('base64')`、`binary=true`、`ctForHeaders=触发请求原 content-type`(不套 json/form 默认)、读失败 `logError` 后 `return` 跳过本次重发(不 arm 定时器、不抛,**文件只读一次、repeat 定时器复用**),否则走现有 rewritePostBody 改参路;`fireReplayResend` 加第 5 参 `binaryBase64`——`page.evaluate` 里 `payload = bin ? Uint8Array.from(atob(b),c=>c.charCodeAt(0)) : b`(字符串路 `bin=false` 零回归;payload 声明 `any` 规避 TS 5.7 `Uint8Array<ArrayBufferLike>` 对 DOM BodyInit 的挑剔);③`request-rules-store.ts` 的 `normalizeResendRule` 加**可选**非空 string 校验(仿 `normalizeBodyReplaceRule` 但不满足只是不写字段、不丢整条)+ 模板注释。**零改动**:`request-rewrite.ts`(文件路只是传不同参数并绕过 rewrite)/`request-interceptor.ts`(录制端死代码)/`main.ts`(热更新链路 `updateRequestRules→applyReplayResend` 随 `resendRules=cfg.resends` 自动带入新字段)/preload/renderer/UI/宏 JSON/旁车/既有各支路。**Content-Length 无需处理**(浏览器对 Uint8Array body 自动置、buildResendHeaders 本就剔除 content-length)。**边界**:文件经 base64→page.evaluate→页面 atob+Uint8Array 再物化,峰值内存比 bodyReplaces 的 CDP 路高(超大 mp4 优先 bodyReplaces、`repeat>1` 建议配 `dedupeMs`);method 仍限 POST|GET;content-type 保留触发请求原值(触发是 json 而文件是 mp4 会声明 json 实为二进制,真实换视频应配 `targetUrl` 指真上传端点);schedule 时读一次(定时窗口内换文件仍发旧字节)。**改动仅 `macro-types.ts`/`macro-runner.ts`/`request-rules-store.ts`/`examples`/`scripts/verify-resend-replace.mjs`(新)+`CLAUDE.md`/`开发计划.md`**。已验证:`npm run build` + `verify-resend-replace`(**回放端 E2E**:页面发 JSON 触发、replaceWithFile=B(600 字节全字节值),断言重发体 == B 逐字节相等、原触发字节 != B、延时 ≈800ms)+ 全套回归 6 项(`verify-resend` 改参路零回归 / `verify-body-replace` / `verify-hotreload` / `verify-replay-intercept` / `verify-request-rewrite`)全过;真实 YouTube 换视频待用户登录回放手动验收
  - **〔2026-07-17 后续:resends 项新增 setHeaders/removeHeaders 改重发请求头〕** 需求=`resends` 支路此前只能改重发**请求体**(set/append/remove/replaceWithFile),不能改**请求头**;重发头由 `buildResendHeaders` 固定生成(继承触发头 → 剔除 host/content-length/connection/cookie/origin/referer/content-type → 补 Content-Type → 补防递归标记头 `x-macro-resend`),用户无法增删改。本次给 `ResendRule` 加可选 `setHeaders?`/`removeHeaders?`,**直接复用 `responseRules` 已验证的大小写不敏感改头模式**(不新造)。**改动**:①`macro-types.ts` 的 `ResendRule` 加 `setHeaders?:Record<string,string>`/`removeHeaders?:string[]`(注释参照 `ResponseHeaderRule`);②`request-rewrite.ts` 的 `buildResendHeaders` **加可选第 3 参 `overrides?:{setHeaders?,removeHeaders?}`**(不传则行为逐字不变、**零回归**)——在补 Content-Type 之后、补标记头之前应用:借鉴 `rewriteResponseHeaderRecord` 的 `dropLower` 写法(remove∪set 键小写去重,先删后写,允许覆盖 Content-Type),**`out[RESEND_MARK_HEADER]='1'` 放最后一行无条件执行**,即便用户在 remove/set 里误碰标记头也强制补回(**防递归核心不被破坏**);③两个调用点透传 `{setHeaders:rr.setHeaders,removeHeaders:rr.removeHeaders}`——回放端 `macro-runner.ts` 的 `scheduleReplayResend`、录制端 `request-interceptor.ts` 的 `scheduleResend`(录制端拦截当前不接线、仅回放端生效,同步更新只为共享函数两端一致 + 该类仍被自检直接实例化);④`request-rules-store.ts` 的 `normalizeResendRule` 加白名单校验(**复用同文件 `normalizeStringMap` 校 setHeaders、`Array.isArray` filter 校 removeHeaders**,与 `normalizeResponseHeaderRule` 同款)——新字段能否穿过归一化的真正闸门。**零改动**:`main.ts`(`resendActive` 门槛不感知字段、`fs.watchFile` 热更新整条透传对象)、`request-rewrite.ts` 的 `matchRule`/`rewritePostBody`、preload/renderer/UI、宏 JSON/旁车、既有各支路。**改动仅 `macro-types.ts`/`request-rewrite.ts`/`macro-runner.ts`/`request-interceptor.ts`/`request-rules-store.ts`/`examples`/`scripts/verify-resend-headers.mjs`(新)+`CLAUDE.md`/`开发计划.md`**。已验证:`npm run build` + `verify-resend-headers`(**回放端 E2E**:setHeaders 加 `x-custom`、removeHeaders 删本会被继承的 `x-inherited`(`x-keep` 证继承仍在故删除非空跑)、标记头 `x-macro-resend=1` 受保护、原始请求不受影响,11 断言全过)+ 全套回归 6 项(`verify-resend` 改参路零回归 / `verify-resend-replace` / `verify-request-rewrite` 纯逻辑证老路径不变 / `verify-replay-intercept` / `verify-hotreload` / `verify-merge`)全过(录制端 CDP 端到端未单跑,逻辑与已过 E2E 的回放端对称)
  - **〔2026-07-17 后续:resends 新增「响应条件触发」(responseTrigger)〕** 需求=把重发的触发从「请求侧 urlPattern 命中即发」升级为**基于响应**:捕获某请求的响应,当**响应头某值** 或 **响应体某 JSON 字段(嵌套路径)=某值** 时才重发(解决续传轮询场景下同一 URL 被反复 POST 致重发被反复触发的困惑)。**用户拍板**:响应体按 **JSON 嵌套点路径=值**;一条规则多条件(status/headers/bodyJson)**全部满足 AND**;**仅回放端**。**设计取向**:扩展现有 `ResendRule`(加可选 `responseTrigger` 子对象)而非新建数组——「重发这个动作」的全部能力(targetUrl/method/set·append·remove/replaceWithFile/setHeaders·removeHeaders/delayMs/repeat/intervalMs/dedupeMs)与响应触发所需完全一致,只是**触发方式**不同,命中后仍复用 `scheduleReplayResend` 整套机器;不带 `responseTrigger` 的规则保持原请求侧触发(**向后兼容**)。**改动**:①`macro-types.ts` 新增 `ResendResponseTrigger{status?,headers?,bodyJson?}` + `ResendRule.responseTrigger?`(带它时 urlPattern 匹配**响应 URL**)。②`request-rewrite.ts` 新增纯逻辑:`getJsonByPath(obj,path)`(点路径逐层取值,遇非对象/缺失→undefined)、`headersAllEqual(headers,expected?)`(提取 `responseConditionMet` 的头比对循环、**把 responseConditionMet 改为委托它**,零行为变化小重构)、`triggerNeedsBody(trigger)`(仅有 bodyJson 才需读体,门控异步)、`responseTriggerMet(trigger,status,headers,bodyText)`(status 等值 + headers `headersAllEqual` + bodyJson:bodyText 为 null/`JSON.parse` 失败→false、逐路径 `String(getJsonByPath)===期望` AND;三组均可选,都不给→恒真)。③`macro-runner.ts`(核心):加实例字段 `responseResendRules`/`resendResponseWant`;`applyReplayResend` 把 `cfg.resends` 按有无 `responseTrigger` **拆两组**(`resendRules`=请求触发原行为、`responseResendRules`=响应触发),分别算 want、两组都关才 `clearResendTimers`;`setupRequestHandling` 在重发观察监听④后加**被动 `context.on('response')`** 观察器⑥→ `handleResponseTrigger(resp)`(`resendResponseWant` 门控 → `isResendOrigin(resp.request().headers())` 跳过防递归 → `matchRule(responseResendRules)` 命中 → status/headers 先筛、`triggerNeedsBody` 才 `await resp.text()`(**try/catch 兜 context 关闭竞态**)→ `responseTriggerMet` 通过 → 以「产生该响应的请求」为蓝本调 `scheduleReplayResend`);请求侧观察器④读 `this.resendRules`(已不含响应触发规则)故不重复触发。④`request-rules-store.ts` 新增 `normalizeResponseTrigger`(status 有限 number、headers/bodyJson 复用 `normalizeStringMap`;只要键是对象就返回对象,即便空表示「任意响应都触发」)接入 `normalizeResendRule` + 模板加 inert 示例。**关键点**:Playwright 网络层读响应体**不受页面 CORS 限制**(能读跨域体);但重发仍用 `activePage` 页面内 fetch、需页面存活+宏在跑(配 `pause` 保活),否则跳过。**`main.ts` 零改动**(`resendActive=enabled&&resends.length>0` 门槛已覆盖、`fs.watchFile` 热更新经 `updateRequestRules→applyReplayResend` 自动带入新字段)。**改动仅 `macro-types.ts`/`request-rewrite.ts`/`macro-runner.ts`/`request-rules-store.ts`/`examples`/`scripts/verify-response-trigger.mjs`(新)+`verify-response-trigger-replay.mjs`(新)+`CLAUDE.md`/`开发计划.md`;`request-interceptor.ts`(录制端)/preload/renderer/UI/宏 JSON/旁车/既有各支路零改动**。已验证:`npm run build` + `verify-response-trigger`(纯逻辑 34 断言全过)+ `verify-response-trigger-replay`(**回放端 E2E**:页面发 done+pending 两响应,断言只有满足 status/headers/bodyJson 的 done 触发 1 次重发、pending 被条件门槛挡掉、延时≈800ms、防递归不增长)+ 全套回归(`verify-resend` 请求触发路零回归 / `verify-resend-replace` / `verify-resend-headers` / `verify-response-header-replay` / `verify-hotreload` / `verify-replay-intercept` / `verify-request-rewrite` / `verify-response-rewrite` / `verify-merge`)全过;真实 YouTube 场景(响应触发→发后续请求)待用户登录回放手动验收
  - **〔2026-07-17 再后续:responseTrigger 重构为「捕获-重放」模型 + 删 targetUrl〕** v1 把「监听哪个响应」和「重发哪个请求」绑死在同一顶层 `urlPattern`,想「监听 A 的响应→重发另一个请求 B」只能靠 `targetUrl` 改道。用户要**彻底解耦**。**用户拍板**:①顶层 `urlPattern`=要**捕获并重发的请求本身**(回放中抓到命中它的真实请求,记 url/method/头/体);②`responseTrigger.triggerUrl`(**新增必填**)=监听哪个响应当触发闸门,status/headers/bodyJson 判的是**这个 triggerUrl 的响应**;③`targetUrl` **从整个 `ResendRule` 移除**(请求触发也只打回原 URL);④triggerUrl 缺失→整条规则丢弃(不降级);⑤触发时**尚未捕获**到 urlPattern 请求→跳过+日志(不中断);⑥响应触发**忠实重放捕获请求**(用捕获的 method+url+头+体,set/replaceWithFile/setHeaders 仍叠加,`method` 字段仅请求触发用)。**新语义**:捕获 urlPattern 请求→triggerUrl 响应满足条件时重发捕获的请求(干净实现「监听 A→重发 B」)。**改动**:`macro-types.ts`(`ResendResponseTrigger` 加必填 `triggerUrl`;`ResendRule` 删 `targetUrl`);`request-rewrite.ts`(**删 `resolveResendTarget`**,目标恒为 base.url;`responseTriggerMet` 等纯逻辑不变——triggerUrl 的 URL 匹配在观察器用 `globToRegExp` 做);`macro-runner.ts`(**核心**:加 `resendCaptures: Map<urlPattern,{url,method,headers,body}>` 存最近一次捕获;请求观察器④加「响应触发捕获支」——遍历 `responseResendRules` 命中 urlPattern 即存捕获、不限方法;`handleResponseTrigger` 改为遍历所有响应触发规则、匹配 `triggerUrl`、命中且条件满足→取捕获重发、无捕获记日志跳过;`scheduleReplayResend` 签名由 `(rr,triggerUrl,triggerHeaders,originalBody)` 改吃 `(rr,base:{url,method,headers,body})`、目标恒 base.url;`fireReplayResend` method 类型放宽 string 支持 PUT 等;`clearResendTimers` 加 `resendCaptures.clear()`);`request-interceptor.ts`(录制端同步去 `resolveResendTarget`/`rr.targetUrl` 改用触发请求 URL——保编译,录制端不实现响应触发捕获);`request-rules-store.ts`(`normalizeResponseTrigger` triggerUrl 必填否则返 null、`normalizeResendRule` 有 responseTrigger 但归一化失败→整条返 null、删 targetUrl 收集;模板改)。**限制**:仅回放端;urlPattern 请求须**先于** triggerUrl 响应被捕获;**捕获上传型(File/Blob)请求 body 会为空**(被动 `req.postData()` 对 Blob 返 null),要重发完整字节须配 `replaceWithFile`(url/头仍捕获);捕获取最近一次。**改动仅 `macro-types.ts`/`request-rewrite.ts`/`macro-runner.ts`/`request-interceptor.ts`/`request-rules-store.ts`/`examples`/`scripts/verify-response-trigger-replay.mjs`(重写)+`CLAUDE.md`/`开发计划.md`;preload/renderer/UI/宏 JSON/旁车/既有各支路零改动**。已验证:`npm run build` + `verify-response-trigger`(纯逻辑不变)+ `verify-response-trigger-replay`(**回放端 E2E 重写**:先 `POST /payload` 被捕获,`GET /status?case=done` 响应满足条件才重发捕获的 payload、pending 被门槛挡掉、延时≈800ms、防递归)+ 全套回归(`verify-resend`/`verify-resend-replace`/`verify-resend-headers` 请求触发路零回归、`verify-replay-intercept`/`verify-hotreload`/`verify-response-header-replay`、离线 `verify-request-rewrite`/`verify-response-rewrite`/`verify-merge`、录制端 `verify-request-intercept`/`verify-timeline-record` 证 request-interceptor.ts 未坏)全过
  - **〔2026-07-20 再后续:响应触发「未命中原因」诊断日志 + 捕获日志〕** 排查真实 YouTube 场景(捕获 createvideo、监听 feedback 响应带 `"fractionCompleted":1` 时重发)——只见 `已捕获` 日志、无 `已重发` 也无 `尚未捕获`,而 record 时间线**不记响应体**,无法看出 `bodyContains` 为何没匹配(最可能原文冒号后有空格 `"fractionCompleted": 1`)。**先加了捕获日志**(`macro-runner.ts` 请求观察器捕获 urlPattern 请求时 `已捕获待重发请求 [...] ← POST ...`,仅首次/URL 变化时打,不刷屏)。**再加「未命中原因」诊断**:`request-rewrite.ts` 新增纯函数 `explainResponseTriggerMiss(trigger,status,headers,bodyText) → {signature,message}|null`(条件满足→null;否则逐组给因——status 期望/实际、头不等、bodyContains 缺哪个子串+**去空白比对命中则标「仅空白差异」**+定位子串最长关键字在响应体里截 `…前30…关键字…后40…` 实际片段、bodyJson 路径/期望/实际;`signature` 用稳定要素去重、`message` 含实际片段)。`macro-runner.ts` 的 `handleResponseTrigger` 在 triggerUrl 命中但 `responseTriggerMet` 为 false 时调它,按 `signature` 去重(`resendMissWarned` Set,`clearResendTimers` 清)打 `未命中 [urlPattern] ← <原因>`。**一跑即知 bodyContains 为何没匹配**(空格/取值/读不到体)。仅改 `request-rewrite.ts`(加纯函数)+`macro-runner.ts`(捕获日志+接线+Set)+两个自检脚本;类型/存储/UI/既有各支路零改动。已验证:`npm run build` + `verify-response-trigger`(纯逻辑加 `explainResponseTriggerMiss`:满足→null、空格差异标「仅空白差异」且 signature 有别于真缺失、实际片段含真实值、null/status/bodyJson 各情形)+ `verify-response-trigger-replay`(**E2E** 断言 pending 的 `fractionCompleted:0.5` 响应打出 `未命中 [*/payload*] ← bodyContains 缺 ["fractionCompleted":1];响应体实际: …0.5…`、done 仍正常重发)+ 全套回归(`verify-resend`/`resend-replace`/`resend-headers`/`replay-intercept`/`hotreload`/`response-header-replay` + 离线 `request-rewrite`/`response-rewrite`/`merge`)全过
  - **〔2026-07-17 再后续:responseTrigger 加 `bodyContains`(响应体原文子串条件)〕** `bodyJson` 用点路径=值匹配,但真实响应(YouTube innertube 上传反馈)是**深层嵌套+异构数组**(`continuationContents[0]...contents[0].transferProgressBar.fractionCompleted`),`contents` 异构、`transferProgressBar` 不保证在 `[0]`,写死下标下次响应就错位。**用户拍板加 `bodyContains`**:响应体**原文子串**数组,全部出现才命中(AND、大小写敏感、不解析 JSON、不依赖路径),如 `["\"fractionCompleted\":1"]` 检测 100%。**改动极小**:`macro-types.ts`(`ResendResponseTrigger` 加 `bodyContains?:string[]`);`request-rewrite.ts`(`triggerNeedsBody` 有 bodyJson **或** bodyContains 即读体;`responseTriggerMet` 加 bodyContains 分支——放 bodyJson 前,纯子串更便宜可先短路,`bodyText.includes(sub)` 逐条 AND);`request-rules-store.ts`(`normalizeResponseTrigger` 校验为非空字符串数组、剔空串);examples/模板补字段。**`macro-runner.ts` 零改动**(`handleResponseTrigger` 已调这两个纯函数自动生效)。与 status/headers/bodyJson 一律 **AND**。**取舍**:原文子串精确匹配(要正则/容忍空白后续再加 `bodyRegex`,本次不做);中文子串仅响应体字面 UTF-8(非 `\u` 转义)时匹配,不确定用 ASCII 标记(`"fractionCompleted":1`)最稳。**改动仅 `macro-types.ts`/`request-rewrite.ts`/`request-rules-store.ts`/`examples`/`scripts/verify-response-trigger.mjs`(加断言)+`verify-response-trigger-replay.mjs`(改用嵌套结构+bodyContains)+`CLAUDE.md`/`开发计划.md`;preload/renderer/UI/宏 JSON/旁车/既有各支路零改动**。已验证:`npm run build` + `verify-response-trigger`(纯逻辑加 bodyContains:单/多子串 AND、缺一个/null/中文子串/与 bodyJson·数字下标点路径组合全过)+ `verify-response-trigger-replay`(**E2E 复刻嵌套数组结构**,`bodyContains:['"fractionCompleted":1']` 只命中 done、pending(0.5)被挡、重发捕获的 payload)+ 全套回归(`verify-resend`/`resend-replace`/`resend-headers`/`replay-intercept`/`hotreload`/`response-header-replay` + 离线 `request-rewrite`/`response-rewrite`/`merge`)全过
- 录制/回放两端「响应头条件改写」支路(2026-07-16,在改写/重发/记录之外新增:按响应头字段条件改响应头):需求=配置「响应头 `xx=1` 时把 `cc` 改成 `1`」。**与请求侧改写的本质区别**:现有 rules(改 POST body)/resends(重发)/record(只读记录)全作用在**请求侧或只读观测**,改响应头必须在**响应返回后**介入——这是项目两端第一次真正触碰响应的可写路径。被动记录支路(CDP Network / Playwright `context.on('response')`)只读、改不了头,不是可行接入点。**用户拍板**:两端生效(与 rules/resends 一致共用同一 `request-rules.json`+`enabled` 总开关)、完整动作(多条件 `when` AND/大小写不敏感/缺省无条件 + `setHeaders` 设置覆盖 + `removeHeaders` 删除)。**数据模型**:新增独立 `ResponseHeaderRule{urlPattern,when?,setHeaders?,removeHeaders?}` + `RequestRulesConfig.responseRules?[]`(与 rules/resends **物理分开**——`matchRule` 首命中即返回,混数组会互抢首命中)。**共享纯逻辑 `request-rewrite.ts`**:①`responseConditionMet(headers,rule)`(when 缺省恒真、逐条 `headerValue` 大小写不敏感相等、AND);②`rewriteResponseHeaderRecord`(**回放端** Record 用:复制头、大小写不敏感删 remove∪set 名单键、再写 setHeaders,保证不出现大小写不同重复键;无动作/条件不满足返回 null);③`rewriteResponseHeaderEntries`(**录制端** CDP HeaderEntry[] 用:先拼 Record 判条件,命中则 filter 掉名单项、push 新 set 项,**保留数组形态不破坏合法重复头如多个 set-cookie**)。**录制端 `request-interceptor.ts`(全新机制)**:`applyPatterns` 给响应头规则 URL 加 `requestStage:'Response'` pattern(改写/重发仍 Request)、**签名 key 纳入 `@requestStage`**(同 URL 双阶段去重不误判);`onRequestPaused` 开头按 `params.responseStatusCode/responseHeaders` 分流——响应阶段走新 `handleResponseStage`(命中则 `rewriteResponseHeaderEntries`→`Fetch.continueResponse({requestId,responseCode,responseHeaders})` 回传完整头替换;**响应阶段绝不能用 continueRequest**),请求阶段现有逻辑一字不变;新增 `responseRewriteActive` 字段。**回放端 `macro-runner.ts`(全新机制)**:`rewriteHandler` 在 isResendOrigin 放行后先 `matchRule(responseHeaderRules)`——命中则改走 `handleResponseHeaderRoute`(与 method 无关,GET 也改:若同一 POST 也命中 body 改写规则则 `route.fetch({postData:newBody})` 组合两种改写,拿 `response` 后 `rewriteResponseHeaderRecord`→`route.fulfill({response,headers})`;**fetch 后已消费请求故统一 fulfill,仅 fetch 前出错回退 continue**),无命中走现有 body 改写逻辑;`applyReplayRewrite` 的 `want` 改为 `enabled&&(rules.length||responseHeaderRules.length)`(只配响应头规则也注册 route)、存 `this.responseHeaderRules` 供 handler 热更新;`updateRequestRules` 零改动(已调 applyReplayRewrite)。**`main.ts`**:`buildSessionOptions` 门槛加 `responseRuleActive=enabled&&responseRules.length>0`;录制端 attach、回放期 `fs.watchFile` 零改动(传整份 config,新字段随之流入热更新)。**取舍**:回放端命中响应头规则的请求由浏览器直连改为 Node 侧 route.fetch 代发(有开销,只作用命中 URL,其余仍 continue 零成本);录制端 Fetch 响应阶段暂停命中 URL 的响应(有延迟)。**限制**:未做 UI(编辑 request-rules.json 的 responseRules 段);未覆盖 CDP `continueResponse` 极端流式响应边界(按标准回传完整 responseHeaders 替换)。**改动仅 `macro-types.ts`/`request-rewrite.ts`/`request-rules-store.ts`/`request-interceptor.ts`/`macro-runner.ts`/`main.ts`/`examples`/`scripts/verify-response-rewrite.mjs`(新)+`verify-response-header-replay.mjs`(新);宏 JSON/旁车/preload/renderer/UI/既有 rules·resends·record 三支路零改动**。已验证:`npm run build` + `verify-response-rewrite`(纯逻辑 23/23) + `verify-response-header-replay`(回放端 E2E:日志「已改写响应头 set=cc;remove=x-drop」、页面 #done 出现证客户端收 cc=1 且无 x-drop、/api/data 命中 1) + `verify-request-rewrite`/`verify-replay-intercept`/`verify-hotreload`/`verify-resend`/`verify-merge` 回归全过(录制端 CDP 响应阶段端到端待 Electron 实跑,逻辑与已过 E2E 的回放端对称)
- 回放端新增「请求头条件改写」支路(requestHeaderRules,2026-07-20,承接响应头改写:对称补齐「改原始请求的请求头」):需求=文档里有 responseRules 改响应头,但**没有支路能改「原始在途请求」的请求头**(rules 改 body、resends[].setHeaders 只改**重发副本**头)。**用户拍板**:仅回放端(与 blocks/dumps/bodyReplaces 及 2026-07-16「拦截仅回放端」架构一致);新键 `requestHeaderRules`(区别于已是「请求 body 改写」的 `rules`)。**高度对称复用**:①`macro-types.ts` 加 `RequestHeaderRule{urlPattern,when?,setHeaders?,removeHeaders?}`(与 `ResponseHeaderRule` 逐字同构)+ `RequestRulesConfig.requestHeaderRules?`。②`request-rewrite.ts` 加 `requestConditionMet`(=`headersAllEqual(headers,rule.when)` 薄封装)+ `rewriteRequestHeaderRecord`(照抄 `rewriteResponseHeaderRecord`,**唯一偏离=输出把 `content-length` 并入 dropLower 剥离**:同一请求 body 可能被 rules 改写,旧长度会与新 body 不符,交网络栈重算);URL 匹配复用泛型 `matchRule<T extends{urlPattern}>`。③`macro-runner.ts`(**核心:route handler 三组合协同**):加实例字段 `requestHeaderRules`;`applyReplayRewrite` 拷贝 + `want` 并入 + 启用日志计数;route handler 在 `isResendOrigin` 放行①、block abort② 之后、respRule③ 之前算一次 `newReqHeaders=reqHdrRule?rewriteRequestHeaderRecord(request.headers(),reqHdrRule):null`,用小工具 `withHdr(opts)=>newReqHeaders!==null?{...opts,headers:newReqHeaders}:opts` 合并进各放行路径——(a)仅请求头/非 POST→`continue(withHdr())`、(b)请求头+body→`continue(withHdr({postData:newBody}))` **一次合并**、(c)请求头+响应头→`handleResponseHeaderRoute` 加第 4 参 `reqHeaders` 走 `route.fetch({headers})`;`newReqHeaders=null` 时 `withHdr` 退化为不带 headers 原调用;**`isResendOrigin` 短路仍最前→重发请求头零改写、防递归标记不破**。④`request-rules-store.ts` 加 `normalizeRequestHeaderRule`(逐字复制 `normalizeResponseHeaderRule` 换类型名,复用 `normalizeStringMap`)+ `loadRequestRules` 一路 + 模板 inert 示例。⑤`main.ts` 的 `buildSessionOptions` 加 `requestHeaderActive` 门槛 + OR 下发 + logInfo;`fs.watchFile` 热更新整份透传无需改。**限制**:仅回放端;**cookie/host 改不了**(浏览器管理,`request.headers()` 不含 cookie、会自动补回不误删登录态;改 cookie 用登录态注入);when 读改写前原始请求头(不含 cookie);勿用 removeHeaders 清空全部头;无 UI(编辑 request-rules.json)。**改动仅 `macro-types.ts`/`request-rewrite.ts`/`macro-runner.ts`/`request-rules-store.ts`/`main.ts`/`examples`/`scripts/verify-request-header-replay.mjs`(新)+`verify-response-rewrite.mjs`(加断言)+`拦截器规则配置手册.html`/`CLAUDE.md`/`开发计划.md`;`request-interceptor.ts`(录制端)/preload/renderer/UI/宏 JSON/旁车/既有各支路零改动**。已验证:`npm run build` + `verify-response-rewrite`(纯逻辑加 `requestConditionMet`/`rewriteRequestHeaderRecord`:set/remove/大小写/**content-length 剥离**/when 门槛/无动作返 null 全过)+ `verify-request-header-replay`(**回放端 E2E**:组合 a set/remove/保留 + when 门槛不改 + 组合 b body/头一次 continue 合并且 body 完整证 content-length 剥离,11 断言全过)+ 全套回归(`verify-response-header-replay` 组合 c 宿主签名变 / `verify-replay-intercept` 组合 b 合并点 / `verify-block-intercept` abort 仍在头改写前 / `verify-hotreload` want 变 / `verify-resend`/`verify-resend-headers` isResendOrigin 仍最前 / `verify-request-rewrite`/`verify-response-trigger`/`verify-merge`)全过;真实站点(换 Authorization 等)待用户登录回放手动验收
- 拦截模块新增「真拦截(硬阻断)」+ 生效范围收窄到回放阶段(2026-07-16,承接改写/重发/响应头/记录四支路):需求两条——①**真拦截**:配置命中的请求**直接阻断、不让其继续发出**(用户原话「统统拦截不让它们继续请求」= 硬阻断);②**生效范围收窄**:整个拦截模块只在**回放阶段**生效,录制时不拦截/不改写(抓真实未改动流量,也不与用户手开 DevTools 抢 debugger)。**与既有四支路的本质区别**:rules(改 body)/resends(重发)/responseRules(改响应头)/record(只读)全是「放行/改/记」,真拦截是**项目首个「不放行」分支**——回放端首次用 Playwright `route.abort()`(全项目此前从未用过 `route.abort`/`Fetch.failRequest`)。**用户拍板**:硬阻断(非伪响应);blocks 是与 rules/resends/responseRules **物理分开的独立数组**(`matchRule` 首命中即返回,混数组会互抢);整个模块**摘除录制端接线**(不是加开关门控)。**数据模型 `macro-types.ts`**:新增 `BlockRule{urlPattern, method?}`(method 可选、大小写不敏感、缺省拦所有方法)+ `RequestRulesConfig.blocks?[]`。**存储 `request-rules-store.ts`**:新增 `normalizeBlockRule`(照 `normalizeResponseHeaderRule` 骨架:校 urlPattern、method 为非空字符串则写);`loadRequestRules` 加 blocks 归一化 + 条件展开;`templateConfig` 加 inert 示例。**回放端真拦截 `macro-runner.ts`(核心,复用现有 route 机制不新建 handler)**:加实例字段 `blockRules`;`rewriteHandler` 在 **`isResendOrigin` 守卫之后、响应头/body 分流之前**插 `matchRule(this.blockRules,url)` 命中(且 method 匹配)则 `route.abort()` return(放 isResendOrigin 后→工具自己的重发请求不被自己阻断;放改写前→命中即拦最干净);`applyReplayRewrite` 刷新 `this.blockRules=cfg.blocks??[]` 且 `want` 并入 `blockRules.length>0`(只配 block 时也注册 route);`updateRequestRules` 零改动(已调 applyReplayRewrite,block 随之热更新)。**收窄到回放 `main.ts`**:`did-attach-webview` **删除** `new RequestInterceptor().attach()` 与 destroyed 里的 `interceptor.detach()`,**保留** `recordingWebContents` 赋值/置空(回放前读 localStorage 用)与 `setWindowOpenHandler`(新标签页归一);删无用的 `RequestInterceptor` import(**类文件本身保留**供自检脚本直接实例化);`buildSessionOptions` 门槛加 `blockActive=enabled&&blocks.length>0`(否则纯 block 配置漏下发)。**关键取舍**:`RequestInterceptor`(录制端 CDP 拦截器)类与录制端自检脚本(`verify-request-intercept`/`_intercept-e2e.cjs`、`verify-timeline-record`/`_timeline-record-e2e.cjs`)**保留不动**——它们自带测试 harness 直接 `require('dist/main/request-interceptor.js')` 实例化、**不经 main.ts**,故摘除 main.ts 接线不破坏它们(实测两脚本仍全过);真拦截因整模块收窄到回放,**只在回放端实现**,录制端 `request-interceptor.ts` 不加 block(加了也是死代码)。**限制**:录制阶段该配置完全不生效(收窄的直接后果,连录制端 CDP 时间线记录也随之停);未做 UI(编辑 request-rules.json 的 blocks 段);跨域/CSP 由浏览器与 route 处理,abort 走默认 errorCode='failed'(net::ERR_FAILED)。**改动仅 `macro-types.ts`/`request-rules-store.ts`/`macro-runner.ts`/`main.ts`/`examples`/`scripts/verify-block-intercept.mjs`(新)+`CLAUDE.md`;`request-rewrite.ts`(纯逻辑,`matchRule<T extends{urlPattern}>` 泛型直接吃 BlockRule)/`request-interceptor.ts`/preload/renderer/UI/宏 JSON/旁车/既有四支路零改动**。已验证:`npm run build` + `verify-block-intercept`(**回放端 E2E**:日志「已阻断 [GET .../blocked]」、服务端收到 /allowed 但**从未**收到 /blocked、result.ok)+ 全套回归 11 项全过(`verify-request-rewrite`/`verify-response-rewrite`/`verify-replay-intercept`/`verify-hotreload`/`verify-resend`/`verify-response-header-replay`/`verify-timeline-replay`/`verify-merge` + 录制端 `verify-request-intercept`/`verify-timeline-record` 证类未坏)
- 回放端新增「请求体落盘(dump 成 mp4)」支路(2026-07-16,承接改写/重发/响应头/真拦截/记录五支路):需求=回放时把命中某接口的 HTTP 请求体**从第一字节到最后一字节的全部内容**完整获取并写成一个 `.mp4` 文件,可配置(承接 `YouTube上传流程分析.html`——把视频上传请求体字节落成 mp4)。**与既有五支路的本质区别**:record(只读记录)用 `req.postData()`(返回 **string**,按 UTF-8 解码)——对**二进制**视频体会损坏字节;dump 必须用 Playwright **`req.postDataBuffer()`(原始字节 `Buffer`)** + `fs.writeFileSync` 二进制写入,这是唯一核心正确性属性,遵守「禁止截断」铁律。**用户拍板**:①触发=复用顶层 `enabled` 总开关(与 resends/blocks/responseRules 一致);②输出=新建专用 `dumps/` 目录(与 downloads/timelines 同级)安静落盘、日志打印路径、不弹资源管理器。**范围=仅回放端**(符合最新架构:拦截模块已收窄到仅回放阶段,录制端 `request-interceptor.ts` 不动)。**数据模型 `macro-types.ts`**:新增 `DumpRule{urlPattern, method?, extension?}`(method 缺省=落所有方法,因上传常是 PUT/POST,区别于改写/重发的仅 POST;extension 缺省 'mp4',容忍带/不带前导点)+ `RequestRulesConfig.dumps?[]`(与 rules/resends/responseRules/blocks **物理分开**——matchRule 首命中即返回,混数组会互抢)。**存储 `request-rules-store.ts`**:`normalizeDumpRule`(照 `normalizeBlockRule` 骨架:校 urlPattern、method/extension 非空 string 才写);`loadRequestRules` 加 dumps 归一化 + 条件展开;`templateConfig` 加 inert 示例。**回放端 `macro-runner.ts`(核心,新增被动监听,不进 route 改写链路)**:①实例字段 `dumpsDir`(构造第 7 参,缺省 `errorDir/../dumps`)/`dumpRules`/`dumpWant`/`dumpSeq`/`dumpsReady`;②`setupRequestHandling` 在重发监听④后加**被动 `context.on('request')` 监听⑤**:`dumpWant` 且非 `isResendOrigin`(跳过工具自发重发)且 `matchRule(dumpRules)` 命中(可选 method 匹配)且 `postDataBuffer()` 非空 → `writeDumpFile`;③`applyReplayDump(cfg)`(仿 applyReplayResend:`want=enabled&&dumps.length>0`,切标志+日志);④`writeDumpFile(rule,buf,url)`:目录懒建(`dumpsReady`)+ `dump-${Date.now()}-${dumpSeq}.${ext}` 防撞名 + `writeFileSync(file,buf)` 完整二进制 + 日志字节数/路径,写失败只记不抛;⑤`updateRequestRules` 末尾加 `applyReplayDump`(热更新)、初始 apply 段加 `applyReplayDump(initial)`。**`main.ts`**:`dumpsDir` 常量入 `ensureDirs`、`new MacroRunner(...)` 第 7 参传入;`buildSessionOptions` 门槛加 `dumpActive=enabled&&dumps.length>0`(纯 dump 配置也整份下发)。**取舍/限制**:①被动监听拿改写前原始 body(与 record 一致);②分块/resumable 上传会 dump 出多个文件(每 chunk 一个)需自行拼接,但 YouTube 实测单次 PUT 传完;③超大 body 由 postDataBuffer 一次性读入内存(不截断);④Playwright 对个别流式/未缓冲上传可能返回 null 则静默跳过;⑤无 UI(编辑 request-rules.json 的 dumps 段)。**改动仅 `macro-types.ts`/`request-rules-store.ts`/`macro-runner.ts`/`main.ts`/`.gitignore`/`examples`/`scripts/verify-dump-body.mjs`(新)+`CLAUDE.md`;`request-rewrite.ts`(matchRule 泛型直接吃 DumpRule)/`request-interceptor.ts`/preload/renderer/UI/宏 JSON/旁车/既有五支路零改动**。已验证:`npm run build` + `verify-dump-body`(**回放端 E2E**:页面发 768 字节全字节值二进制,断言恰落 1 个 `dump-*.mp4` 且与原始 Buffer 逐字节相等、反证 string 路径会损坏)+ 回归全过(离线 `verify-request-rewrite`/`verify-response-rewrite`/`verify-timeline-recorder`/`verify-merge` + E2E `verify-block-intercept`/`verify-replay-intercept`/`verify-resend`/`verify-response-header-replay`/`verify-timeline-replay`/`verify-hotreload`)
  - **〔2026-07-16 后续:升级为 CDP Fetch,抓 File/Blob 上传体〕** 实测上一版对 YouTube 视频上传**无效**:配 `dumps:[{urlPattern:"*upload.youtube.com*"}]` 回放,只落盘了 start 请求的 78 字节 JSON,28MB 视频字节上传**完全没落盘**。**探针实证根因**(跑两个诊断脚本后删):不是 urlPattern/method/大小问题,而是 **body 类型**——Playwright 的 `postDataBuffer()` 对 **File/Blob 类型**请求体一律返回 `null`(1MB Blob 就 null,32MB Uint8Array 却能拿到);YouTube 传的是用户本地选的 **File(Blob 子类)**,走 Chromium 内部 blob store 引用传输、不进 CDP post data,故 Playwright 高层 API 读不到。**但底层 CDP 能拿到**:探针用 `context.newCDPSession(page)`+`Fetch.enable`+`Fetch.requestPaused`,从事件自带的 **`request.postDataEntries`(base64 分块)** 对 Blob 逐字节保真重组。**升级方案(仅改 `macro-runner.ts` + `verify-dump-body.mjs`)**:dump 支路**从 Playwright 被动 `postDataBuffer` 改为 per-page CDP Fetch 拦截**——①import `type CDPSession`;②新增字段 `dumpCdpSessions:Map<Page,CDPSession>`/`dumpPages:Set<Page>`;③**移除**被动监听⑤;④新增 `attachDumpCdp(page)`(`ctx.newCDPSession(page)`→挂 `Fetch.requestPaused`→`Fetch.enable({patterns:dumpFetchPatterns()})`,幂等、失败记告警不致命)、`onDumpRequestPaused`(命中 dump 规则→`reassemblePostData`→`writeDumpFile`,**无论如何最后 `Fetch.continueRequest` 放行**)、`reassemblePostData`(优先 `postDataEntries[].bytes` base64 `Buffer.concat`、回退事件 `postData` 字符串)、`refreshDumpCdp`(开→对 `dumpPages` 补挂/重下发 patterns、关→`detachAllDumpCdp`)、`detachAllDumpCdp`(`Fetch.disable`+`detach`,全 try/catch);⑤`applyReplayDump` 尾部 `void refreshDumpCdp()`(热更新);⑥run() 初始 page(L336)与 `context.on('page')` 弹窗各 `dumpPages.add`+`attachDumpCdp`(**CDP session 是 per-page,弹窗需各自 attach**);⑦`cancel()`/finally 各加 `detachAllDumpCdp()`(须早于 context 关闭,同重发定时器)。`writeDumpFile` 保留复用。**关键取舍**:①**完全替换**(非双路)——CDP `postDataEntries` 覆盖 bytes/blob/字符串所有类型,移除 postDataBuffer 避免双重落盘;CDP attach 失败则该页不落盘(记告警)。②**录制端不能照抄**:其抓 body 用的 `Fetch.getRequestPostData` 是**非标准 CDP 方法**(探针实测"wasn't found",录制端一直静默走 catch 回退到 `request.postData`、对 Blob 本就不可靠),记录支路用 `Network.getRequestPostData`(返回字符串、二进制不保真);**录制端从未用 `postDataEntries`**,正确路径是 `Fetch.requestPaused` 事件自带的 `postDataEntries`,不调用任何 getRequestPostData。③**route 共存不冲突**(实测):dump(独立 CDP Fetch)与改写/真拦截(Playwright route,底层亦 CDP Fetch)命中同一 URL 时两者各自拦截、都生效、不 hang(专门跑临时探针验证);dump 抓到的 body 时序上可能改写前或后。④**超大 body**:postDataEntries 经 CDP 传输(28MB→约 37MB base64)有内存开销但可接受;极端超大 CDP 未 inline 致 entries 空则该请求落不到(待真实验证)。**改动仅 `src/core/macro-runner.ts` + `scripts/verify-dump-body.mjs`(改为 **Blob** 验证:页面发 `new Blob(...)` body,断言落盘与原始逐字节相等——旧 postDataBuffer 实现下 Blob→null 必失败,新 CDP 实现通过) + `CLAUDE.md`/`开发计划.md`;`macro-types.ts`/`request-rules-store.ts`/`main.ts`/`request-interceptor.ts`(录制端)/preload/renderer/UI/宏 JSON/旁车零改动**。已验证:`npm run build` + `verify-dump-body`(**Blob + Uint8Array 两用例逐字节相等**)+ route 共存探针(改写生效且 dump 落盘、无 hang)+ 全套回归 12 项(`verify-cancel`/`verify-hotreload`/`verify-resend`/`verify-block-intercept`/`verify-replay-intercept`/`verify-response-header-replay`/`verify-timeline-replay`/`verify-popup-wait` + 离线 `verify-request-rewrite`/`verify-response-rewrite`/`verify-merge`)全过。**真实 YouTube 28MB 上传体待用户登录回放手动验收**
- 回放端新增「请求体整体替换(拦截替换)」支路(2026-07-17,承接 dump——能读就能写):需求=回放时拦截命中某 URL 的请求,把它的请求体**整个替换成一个本地文件的字节**再放行发出(用户拍板:整体替换、来源=本地文件)。**现有 rules 改写对 Blob 无效**——它是文本字段级(JSON/form 的 set/append/remove),回放端 `request.postData()` 对 File/Blob 返回 `null`、在 rewriteHandler 里 `!original` 就静默放行,`rewritePostBody` 压根没调。要在拦截时替换 body(含 Blob),唯一路径是 **CDP Fetch**:`Fetch.requestPaused` 暂停 → `Fetch.continueRequest({postData: 文件base64})` 整体替换(Content-Length 网络栈自动重算,同录制端 `request-interceptor.ts:442`)。**dump 已在回放端建好 per-page CDP Fetch 拦截,替换支路直接搭同一套**(读旧、发新是一对)。**数据模型 `macro-types.ts`**:新增 `BodyReplaceRule{urlPattern, method?, replaceWithFile}` + `RequestRulesConfig.bodyReplaces?[]`(与 rules/dumps 等物理分开)。**存储 `request-rules-store.ts`**:`normalizeBodyReplaceRule`(**replaceWithFile 必须非空 string 否则丢弃**)+ loadRequestRules 归一化 + templateConfig inert 示例。**回放端 `macro-runner.ts`(主改,复用 dump 的 CDP 基础设施)**:①字段 `replaceRules`/`replaceWant`;②`dumpFetchPatterns()` 泛化为返回 **dump ∪ replace 规则 URL 并集**(去重)、新增 `cdpFetchWant()=dumpWant||replaceWant`,`attachDumpCdp`/`refreshDumpCdp` 门槛改用它(CDP 会话 dump/替换共用);③**`onDumpRequestPaused` 加替换分支**:先按 dump 落盘原始 body(读旧)→ `matchRule(replaceRules)` 命中且 method 匹配 → `fs.readFileSync(replaceWithFile)` → `continueRequest({postData: buf.toString('base64')})` + 日志 → **return**;读文件失败记告警落到末尾原样放行——**每路径恰好放行一次**;④`applyReplayBodyReplace(cfg)`(仿 applyReplayDump,尾 `void refreshDumpCdp()`),`setupRequestHandling` 初始 apply 段 + `updateRequestRules` 各调一次(热更新);run() page attach、cancel/finally detach 复用不变(门槛已含 replaceWant)。**`main.ts`**:`buildSessionOptions` 门槛加 `replaceActive=enabled&&bodyReplaces.length>0`。**关键点**:①**Content-Length 已验证**:替换成不同大小文件(自检 256→600 字节)CDP 自动重算 content-length=600,无需显式传 headers;②**绝对路径**:replaceWithFile 绝对路径(core 层不知 dataRoot),文件每次命中读(内容最新);③dump 与 replace 同一请求可同时命中(dump 落原始、replace 发新);④仅回放端。**改动仅 `macro-types.ts`/`request-rules-store.ts`/`macro-runner.ts`/`main.ts`/`examples`/`scripts/verify-body-replace.mjs`(新)+`CLAUDE.md`/`开发计划.md`;`request-rewrite.ts`(matchRule 泛型吃 BodyReplaceRule)/`request-interceptor.ts`(录制端)/preload/renderer/UI/宏 JSON/旁车/既有六支路零改动**。已验证:`npm run build` + `verify-body-replace`(**回放端 E2E**:页面发 256 字节 Blob 上传体,被替换文件 B(600 字节)整体替换,服务端实收 == B 逐字节相等、!= A、content-length=600)+ dump 未回归(`verify-dump-body` Blob+Uint8Array 逐字节相等)+ 全套回归 10 项(`verify-hotreload`/`verify-cancel`/`verify-block-intercept`/`verify-replay-intercept`/`verify-resend`/`verify-response-header-replay`/`verify-timeline-replay`/`verify-popup-wait` + 离线 `verify-request-rewrite`/`verify-response-rewrite`/`verify-merge`)全过。**真实 YouTube 换视频待用户登录回放手动验收**
- 宏库面板:批量列出 + 逐个/批量后台运行 + 一键载入(2026-07-07,把「单加载·单运行」升级为可管理的宏库):原本 `加载宏` 只弹**单文件**对话框、`运行宏` 只跑当前编辑区内存宏(`buildMacro()`),宏一多不好挑着跑。**核心=一个侧栏「📚 宏库」面板(基础视图即可见,非高级)+ 两个只读 IPC,不改回放引擎/宏 JSON/旁车结构**。①**存储层**——`macro-store.ts` 加 `listMacros(dir)`:读目录所有 `*.json`、**排除 `*.captures.json` 旁车**、逐个 `JSON.parse` 取 `name`(缺省用文件名)与 `steps.length`、损坏跳过、按 `mtimeMs` 倒序;类型 `MacroSummary{filePath,name,stepCount,modifiedMs}` 放 `macro-types.ts`。②**主进程 IPC**(`main.ts`,均只读)——`list-macros`→`listMacros(macrosDir)`;`read-macro(filePath)`**无对话框**版 load(复用 `loadMacro`+`loadMacroCaptures` 返回 `{macro,captures,filePath}`,失败返 null),供列表项「打开」/「后台运行」取宏对象。`preload` 暴露 `listMacros`/`readMacro`。③**渲染进程**(`renderer.ts`+`index.html`)——`#macro-lib-panel`(珊瑚 `--accent` 色带,置于 `#steps-panel` 前),`renderMacroLibrary()` 仿 `loadPlugins()` 动态渲染每项 `.macro-item`=复选框(`dataset.filePath`)+ 名称/「N 步」+「打开」+「运行」两按钮。**关键设计**:(a)**载入逻辑抽成 `applyLoadedMacro(loaded)`**——原 `loadBtn` 内联体(回显名称/步骤/旁车/extract/插件+追踪 `currentMacroPath`+goto 打开)抽出,`loadBtn`(弹框)与列表项「打开」(`readMacro`)共用;(b)**列表项「运行」=后台直接跑不动编辑区**:`readMacro`→`runMacro(macro)`(完全复用 `setBusy`/`onMacroRunStarted`→`activeRunId`/暂停/停止),结果走 **`reportRunResult(result,{fromEditor:false})`**;(c)**`reportRunResult` 加 `opts.fromEditor`(默认 true)**——`false` 时**跳过 `backfillRecordedUrls`**(不写编辑区 `steps`),这是「不动编辑区」的关键一处(其余数据行/下载/错误/`lastRows` 照常);(d)**批量顺序运行**`#macro-lib-run-selected` 收集勾选项 `for` 循环依次 `readMacro`→`runMacro`→`reportRunResult(…,{fromEditor:false})`,`[k/N]` 进度 + 末尾「成功 X 失败 Y」汇总,遇 `cancelled` 中止后续;(e)**忙态协调**——`setBusy`/`setRecordingUI` 各加 `setMacroLibButtonsDisabled(…)`(运行/录制中禁用刷新·运行选中·各项按钮,防叠跑);(f)**刷新**`#macro-lib-refresh`→`renderMacroLibrary()`,并在**手动保存成功后**顺带刷一次让新宏即时出现;`init()` 的 `Promise.allSettled` 加 `renderMacroLibrary()` 启动即扫描。**限制**:仅列 `macros/` 目录(不含 examples);仅单跑(后台运行也占用同一回放,「停止回放」对它同样生效)。**改动**:`macro-types.ts`/`macro-store.ts`/`main.ts`/`preload.ts`/`renderer.ts`/`index.html`,**core 回放引擎/宏 JSON/旁车结构零改动**。已验证:`npm run build` + `verify-captures`/`verify-merge`/`verify-cancel`/`verify-pagination`(30 行)回归通过 + `listMacros` 实跑 `macros/` 返 8 项(排除 2 个 `.captures.json` 旁车、按时间倒序、名称步数准确)
- 应用图标(2026-07-03,与主页 🐟「二爪鱼」品牌一致):`assets/icon.png`(256)+`assets/icon.ico`(16/32/48/64/128/256 多尺寸)由 `scripts/make-icon.mjs` 生成——**纯 Node 内置能力**(`zlib` 编码 PNG + 手工封装 ICO,超采样抗锯齿),环境无 ImageMagick/sharp 也能出图;图案=海洋青渐变圆角底 + 白色小鱼 + 珊瑚色鱼尾/背鳍/腹鳍 + 气泡。接入两处:①`main.ts` 的 `BrowserWindow.icon = path.join(projectRoot,'assets','icon.ico')`(运行时窗口/任务栏图标;打包态 `projectRoot` 解析到 asar 根,`assets/` 随包);②`package.json` 的 `build.win.icon = "assets/icon.ico"`(electron-builder 出 EXE/NSIS 安装包图标)。改图标改 `make-icon.mjs` 里的几何/颜色常量后重跑即可
- 选择器上下文旁车实时持久化 + 批量 AI 校正(2026-07-03):原旁车只在手动「保存宏」时随宏落盘,加载旧宏后重选/校正选择器不点保存就丢。**①实时持久化**——`renderer.ts` 加 `currentMacroPath`(主进程 `load-macro` 回传 `filePath`、手动 `save-macro` 成功后各记住;新录未存的宏无路径故不自动保存),复用「所有 `steps` 改动都紧跟 `renderSteps()`」这一既有统一切入点(同 undo/redo 记账),在 `renderSteps()` 末尾调 `scheduleAutosave()`(防抖 600ms)→ `flushAutosave()` 用 `macroSignature()=JSON.stringify([buildMacro(),buildCaptures()])` 签名去重(挡掉滚动折叠等非改动型渲染的无谓写盘;提取规则 JSON 非法时 `try/catch` 静默跳过不刷屏)→ 经**无对话框**新 IPC `persist-macro`(`main.ts`,直接 `saveMacro`+`saveMacroCaptures` 写入传入路径,复用既有存储层)把**宏 + 旁车一起**写回。**必须宏+旁车一起写**:旁车靠「下标+type+selector」与磁盘宏对齐(`relinkCaptures`)、且选择器改动本身在宏本体里,只存旁车会错位失效、改动也不落盘。`seedPersistedSig()` 在 load(须在 extract/插件回显后)/手动 save/开开关后置基准,防加载后立刻写一遍相同内容。**②开关**——`index.html` 工具栏 `#autosave-toggle`(默认 checked,存 `localStorage:macro.autosave`;关掉可临时改宏不覆盖原文件,`setBusy`/`setRecordingUI` 不禁用它),`autosaveEnabled()`+`currentMacroPath` 双闸门。**③批量校正**——`#ai-fix-all`「🤖 校正全部选择器」加回工具栏(见下条「AI 校正选择器」历史:曾因基于实时 DOM 多「跳过」移除,现旁车持久化后离线批量可行),`fixAllSelectors` 收集所有带 selector 步骤、复用单步 `fixStepSelector`(内部自动分流 `fixWithCapture` 离线 / `fixWithLiveDom` 实时),进度 `[k/N]` + 汇总「校正 X 跳过 Y 未成功 Z」,`setFixBusy` 切忙态,录制/回放中禁用;各步校正成功 `renderSteps()`→自动触发落盘。接线:`TOOLBAR_ACTIONS` 加 `'ai-fix-all'`、`MAX_VISIBLE_ACTIONS` 6→7(export 仍进「更多」);`preload.persistMacro` + renderer 侧 `ElectronAPI` 接口 `loadMacro` 返回加 `filePath`。**改动仅 `main.ts`/`preload.ts`/`renderer.ts`/`index.html`,core/旁车结构/宏 JSON 格式不动**。已验证:`npm run build` + `verify-captures`/`verify-merge`/`verify-pagination`(30 行)回归通过
- 步骤按录制来源 URL 分组显示 + 拖拽限本组(2026-07-06,纯前端 `renderer.ts`+`index.html`,`StepFlags` 加一个可选字段):步骤常录自不同页面(点链接跳转/翻页/进详情页),`#steps` 平铺看不出归属。**①数据**——`macro-types.ts` 的 `StepFlags` 加 `recordedUrl?:string`(交集字段,同 `pagination`,**存进宏 JSON、回放层按 type 分发忽略之、不破 `never` 穷尽检查**);`renderer.ts` 的 `addStep` 对**非 goto** 录制步骤用既有 `safeGetUrl()`(取录制 webview 当前 URL)打戳(goto 自带 `url`;点链接跳转的 click 在 capture 阶段先录,此刻 URL 仍是源页面=正确归属)。手动 splice 插入的 pause/wait 等不打戳,靠渲染期向前填充继承邻居。**②派生**——`computeStepUrls()` 与 steps 同序:goto.url 与 recordedUrl 都更新"当前 URL",其余继承(旧宏无戳→退化为按 goto 边界分组)。**③渲染分块**——`renderSteps()` 按连续同 URL 分成 `.url-group`(`createUrlGroupHead`:折叠箭头 ▾/▸ + `friendlyUrl`=域名+路径截断 48 + `title` 悬停完整 URL + 「N 步」计数),body 内再跑既有连续滚动折叠——**原渲染循环抽成 `renderStepRange(container,start,end)`**(滚动折叠扫描上限 `end` 不跨组),`renderSteps` 只做 URL 分段驱动。折叠态 `collapsedUrlGroups`(`Set<string>` 按 URL 字符串记忆,**默认展开**=不在集合;与 scroll 组的"默认折叠 expandedScrollGroups"相反语义;`load-macro` 处 `.clear()` 重置)。**④拖拽限本组不出界**(用户要求)——模块级缓存 `let stepUrls`(每次 render 刷新、与 steps 对齐),`createStepLine` 的 `dragover`/`drop` 各加"同组"闸门:`stepUrls[dragFromIndex]!==stepUrls[i]` 时 dragover 设 `dropEffect='none'` 不画指示线、drop 直接 return,**步骤只能在本 URL 组内上下重排、拖到别组鼠标显禁止且松手不生效**。**保持不变**:step-line 文本/徽标/右键菜单/拖拽事件全绑真实下标 i;`clearDropIndicators` 用 `stepsEl.querySelectorAll` 递归找嵌套 step-line 仍有效;`#steps:empty` 空态仍成立(无步骤=无 `.url-group`);undo·autosave 机制零改动(recordedUrl 天然进 buildMacro→steps 快照/签名,折叠切换 renderSteps 但签名不变故不触发无谓写盘)。**已知取舍**:拖拽被限在本组(要挪到别页需重录);折叠按 URL 记忆,同 URL 出现两段会联动;仅新录制有 recordedUrl,旧宏走 goto 边界兜底(点链接跳转段并入前一 goto 组)、重录即精确。CSS `.url-group`/`.url-group-head`/`.url-group-body` 复用设计令牌镜像 scroll-group。**改动仅 `macro-types.ts`(加一字段)/`renderer.ts`/`index.html`,core 回放/preload/主进程/旁车结构/宏 JSON 其它格式全不动**。已验证:`npm run build` + `verify-captures`/`verify-merge`/`verify-pagination`(30 行)回归通过
- 步骤列表「人话」化(2026-07-15,面向小白;纯前端 `renderer.ts`+`index.html`,core 回放/宏 JSON/step 数据结构/preload/主进程零改动,新旧宏兼容):原 `describeStep`(renderer.ts)对 `goto/click/fill/press` 输出**英文动词 + 原始 CSS/xpath 选择器**(`click a.next>a`、`fill input#kw = "x"`),小白看不懂。**根治=用录制时已存进 step 的语义指纹**(`fingerprint.text/ariaLabel/tag`——如"下一页""登录")+ `value`/`url` 生成中文短语,**渲染时这些字段本就在 step 上、只是原来没用**。①**新增纯文案函数**(renderer.ts,`describeStep` 附近):`stepFingerprint`(安全取指纹对象)/`elementKind`(a→链接·button→按钮·input/textarea/select→输入框·其余→元素)/`elementLabel`(`fp.text`优先→`ariaLabel`→拼`「登录」按钮`;都无→兜底`一个链接`,截断 24 字)/`friendlyKey`(Enter→回车 等)/`stepIcon`(🌐🖱️⌨️⏳⏸️⬇️↕️)/`stepSelector`(原始选择器,goto 为 url,供灰字 span)/`stepRawDetail`(如 `click a.next>a`,供 title 悬停)/`stepHelp`(仅 waitForSelector/waitForClickable/wait-for-load/scroll-bottom/pause 返回一句话解释)。②**改写 `describeStep`**→输出人话:`打开网页 {friendlyUrl}`/`点击「下一页」链接`/`输入「张三」`(有 ariaLabel→`在「用户名」框输入…`)/`按下 回车 键`/`等待「用户列表」出现`/`暂停,等待人工操作`,**不含选择器**;复用既有 `friendlyUrl`(函数声明提升,`describeStep` 在其前调用无碍)。③**重构 `createStepLine`**:第 402 行单一 `div.textContent` 拆成 `序号 span.step-index + 图标 span.step-icon + 人话 span.step-text + (难懂类型)ⓘ span.step-help[title=解释] + (有 selector)span.step-selector`;**徽标(翻页/暂停)/右键菜单/`draggable`/拖拽事件逻辑一字不动**(仅文本部分从 textContent 改多 span),`renderStepRange` 的连续滚动折叠组首行/子项仍调 `createStepLine`、scroll 无 selector 故不加灰字 span、`scroll-group-toggle`(float:right)照常 append 零冲突。④**选择器隐藏+悬停+高级可见**(用户拍板):`div.title=stepRawDetail`(悬停显示原始选择器供排错);`.step-selector{display:none}` 默认隐藏,**复用既有高级模式**(`#steps` 在 `.sidebar` 内、`setupAdvancedMode` 把 `.advanced` 挂 `.sidebar` 上)→ `.sidebar.advanced .step-selector{display:inline}` 勾「✨ 高级」才现灰字。⑤**难懂类型加 ⓘ 悬停说明**(等待出现/可点击/滚动到底部/暂停各一句)。**边界**:旧宏(fb.json)无指纹→兜底`点击一个按钮/链接/元素`,选择器仍可悬停/高级查看;`xpath=` 前缀只进 title/灰字、人话不含;撤销/自动保存/URL 分组/徽标均只依赖既有数据与 class 故零回归;CSS 灰字/ⓘ 用令牌 `--muted`/`--font-mono`。已验证:`npm run build` + 一次性数据核对脚本(逐字复制纯函数)跑 5 个真实宏 / 67 步全部人话、**零选择器泄露、旧宏兜底正确**(脚本跑完即删,未入库)
- 点击步骤在页面上高亮对应元素(2026-07-15,面向小白;承接「人话化」;纯前端 `renderer.ts`+`index.html`,preload/主进程/core/宏 JSON/旁车零改动,新旧宏兼容):`点击一个元素`(无文字 svg)/`输入「M」`这类人话到头仍看不出是页面上哪个元素,故加「**左键点步骤行 → 在录制 `<webview>` 里高亮该元素闪一下**」。**核心=复用** `locateAndSnapshot`(renderer.ts:2496)的定位内核(selector 的 `xpath=`/CSS 分诊 + fingerprint aria/text/href 三路 + `vis`/`uniqVisible` 可见性去重),只把结尾从「打 `data-macro-fix` 标记」换成高亮收尾。①**新增 `highlightInWebview(selector,fingerprint)`**:走 `webview.executeJavaScript` 注入 IIFE(内核自带一份副本、**不改** `locateAndSnapshot` 本体以免动 AI 校正链路),命中后注入(幂等 `id=__macro_hl_style__`)珊瑚 `#ff7a59` outline+半透明底样式(**换色以区别拾取器蓝 `#2f6df6`**)、`classList.add('__macro_hl__')`、`scrollIntoView({block:'center'})`、`setTimeout` 2.5s 移除类;返回 `{found,via}`,未命中/异常→`{found:false}`。②**`navigateWebview`**(抽 openBtn/`applyLoadedMacro` 的 `addressInput.value=url;webview.src=url`,**不 addStep** 免污染步骤)、**`sameUrl`**(忽略尾斜杠/hash 宽松比较)、**`navigateAndRetryHighlight`**(导航后 `dom-ready` 一次性+8s 超时兜底、只执行一次重试)。③**`locateStepOnPage(i)` 主流程**:高亮当前页;未找到→目标 URL=`stepUrls[i]`(继承组 URL)`||recordedUrl`;`recording` 中仅提示不跳转(避免 `dom-ready` 重挂录制器副作用);已在目标页→提示「需展开菜单/滚动等特定状态才出现」;否则 `navigateAndRetryHighlight` **自动跳转+重试**。④**`createStepLine` 加入口**:对「有元素」步骤(`type!=='goto'` 且有 `selector`/`fingerprint`——即 click/fill/waitForSelector/waitForClickable/带 selector 的 press)加 class `has-element`+行尾 🔦 可点图标(`title=点击可在页面上标记此元素`)+左键 `div.click`(`dragFromIndex!==null` 守卫防拖拽误触);goto/滚动/暂停无元素不加监听(点了无反应)。**利用既有 URL 分组特性**(`stepUrls`/`recordedUrl`)解决「加载宏后 webview 只停在首个 goto 页、后面分组步骤元素不在当前页」。**边界**:只高亮唯一命中(命中多个/指纹不唯一按未找到处理);有状态元素跳转后仍可能标不到,诚实提示;旧宏无指纹靠 selector 命中。CSS:`.step-locate`(dim,行 hover 提亮,cursor pointer)。**改动仅 `renderer.ts`(4 新函数+createStepLine 左键/图标)/`index.html`(`.step-locate` CSS);定位内核复用自 renderer.ts:2502-2511,高亮视觉抄自 webview-preload.ts:48-51**。已验证:`npm run build` + 注入页面端 JS `new Function` 语法校验 + **真实 Chromium 端到端**(注入串按选择器/**文字指纹「2026年」**/aria 各命中并加高亮类、未命中返 `found:false`;用捆绑 `chromium-1228` 的 `chrome.exe` 作 executablePath 因 headless_shell 已被打包裁掉;一次性脚本跑完即删)
- 批量标记步骤 → 页面持久多色高亮(2026-07-15,承接单步高亮;纯前端 `renderer.ts`+`index.html`,preload/主进程/core/宏 JSON/旁车零改动):单步闪一下太单薄 → 升级为「**行尾 📌 批量标记 → 同时持久高亮(标记在就一直亮),每标记轮换不同颜色**」。用户三决策:①标记手势=**行尾 📌 图标**(🔦/行点击保留单步 peek);②区分=**轮换多种颜色**(列表色点↔页面元素 outline **同色对应**);③跨页=**粘住·切页自动亮**。**①状态** `const markedColors = new Map<Step,string>()`(**Step 对象引用→颜色 hex**,不用 cid:并非每步都有 cid、懒分配还污染撤销快照;标记纯 UI 态,不写 steps/不进 `buildMacro`故不触发自动保存/不进撤销快照;拖拽保对象身份故稳,undo/redo JSON.parse 重建对象致失配→那两处 + 换宏 clear())+ `MARK_PALETTE`(8 高区分色)+ `pickMarkColor`(选未占用色,全占用取模)+ `hexToRgba`。**②批量注入 `applyBatchHighlightInWebview(items[{selector,fingerprint,color,bg}])`**:复用单步定位内核(q/vis/uniqVisible/指纹三路),但**去掉单选清空 + 2.5s setTimeout、改持久多元素**;style `.__macro_bhl__{outline:3px solid var(--bhl-c)!important;background:var(--bhl-bg)!important}`,**颜色作用于元素自身 CSS 变量 `--bhl-c/--bhl-bg` → 随滚动天然跟随、无覆盖层/无滚动重定位**;每次先清上轮(`querySelectorAll('.__macro_bhl__')` 移 class+`removeProperty`)再逐项定位加色(幂等);返回 `{applied,total}`。**③编排 `applyBatchHighlights()`**:`computeStepUrls()`+`safeGetUrl()`,遍历 steps 把 `markedColors` 命中项按 `sameUrl(urls[i],cur)` 分「本页」(进 items 带 color/bg=hexToRgba .14)与「其它页」计数→注入→`updateMarkStatus(本页亮,其它页,本页未命中)`。**④`toggleMark`**(有则删/无则 `pickMarkColor`;存/复 `#steps` scrollTop 后 `renderSteps()`——标记不在 JSON 故不触发撤销/自动保存——再 `applyBatchHighlights`)/`clearAllMarks`。**⑤UI**:`createStepLine` 元素步骤加 `.step-pin`📌(`click` `stopPropagation`→`toggleMark`,不冒泡触发行 peek)+ 标记态给行加 `.marked`+追加 `.mark-dot`(`background`=该色,📌 emoji 不可染色故独立色点);`createUrlGroupHead(url,count,markedCount)` 加 `.url-group-marks`「🔖 N」(`stopPropagation`→`navigateWebview(url)` 跳该组页);`renderSteps` 按组范围数 `markedColors` 命中传入。**⑥接线**:全局 `dom-ready`(renderer.ts) 末尾 `if(markedColors.size) applyBatchHighlights()`(**跨页粘住核心**:切页后亮出新页标记);`applyLoadedMacro` 换宏 `markedColors.clear()`;`undoSteps/redoSteps` 恢复后 `if(size){clear();applyBatchHighlights()}`(抹页面残留)。**⑦index.html**:`#steps-title` 与 `#steps` 间插 `#steps-toolbar`(`#mark-status` 文「已标记X·本页亮Y·其它页Z」/空提示 + `#mark-clear`);CSS `.step-pin/.step-pin.on/.mark-dot/.step-line.marked/.url-group-marks/#steps-toolbar`。**边界**:标记数>8 色轮换复用靠位置辅助;只高亮唯一命中(多个/指纹不唯一→计本页未命中);SPA 就地改 DOM 无导航不自动重注入(dom-ready/切页/toggle 才重注入)。**改动仅 `renderer.ts`(状态+批量注入+toggle/clear/apply+createStepLine 📌+分组头徽标+dom-ready/undo/redo/load 接线)/`index.html`(`#steps-toolbar`+CSS)**。已验证:`npm run build` + **真实 Chromium 端到端**(注入多 items 不同色→各元素 `--bhl-c` 正确含**文字指纹「2026年」**、未命中计入、重注入清旧应用新、空 items 清空,9 断言全过;一次性脚本跑完即删)
- 侧栏单板块全占(专注)模式(2026-07-15,纯前端 `index.html`(CSS)+`renderer.ts`,core 回放/preload/主进程/宏 JSON/旁车/既有折叠·高级·撤销·自动保存·URL 分组零改动):侧栏 7 板块纵向堆叠,想专注看/操作一个时其它分散注意力。**需求(用户确认)**=点某板块标题的 `⛶` 按钮 → 该板块占满侧栏可用高度,其余板块全部收起,侧栏顶部出现一排 **sticky 缩小图标**(每图标一个板块),点图标切换全占板块、点 ✕ 退出回正常堆叠。**实现**:①**中央映射 `FOCUS_PANELS`**(renderer.ts,panel id→图标+名称,顺序=DOM 顺序;3 个原无 emoji 的 `extract-panel📋`/`browser-panel🔐`/`steps-panel🎬` 新指定,余 `ai🤖`/`plugin🧩`/`tool🧰`/`macro-lib📚` 复用)是单一事实来源。②**`setupFocusMode()`**(挂在 `init` 的 `setupMoreMenu` 之后、**须晚于 `setupAdvancedMode`**,其高级联动监听才能读到已更新的 `.advanced` class):遍历给每个 `.panel-title`(`#{前缀}-title`,由 `id.replace('-panel','-title')` 派生)`appendChild` 一个 `.focus-btn`(⛶),click 里 **`e.stopPropagation()`** 不触发标题上既有的折叠 toggle、`applyFocus(id===focusedPanelId?null:id)`(toggle 语义);构建 `.focus-rail`(每板块 `.rail-icon[data-panel]` + 末尾 `.rail-exit`✕)`insertBefore` 为 `.sidebar` 首子元素。③**`applyFocus(id|null)`**:切 `.sidebar` 的 `.focus-mode` + 目标板块 `.panel-focused`(并 `remove('collapsed')` 强制展开,否则 body 被折叠规则隐藏),状态存 `localStorage:macro.focusPanel`,重启 `setupFocusMode` 恢复(读 key + `getComputedStyle(panel).display!=='none'` 校验可见性,失效则清 key)。④**CSS**:`.focus-rail` 默认 `display:none`,`.sidebar.focus-mode>.focus-rail{display:flex;position:sticky;top:0}`;`.rail-icon` 30×30 `font-size:16px`(**缩小图标**,重置全局 button 蓝底大 padding);`.sidebar.focus-mode>.panel{display:none}` + `.sidebar.focus-mode>.panel-focused{display:flex;flex:1 1 auto;min-height:0}`(**两条同特异性 `(0,3,0)`,`.panel-focused` 写在后面靠源码顺序取胜**),全占面板各 body(`.ai-body`/`.plugin-body`(兼覆盖 tool)/`.browser-body`/`.macro-lib-body`/`#steps`/`#extract`)`flex:1;min-height:0;overflow:auto`,`#extract` 另 `height:auto` 覆盖固定 160px。**核心难点=规避高级 ID 规则特异性冲突**:`.sidebar:not(.advanced)>#ai-panel{display:none}`(`(1,2,0)`)高于 `.panel-focused`(`(0,3,0)`),**不打特异性战争**,策略=**只允许全占「当前可见」板块**——`isPanelAvailable(id)` 用 `.advanced` class(非 `getComputedStyle`,因 focus-mode 下全部 `display:none` 会误判)判定 `advanced || !ADVANCED_ONLY.has(id)`(`ADVANCED_ONLY` 与那条 CSS 名单一致);基础视图 `refreshRail` 隐藏 4 个高级图标、关高级时若正全占高级面板则 change 监听自动 `applyFocus(null)` 退出。**零影响**:只做 CSS class 切换 + 注入装饰性 DOM(rail 与标题按钮均不在 `#steps`(renderSteps 清空重填)内,不被动态渲染冲掉),不碰 steps 数据/`buildMacro`/captures/IPC,故 undo/autosave/URL 分组/录制回放全不受影响;`.app.picking .sidebar{pointer-events:none}` 已覆盖 rail 无需额外处理。**改动仅 `src/renderer/index.html`(一段 CSS)+`src/renderer/renderer.ts`(`setupFocusMode` 等 + `init` 挂载一行)**。已验证:`npm run build` + `verify-captures`/`verify-merge` 回归通过;Electron 窗口视觉/交互(⛶ 进入、图标条切换、✕/再点退出、基础↔高级图标条增减、重启恢复、关高级自动退出)留肉眼验收
- 回放时回填步骤来源 URL(2026-07-06,救旧宏精确分组):旧宏无 `recordedUrl`,离线只能按 goto 边界分组——点链接跳转(无 goto)后的步骤会错并进前一页组。**回放本就真实访问每页**,故 `macro-runner.ts` 主循环每步执行前 `stepUrls[i]=snapUrl(activePage)`(取 `activePage.url()`,`about:blank`/异常记 null;语义=该步来源页,与录制打戳一致),`RunResult.stepUrls?:(string|null)[]` 随成功/失败(部分执行也回传)带回;`main.ts` 的 `run-macro` 直接透传。renderer 的 `reportRunResult` 先调 `backfillRecordedUrls(result.stepUrls)`:长度与 `steps` 对齐时,把 URL 写回**缺失** `recordedUrl` 的非 goto 步骤(**只补不覆盖**、不动 goto 与新宏已有戳),`renderSteps()` 刷新分组 + 既有自动保存落盘。**旧宏跑一次即精确分组、以后打开都对**。纯附带、不改回放行为;renderer 侧本地 `RunResult` 接口(第 46 行,与 preload API 对齐的副本)也同步加了 `stepUrls?`。自检 `scripts/verify-stepurls.mjs`(quotes 首页点作者链接进详情页,断言 click 步来源=首页、其后 waitForSelector 步来源=详情页,正是离线救不了的链接跳转场景;需网络 + `MACRO_HEADLESS=1`)
- 工具栏动作按钮自动溢出(2026-07-03,`renderer.ts` 的 `setupMoreMenu`):有序 `TOOLBAR_ACTIONS = ['start','stop','run','stop-run','save','load','ai-fix-all','export']`(顺序即优先级)+ 固定显示数量 `MAX_VISIBLE_ACTIONS=7`,`slice(7)` 的靠后按钮(导出Excel)自动 `appendChild` 进 `#more-menu`(「更多」下拉),`#more-menu` 空则隐藏 `.more-wrap`。**改按钮顺序/可见数量只改这两个常量**。按钮监听全按 id 绑定,工具栏↔菜单间移动 DOM 不影响点击/`setBusy`/`setRecordingUI` 的 disabled 切换。配套:页面内 🐟「二爪鱼」品牌块已删(窗口标题栏/任务栏图标已承载,避免重复);「插入暂停」工具栏按钮已删(右键菜单「在此前/此后插入暂停」仍在,`insertPause` 保留);HTML 里动作按钮平铺在工具栏行、`#more-menu` 初始置空由 JS 填充
- 日志区高度拖动条(2026-07-03,`#log-divider`,`renderer.ts` 的 `setupLogDivider`,与左右 `#main-divider`/`setupMainDivider` 同构):`.main` 与 `.logwrap` 间插上下分隔条(`row-resize`),拖动实时改写 `.logwrap` 的 `flex`/`height`(向上拖变高,限 [90px, 整窗高 60%]),复用 `.main.resizing webview{pointer-events:none}` 防拖拽时 webview 吞事件,高度存 `localStorage:macro.logHeight`
- 主页面 UI 主题(2026-07-03,「明亮海洋」设计系统,纯 `src/renderer/index.html` 改动、`renderer.ts` 零改动;后续叠加了日志拖动条一处 `renderer.ts` 改动):用 `frontend-design` skill 重塑,方向明亮活泼·亲和非技术用户,**保持三分区布局**(浏览器 + 侧栏卡片面板 + 底部日志)。核心=`<style>` 顶部 `:root` **CSS 设计令牌**(`--primary:#0fb0b8` 水青主色 / `--accent:#ff7a59` 珊瑚点缀 / `--deep:#0e2a33` 深青海底作工具栏·日志 / `--ok`绿·`--danger`红·`--ai`紫·`--plugin`青 语义色 / `--r-*` 圆角 / `--sh-*` 阴影 / `--font-body` YaHei·`--font-mono`),全局硬编码色值统一走令牌。视觉:工具栏加 🐟 品牌区(渐变标题 `-webkit-background-clip:text`)+ 底部主色/珊瑚渐变描边,`rec-indicator` 录制中呼吸脉冲(`@keyframes rec-pulse`);侧栏面板全部卡片化(圆角+柔和阴影+hover),`.app` 径向渐变网格氛围,滚动条美化,`#steps:empty` 空状态提示,按钮 hover 抬升 + `:focus-visible` 环。**硬约束:不重命名/删除任何 element ID 与 `renderer.ts` 依赖的 class**(步骤行/徽标/右键菜单/插件/日志/拾取/暂停/折叠等动态 class 全覆盖不改名);改动前脚本核验 48 项 ID 仍在。**AI 提取面板归类「高级」**:`.sidebar:not(.advanced)` 隐藏组追加 `#ai-panel`(与提取 JSON/插件/登录态一致),基础视图侧栏只留「已录制的操作」,勾「✨ 高级」才现出四个高级面板;逻辑零改动(早有 `.advanced` class 机制)。字体受限于离线 Electron(不引 Google Fonts),用 Windows 系统字体 YaHei/Segoe UI
- 录制:Electron `<webview>`(内置 Chromium);回放/提取:独立 Playwright Chromium
- 回放反检测加固(因 Playwright 自带的「Chromium for Testing」易被反爬识别:`navigator.webdriver`、`--enable-automation` infobar、缺 Chrome 品牌组件):①**加固启动**——`launch`/`launchPersistentContext` 统一加 `args:['--disable-blink-features=AutomationControlled','--no-first-run','--no-default-browser-check']` + `ignoreDefaultArgs:['--enable-automation']`;②**内核优选回退链**——`preferSystemChrome` 为真时依次试 `channel:'chrome'`→`channel:'msedge'`→捆绑 Chromium(undefined),try/catch 逐个回退并 `logInfo` 实际命中内核(Windows 10 必带 Edge,几乎总命中真品牌);③**addInitScript**(导航前、cookie 注入前)——置空 `navigator.webdriver`、补 `window.chrome.runtime`、修正 `permissions.query(notifications)`、补 `languages`/`plugins` 非空;④仅**回退到捆绑 Chromium** 时才设 `userAgent`(Chrome/131 串)+`locale:zh-CN`+`viewport`(真 Chrome/Edge 不覆盖 UA,免与 Sec-CH-UA 版本错配)。开关 `browser-config.json.useSystemChrome`(默认开)经 `SessionOptions.preferSystemChrome` 传入 core;UI 在「浏览器登录态」面板 `#bc-chrome`。**后续若遇 Cloudflare/DataDome 等深度 CDP 指纹检测,再升级 patchright(drop-in 替换 playwright,需改 `copy-playwright-browser.mjs` 下载命令)**
- 安全:Playwright 仅在主进程,渲染进程通过 IPC + contextBridge 访问
- 构建:纯 tsc 双 tsconfig(主进程 CommonJS + 渲染进程 ESM)+ `scripts/copy-assets.mjs`
- 入口:`dist/main/main.js`;启动:`npm run dev`
- 运行时目录(自动创建):`macros/`(宏)、`exports/`(Excel)、`errors/`(错误截图)、`downloads/`(回放捕获的下载文件)
- 提取模式(`ExtractConfig.mode`):`single`(整页单行)、`list`(逐项采字段)、`list-detail`(逐项进详情页采字段)、`list-action`(逐项点击按钮,不采数据)。前三种产出 `ExtractRow[]`→Excel;`list-action` 产出下载文件
- list-detail 详情入口改造(2026-06-29):**不再由 AI 生成 `detailLinkSelector`**(老做法 agent 易选成 `<head>` 里的 `link[rel=canonical]` 等全页元素,而运行时在「项内」找 → 0 命中 → 不进详情页;自检回路又因「项内任意一个命中即可」放行,坏宏被保存)。改为:`ListDetailExtractConfig` 用 **`detailLinkField`**(字段名,引用 `fields` 里某项)取代 `detailLinkSelector`;`extractor.ts` 的 `collectListDetailPage` 按 `detailLinkField` 找到该字段、用**其 selector** 在项内取 `href` 再绝对化(字段缺失/留空→取列表项自身)。UI:`#ai-mode` 选 `list-detail` 时显示 `#ai-detail-link-field` 下拉(选项来自现有 list 规则的 `fields[].name`,`renderer.ts` 的 `refreshDetailLinkFieldOptions`),用户选定的字段在生成时由 renderer 写入 `lastRules.detailLinkField`(AI 只补 `detailFields`,`buildModeHint` 已去掉详情链接);`verifySelectors` 对所选字段额外实测「首项能否取到非空 href」(`wantHref`/`hasHref`),取不到只在日志非致命提醒、不阻断。**彻底移除 `detailLinkSelector`,不做旧宏自动迁移**(旧 list-detail 宏需重新生成才进详情页);agent 侧 SOUL.md 的 list-detail 契约同步删除该字段。已生成的 `macros/webscraper.json` 按要求保留未改。
- 通用下载捕获 + 列表逐项动作(list-action):①**通用捕获(context 级原语,对所有 mode 生效)**——回放 context 统一加 `acceptDownloads:true`,新增 `src/core/download-manager.ts` 的 `DownloadManager` 挂 `context.on('download')`,用 `download.suggestedFilename()` 落盘到 `downloads/`,重名加 `(1)/(2)` 去重(保存失败 try/catch 不致命);暴露 `savedPaths`/`count()`/`waitForNext(timeoutMs)`(pending-resolver 队列,FIFO 唤醒)。单个下载按钮等普通场景也直接受益。②**list-action 模式(`extractor.ts` 的 `extractListAction`,与 list/list-detail 并列)**——遍历 `listSelector`,逐项点 `actionSelector`(留空点项本身),每点一项 `await downloads.waitForNext(actionTimeout??30000)` 等这次下载开始再点下一项(超时只告警继续,晚到下载仍被全局 handler 保存);复用现有 `PaginationContext` 翻页;返回 `[]`,下载结果经 `DownloadManager.savedPaths` 由 runner 回传到 `RunResult.downloads`。③**贯通**:`MacroRunner` 构造函数追加可选末参 `downloadDir`(`new MacroRunner(errorDir,timeoutMs?,onPause?,session?,downloadDir?)`,缺省回退 errorDir 同级 downloads);`extract()` 加可选第 4 参 `downloads?:DownloadManager`(仅 list-action 用);主进程 `main.ts` 加 `downloadsDir` 入 `ensureDirs`、传入 runner,`run-macro` 返回若 `downloads.length>0` 则 `shell.showItemInFolder` 定位;UI `#ai-mode` 加 `list-action` 选项,回放后按 `RunResult.downloads` 显示「已下载 N 个文件」。④**AI 一等支持**:`ai-extract.ts` 的 `GenerateInput.mode` 与 `buildModeHint` 加 `list-action` 分支(hint 直接喂结构 `{mode,listSelector,actionSelector}`,无需改 webextract 的 SOUL.md;不需 baseRules)
- 翻页标记:步骤行右键「标记翻页操作」可设总页数 N,步骤加 `pagination:true`/`pageCount:N` 字段;回放跳过该步骤,提取(list/list-detail/list-action)时按总页数逐页采集,每页采完执行翻页序列。list-detail 先跨所有页收完整列表再统一进详情。**翻页标记是「是否翻页」的唯一控制(标了逐页处理、没标只当前页),不另设开关**
- 翻页逐页处理鲁棒性(2026-06-29,三模式共享、通用化):list-action 配翻页后实测「2 页只有第 2 页执行动作」。根因——三种翻页循环(`extractList`/`extractListDetail`/`extractListAction`)都只在 `turnPage()` **之后** `waitForSelector(listSelector)`,**首页处理之前无任何等待**:list-action 首页列表 AJAX 未就绪即 `items.count()=0`,整页点击被跳过(只剩末页因翻页后有等待才生效);list/list-detail 采文本通常已就绪故更隐蔽但同类存在。修复**不动翻页标记机制、不加任何配置/UI**,只在 `extractor.ts` 抽两个共享 helper 让三模式统一变鲁棒:①`waitListReady(page,listSelector)`——每页处理前 `waitForSelector`(修首页),超时 catch 放行(真空页按 0 项,非致命);②`turnPageAndSettle(page,pagination,listSelector)`——翻页前记「首个列表项文本」,`turnPage()` 后用 `waitForFunction` 等首行文本变化(SPA 纯 JS 换内容后旧行常仍在 DOM,单纯 `waitForSelector` 会立即通过→读到旧页/重复处理;两页首行恰好相同或超时则放行,靠下轮 `waitListReady` 兜底;非 CSS 选择器 `document.querySelector` 抛错则放行)。三循环各接「循环顶 `waitListReady` + 翻页分支换 `turnPageAndSettle`」两处。超时常量 `PAGE_SETTLE_TIMEOUT=30000`(空页不干等满全局 60s)。**行为变化**:换页后等待由「无超时、超时即抛致 run 失败」改为「30s catch 放行按空页处理」,对慢/真空页更宽容。验证:`scripts/verify-pagination.mjs`(list 3 页 30 行)+ `scripts/verify-merge.mjs` 回归通过;bcpay live list-action 待手动复跑。**重构(2026-06-29)**:三处翻页循环骨架本是复制粘贴(此 bug 即因此三发),已抽公共高阶函数 `paginatedCollect(page,listSelector,pagination,processPage)` 收敛「逐页等就绪→处理本页→翻页等切换」为单一事实来源,`list`/`list-detail` 阶段一/`list-action` 三处改为传「处理本页」回调(累加变量留外层闭包),`waitListReady`/`turnPageAndSettle` 仅被它调用。**纯结构重构零行为变化**;以后改翻页策略只改一处、加新提取模式只写回调即自动获得逐页+翻页+等待,接线不会漏
- 选择器健壮性(翻页等「录制后页面增减兄弟元素」场景的通用修复,非分页器专用):根因是结构/位置选择器按「录制那一刻 DOM」生成——分页器首页只有 `<li class="next">`,生成器没给 `li` 加 `:nth-of-type`,得 `…ul > li > a`;翻页后冒出 `<li class="previous">`,该选择器同时命中 Previous+Next,严格模式报错/误点 Previous 往回翻。**双层通用防御**:①**录制端根治**——`selector-generator.ts` 的 `buildCssPath` 每段在可用时携带稳定 class(`li.next`),`:nth-of-type` 仅在 class 仍无法区分同标签兄弟时才追加;并新增稳定 class 提前截断(类比 id 截断,全路径校验唯一)。新录制即得 `li.next > a` 这类与兄弟增减无关的选择器。②**运行端通用兜底**——`selector-generator.ts` 导出 `buildFingerprint(el)` 为 click 采语义指纹(`tag/text/ariaLabel/href/anchor`,anchor=最近一个稳定锚点祖先选择器),preload click 回调随 `{type:'click',selector,fingerprint}` 回传,`ClickStep.fingerprint?` 可选(旧宏缺省照常跑);`macro-runner.ts` 的 `handleClick` 先 `locator(selector).count()`,唯一命中走主路径,命中 0/多个/非法时若有指纹则 `relocateByFingerprint`(anchor 缩小 → 文本精确 → aria → href,逐条取首个可见可用元素 `firstVisible`)通用重定位,无指纹/失败回退原生 `page.click` 沿用出错截图路径。**翻页 click 经 `turnPage→executeStep→handleClick` 自动受益,无翻页专用代码**。③**翻页竞态修复**——`turnPage` 在执行完翻页步骤后 `await runPage.waitForLoadState('domcontentloaded')`,等整页导航稳定再交回提取流程,消除「在旧页/半载页采集致少采、或读到旧分页器误判命中数」(纯 JS 换内容无导航时即时返回,由提取端 `waitForSelector` 兜底)。**硬约束**:指纹仅对此后新录制生效,已录旧宏(无指纹)靠改动①后**重录一次**修复。验证:`node scripts/verify-pagination.mjs`(脆弱 `.pager li a` page2 命中 2 + 指纹 anchor 重定位,quotes.toscrape.com 采满 3 页 30 行)。④**原子化 CSS 类过滤(2026-07-02)**——`isStableClass` 是「哪些 class 算稳定、可拼进选择器」的唯一判据;原三条(≥16 长哈希、`\d{4,}`、`css-/sc-/jsx-/emotion-` 前缀)拦不住 **Facebook Stylex** 的原子类(`x1ey2m1c`/`x78zum5`/`xdt5ytf` 等 `x` 开头短哈希,长度仅 6~9,每次发版变),致 FB 录制选择器变成一长串 `div.x1ey2m1c.x78zum5…` 垃圾类。追加两条通用规则:`^x[a-z0-9]{4,}$` 且含数字(FB Stylex)、`^r-[a-z0-9]{5,}$` 且含数字(Twitter/X)。**均强制含数字**故纯词类名/带连字符语义类(`x-axis`/`col-md-6`)天然不匹配、不误伤;`next`/`previous` 等翻页类保留(回归已断言)。过滤后 FB 元素无稳定类则回退纯结构 CSS path(短且无垃圾类),仍有指纹兜底。`isStableClass` 已 `export`,自检 `node scripts/verify-selector-class.mjs`(过滤 14 原子类 / 保留 9 语义类)
- 修改 fill 填写内容(2026-07-02):宏里 `fill` 步骤的 `value` 是录制当时的固定文本(账号/搜索词/表单内容),换内容不必重录或改 JSON。步骤行右键**仅当 `step.type==='fill'`** 出现「✏️ 修改填写内容」(`renderer.ts` 的 `showStepContextMenu` 内,紧邻「重新点选此步骤的选择器」段),点击调 `showFillValueInput(menu,index)`——**仿 `showPauseReasonInput`** 在菜单内转文本输入框、预填当前 `value`、回车/确定生效。与 pause.reason 的关键差异:**不 trim、不删字段、允许空串**(填写内容可能含前后空格且语义上必填,空串代表清空)。改后 `renderSteps()` 即刷新步骤行文案(`describeStep` 已渲染 `fill … = "value"`),点「保存宏」落盘;回放端 `handleFill` 读的就是 `value`,零改动。选择器修正仍归既有「重新点选此步骤的选择器」;**仅前端一文件改动**(菜单项 + `showFillValueInput`),core/主进程/HTML/样式(复用 `.ctx-menu-input`)均未动
- 人工介入暂停:步骤行右键「在此前/此后插入暂停」或工具栏「插入暂停」插入 `pause` 步骤(`{type:'pause',reason?,timeout?}`);回放到此停住,有头浏览器窗口保持可交互,用户手动完成登录/验证码/扫码后,在主窗模态框点「继续」恢复。机制:`MacroRunner(errorDir,timeoutMs?,onPause?)` 注入回调(core 不依赖 Electron,无回调默认放行);主进程 `run-macro` 用递增 `runId` 隔离 `resume-macro` 信号、`macro-paused` 事件通知渲染进程;`timeout` 防无人值守挂死(超时走出错截图)。**pause 与 `pagination` 互斥**(回放主循环跳过 pagination 步骤),UI 禁止对 pause 标翻页
- 运行中停止/中断回放(2026-07-03,修「宏跑起来没法中断」):回放一旦开始只能等它跑完/报错/撞超时,卡在慢步骤(如 60s 超时的 `waitForSelector`)时只能干等。**方案 = 照搬「运行→暂停→恢复」信号往返机制加一条「停止」信号**,不引入新框架。**中断分两层**:①**协作式取消标志**——`MacroRunner` 加 `private cancelled=false`,主循环每步前(及提取前)检查,已置位则抛出、跳过后续步骤/提取,保证"步骤之间"干净停;②**强制打断正在进行的操作**——`cancel()` 主动 `this.activeContext.close()`,让正在 await 的 Playwright 操作(慢 waitForSelector/goto/提取阶段 locator)立即抛 "Target closed",`run()` 从 catch 退出。**单靠标志无法打断已进入的 await,关 context 才是即时性关键**。实现:`macro-types.ts` 的 `RunResult` 加 `cancelled?:boolean`(区分"用户停止"与"失败");`macro-runner.ts` 加实例字段 `cancelled`/`activeContext` + 公有 `cancel()`(依赖倒置,同 `onPause`,core 无 Electron 依赖),`run()` 拿到 context 后存 `activeContext`、主循环/提取前检查、**catch 最前面**判 `if(this.cancelled) return {ok:false,cancelled:true}`(绕过错误截图,此时页面已关会抛错)、finally 的 `context.close()`/`browser.close()` 各包 try/catch(停止时已关过,防二次 close 抛错覆盖返回值)。主进程 `main.ts` 的 `run-macro`:runner 创建后注册 `stop-macro` 监听(匹配 `runId` 调 `runner.cancel()`,同 resume 隔离)+ push 到 `listeners` finally 清理,并 `wc.send('macro-run-started',{runId})` 让 renderer 拿到 runId。`preload.ts` 加 `stopMacro(runId)`/`onMacroRunStarted(cb)`(仿 `resumeMacro`/`onMacroPaused`)。UI(`renderer.ts`+`index.html`):工具栏加 `#stop-run`「停止回放」按钮(注意 `#stop` 已被「停止录制」占用;`setBusy` 令其与运行按钮反相 enable,点击发 `stopMacro(activeRunId)` 并禁用+「停止中…」防重复)、暂停模态框 `.pause-actions` 加 `#pause-stop`(**同时发 stop+resume**——暂停期间无 Playwright 操作在跑,单靠关 context 不解除 `onPause` 等待,需 resume 让 `handlePause` 返回、主循环下一轮顶部检查 cancelled 抛出);`reportRunResult` 最前面加 `if(result.cancelled)` 分支提示"已被用户停止"、不弹错误。**停止=中止并关闭浏览器**(需保留窗口手动操作用「暂停」);`runId` 隔离保证信号不串;旧宏零迁移、无 step 类型改动(`never` 穷尽检查不受影响)。**已验证**:`MACRO_HEADLESS=1 node scripts/verify-cancel.mjs`(卡 30s waitForSelector,800ms 后 cancel(),846ms 返回 `cancelled=true`)+ `verify-pagination`/`verify-merge`/`verify-popup-wait` 回归通过 + `npm run build` 通过
- 滚动到底部步骤(无限滚动支持,2026-06-30):无限滚动页录不出(录到的 `scroll` 是**固定座标**、现有翻页是「点下一页」置换型,均不适配)。新增专用步骤 `{type:'scroll-bottom'}`(`ScrollBottomStep`,无参,运行时取实际页高):步骤行右键「在此后添加滚动到底部」插入(`renderer.ts` 的 `insertScrollBottom`,仿 `insertPause`),回放执行到此 `macro-runner.ts` 的 `handleScrollBottom`——**`window` 滚到底 + 扫描所有 `overflowY:auto/scroll/overlay` 且 scrollHeight>clientHeight 的内部可滚动容器(含 fixed 定位弹层/侧栏/主内容区)各自 `scrollTop=scrollHeight`**(设 scrollTop 会派发 scroll 事件,兼容 scroll 监听型与 IntersectionObserver 哨兵型懒加载;`logInfo` 输出命中容器数便于诊断)+ 固定 `waitForTimeout(1000)` 等懒加载就绪。仅滚 window 对「内部容器滚动型」无限滚动无效,故滚全部容器最通用。**与 pause/scroll 一样是普通顺序步骤(正常回放就执行,不像 pagination 被跳过、不联动提取流程)**;需多次加载就插多条。`executeStep` switch 与两处 `describeStep`(core/renderer)同步加 `scroll-bottom` 分支(`never` 穷尽检查)
- 等待页面加载完成步骤(2026-06-30):新增专用步骤 `{type:'wait-for-load'}`(`WaitForLoadStep`,无参):步骤行右键「在此后添加等待页面加载完成」插入(`renderer.ts` 的 `insertWaitForLoad`,仿 `insertScrollBottom`),回放执行到此 `macro-runner.ts` 的 `handleWaitForLoad`——调 `page.waitForLoadState('load')` 等整页(DOM+所有资源)加载完毕再继续后续步骤;**超时只 `try/catch` 告警不致命**(防轮询型站点 `load` 永不触发把回放挂死),沿用全局默认超时(`MACRO_TIMEOUT`)。与 `pause`/`scroll-bottom` 一样是**普通顺序步骤**(正常回放就执行,不像 pagination 被跳过、不联动提取流程)。与 `waitForSelector`(等某个特定元素)不同,这是等**整页**加载完成的通用步骤。`StepType`/`Step` 联合、`executeStep` switch、两处 `describeStep`(core/renderer)同步加 `wait-for-load` 分支(`never` 穷尽检查)
- 元素拾取器 +「等待元素出现」步骤(2026-06-30):用户要「等待网页某个特定元素加载完成」并希望在浏览器里**点选**该元素而非手写选择器。`waitForSelector` 步骤类型与回放 `handleWaitForSelector` **本已存在**(只缺 UI 入口),故本功能 = **元素拾取器** + 生成 `waitForSelector` 的右键入口。**实现复用录制端注入通道**(`webview-preload.ts` 已在隔离世界 import `generateSelector`/`buildFingerprint`、有 `toggle-recording`/`sendToHost` 通道):①preload 加 picker 模式——新增 `toggle-picker` IPC 监听 + `picking` 状态;`enterPicker` 注入高亮 `<style>`(hover 元素蓝色 outline+半透明底、整页 `cursor:crosshair`)并以 capture 注册 `mouseover/mouseout/click/keydown` 具名监听;`onPickerClick` 对命中元素**直接** `generateSelector`(不走 `resolveClickable`,用户点的就是要等的元素)、`preventDefault`+`stopPropagation`+`stopImmediatePropagation` 拦截跳转、`sendToHost('picker-result',{selector,fingerprint})` 后自动 `exitPicker`;ESC→`exitPicker(true)` 回传 `{cancelled:true}`;`exitPicker` 成对移除全部监听/清高亮/移 style。**与录制互斥**:录制 click handler 开头加 `if(picking)return`(`stopImmediatePropagation` 拦不住注册更早的录制监听)。②renderer——右键菜单加「在此后添加等待元素出现(点选)」→ `startPicker(index+1)`(记 `pickerInsertAt`、若在录制则 `armRecorder(false)` 临时挂起、`webview.send('toggle-picker',true)`,try/catch 页面未就绪回滚);`ipc-message` 加 `picker-result` 分支(取 `pickerInsertAt`、恢复 `armRecorder(true)`、`cancelled`/无 selector 各自日志、否则 `insertWaitForSelector` 插入 `{type:'waitForSelector',selector}`);`dom-ready` 兜底:拾取期间导航则清 `pickerInsertAt`。**超时致命**(主循环无 per-step try/catch,等不到元素由顶层 catch 截图中止,符合 `waitForSelector` 强前置语义,与 `wait-for-load` 的非致命不同)。两处 `describeStep` 的 `waitForSelector` 文案统一为「等待元素出现 …」。**限制**:跨域 iframe 内元素拾取不到(preload 只注入顶层文档,同录制 click)
- 等待元素可点击步骤(2026-07-01):澄清既有「等待元素出现」= `page.waitForSelector` 默认 `state:'visible'`,**只等出现且可见,不等可交互**;补一个语义更强的 `waitForClickable` 覆盖「出现≠可点击」场景(透明遮罩盖住内容 / disabled 按钮变 enabled / SPA hydration 未完成)。**这些场景的共同点是下一步不是 click**——若下一步是 click,Playwright `click` 自带完整 actionability auto-wait 已兜住,无需本步骤;本步骤专供「下一步是提取/fill/滚动/纯同步屏障」的情形。**核心设计取舍(用户明确要求零副作用)**:**不用** Playwright 的 `locator.click({trial:true})`——trial 虽 actionability 最完整(含稳定性/遮挡),但官方文档确认它**只跳过最后的点击动作、不跳过 scroll into view**,会改变页面滚动位置,对本项目的 `scroll-bottom`/无限滚动懒加载是隐患。改用 `page.waitForFunction` 在页面上下文里做**纯只读判定**、轮询到通过或超时:①`getBoundingClientRect` 尺寸>0;②非 `display:none`/`visibility:hidden`;③非原生 `disabled`、非 `aria-disabled=true`;④**遮挡检测**——仅当元素中心点在视口内时 `document.elementFromPoint(cx,cy)` 命中自身/后代/祖先才算未被遮挡(视口外测不准 → **降级跳过遮挡检测**,只判尺寸+disabled)。**零副作用**(不滚动/不派发事件/不改 DOM);**超时致命**(同 `waitForSelector` 强前置语义,区别于 `wait-for-load` 非致命);未指定 timeout 走全局默认(`setDefaultTimeout` 对 waitForFunction 生效)。UI 复用拾取器:步骤右键「在此后添加等待元素可点击(点选)」→ `requestPick(sel=>insertWaitForClickable(at,sel))`。**9 处照 `waitForSelector` 模式机械接线**(`macro-types.ts` 的 StepType/`WaitForClickableStep`/Step 三处、`macro-runner.ts` 的 executeStep case/`handleWaitForClickable`/core 侧 describeStep、`renderer.ts` 的右键菜单项/`insertWaitForClickable`/renderer 侧 describeStep);**preload 与 index.html 零改动**(拾取器已通用、菜单动态生成),`executeStep` 的 `never` 穷尽检查是漏接线安全网。**限制**:不检测「动画是否停止」(waitForFunction 无稳定性判定);视口外元素不判遮挡。**选择器引擎修复(2026-07-01)**:判定函数最初只用 `document.querySelector`(浏览器原生,**只认 CSS**),而 `generateSelector` 对纯文本元素(`<p>`/`<span>` 等无稳定 CSS 锚点)会产出 `xpath=` 前缀选择器 → `querySelector` 抛错被 catch 吞成「未就绪」→ 60s 误超时(实测 uitestingplayground.com/ajax 的 `<p>` 复现;而「等待元素出现」走 `page.waitForSelector` 认 `xpath=` 故正常,对比即锤实此因)。修复:判定函数**找元素这一步加引擎分诊**——`sel` 以 `xpath=` 开头则 `document.evaluate(sel.slice(6), …, XPathResult.FIRST_ORDERED_NODE_TYPE)` 取 `singleNodeValue`,否则原 `querySelector`;加 `nodeType!==1` 守卫(XPath 可能命中文本/属性节点)。`document.evaluate` 与 querySelector 同为**纯只读**,零副作用不变;CSS 走 else 老路径逐字不变;下游只读检查一字未改;每轮轮询重新解析故对 AJAX 重渲染免疫。**同类「只认 CSS」消费点** `extractor.ts` 的 `turnPageAndSettle`、`renderer.ts` 的 `verifySelectors` 撞 xpath 时是放行/报非法(不硬失败),本次未改
- 拾取模式进入提示 + 焦点/ESC 兜底(2026-07-02):两个体验问题——①拾取(picker)的 ESC 监听只在 webview guest 内(preload `onPickerKey`,capture 注册在 guest document),焦点在主窗口(工具栏/侧栏)时按 ESC 无效,须先手点浏览器获焦;②进入拾取后主界面零变化,用户不知已进入、可能误点录制浏览器里的元素(误生成选择器)。**修复仅前端两文件(`renderer.ts`+`index.html`),不碰 preload/core/主进程**(preload 的 ESC/高亮/清理已完善,只补主窗口侧):`renderer.ts` 抽三辅助——`enterPickingUI()`(进入拾取即 `webview.focus()` 把焦点交给内嵌浏览器 + `appEl.classList.add('picking')` + 显示提示横幅)、`exitPickingUI()`(成对移除)、`cancelPick()`(主窗口有焦点时的取消:发 `toggle-picker false` 让 preload `exitPicker(false)` 清理 guest、复位 `pendingPick`、恢复录制、退 UI);`requestPick` 成功分支尾调 `enterPickingUI`,`picker-result` 分支加 `exitPickingUI`,新增 **document 级 `keydown` ESC 监听**(`pendingPick` 非空才拦、`preventDefault`+`cancelPick`),横幅取消按钮亦调 `cancelPick`。`index.html` 加 `#pick-hint` 横幅(蓝色顶部居中,`.show` 切换,仿 `#pause-overlay`)+ 取消按钮,CSS `.app.picking` 令 `.toolbar/.sidebar/.logwrap/.main-divider` `pointer-events:none`+`opacity:.45`(达成「只能在浏览器操作」)、`.browser-panel` 高亮描边。**退出双路互斥无重复**:焦点在 guest→ESC 由 preload 处理回传 `picker-result`(guest keydown 不冒泡到主窗口);焦点在主窗口→document 监听走 `cancelPick`(`toggle-picker false` 不回传 picker-result);两者均以 `pendingPick` 为闸门幂等。`npm run build` 双 tsconfig 通过
- 登录态复用(回放/录制两浏览器引擎不同,无法严格共用同一目录,故用两条可叠加 UI 开关,默认关,存 `browser-config.json`):①**持久化回放 profile**——回放从临时 profile 改 `chromium.launchPersistentContext(userDataDir)`,跨次复用(含 cookie/localStorage),一次登录长期有效;②**录制→回放 cookie 注入**——回放前主进程从 `session.defaultSession`(webview 默认 session)导出 cookies,`toPlaywrightCookies` 转格式后 `context.addCookies` 注入。`MacroRunner(errorDir,timeoutMs?,onPause?,session?)` 第 4 参 `SessionOptions{userDataDir?,cookies?,localStorage?}`(core 不依赖 Electron,cookie 来源/转换全在主进程,同 onPause 依赖倒置);③**录制→回放 localStorage 注入**(开关 `injectRecordingLocalStorage`,默认关)——很多站点把登录 token 存 localStorage 而非 cookie,光注入 cookie 回放仍未登录。localStorage 无 session 级 API,只能在页面上下文里读且按 origin 隔离;主进程在 `did-attach-webview` 持有 guest webContents 引用(`recordingWebContents`,webview 销毁置 null),`buildSessionOptions` 里经 `captureRecordingLocalStorage()` 用 `executeJavaScript` 读取**录制 webview 当前页 origin** 的 localStorage(仅 http(s)、非空才返回),组装成 `SessionOptions.localStorage`(`{origin:{key:val}}`);回放端 `macro-runner.ts` 在 cookie 注入后用**独立 `addInitScript`**(导航前注册)按 `window.location.origin` 精准命中目标站注入,逐条 `setItem` try/catch 防配额超限,不污染其它 origin。**限制:实时 webview 只能读「当前页所在 origin」,跨多 origin 的 localStorage 不迁移**。UI 在侧栏可折叠面板「浏览器登录态」(`#bc-persist`/`#bc-inject`/`#bc-localstorage`/`#bc-choose`),IPC `get/set-browser-config`、`choose-user-data-dir`
- 新标签页归一(单视图工具,不做真多标签):点击 `target=_blank`/`window.open` 链接时 Electron 默认丢弃弹窗→录制不生效。**关键前置:`<webview allowpopups>`(index.html)——缺此属性 Electron 在源头丢弃弹窗,`setWindowOpenHandler` 根本不触发(163 首页「世界杯」链接点击无反应即此坑)**。主进程 `main.ts` 用 `mainWindow.webContents.on('did-attach-webview')` 拿到 guest webContents 后 `setWindowOpenHandler(({url})=>{ loadURL(url); return {action:'deny'} })`,把新窗口导航重定向回同一 webview;点击仍由 preload 的 capture click 先行录制,跳转后 `dom-ready` 自动 `armRecorder`。回放端 `macro-runner.ts` 用 `activePage` 可变引用+`context.on('page')` 跟随弹窗(初始页先建后挂监听,故此后 page 事件皆弹窗),executeStep/extract/pagination/错误截图均改用 `activePage`。两端落点一致(同 URL/DOM),选择器可匹配,无需新增步骤类型
- 等待类步骤跟随迟到的新窗口(2026-07-02,修「点击开新窗后下一步卡旧页」竞态):现象——某站第 2 步点击会**打开新浏览器窗口**,第 3 步「等待元素出现」的元素在新窗口里,回放卡死;日志停在「第 3 步 …执行中」后**晚约 1 秒**才打出「检测到新标签页弹窗,已切换为活动页继续回放」。根因——主循环 `executeStep(activePage, step, i)` 把**当下这一刻**的 `activePage` 作参数传入;点击完成后循环**立即**进入第 3 步,而此时新窗口的 `context.on('page')` 事件(异步)**尚未发火**,`activePage` 仍是旧页 → `handleWaitForSelector` 在旧页上等一个永不出现的元素直到超时;约 1 秒后事件才发火更新 `activePage`,但第 3 步早已绑定旧页,为时已晚。**切换机制本身没问题(日志证明能切过去),只是时机晚**。用户在第 3 步前插「人工介入暂停」可绕过(暂停期间事件已发火、`activePage` 已更新)。**修复(仅 `macro-runner.ts`,复用「切换会发生、只是晚」这一现有事实,让等待步骤主动追随迟到的新窗口)**:新增私有 helper `raceWaitAcrossPopup(page,context,waitFn,timeout)`——对「当前页」与「随后 `context.waitForEvent('page')` 弹出的新页(先 `waitForLoadState('domcontentloaded')` 兜底)」**竞态执行同一个等待函数**,`Promise.race` 先满足者胜出,`finally` 里对两分支各 `.catch(()=>undefined)` 抑制未采纳分支的迟到 rejection(避免 unhandledRejection)。`handleWaitForSelector`/`handleWaitForClickable` 各加 `context?` 参数、把原单页等待包进 helper(`waitForClickable` 的只读判定 predicate **一字未改**,仅抽成 `(p)=>p.waitForFunction(...)` 传入);`executeStep` 签名加末参 `context?:BrowserContext` 并在两个 wait case 透传,主循环调用处传 `context`,翻页 `turnPage` 闭包内因收窄丢失改用捕获的 `const runContext=context`。**只改等待类步骤**(用户录制模式=新窗口后紧跟一个等待步骤,已足够覆盖;点击等其它步骤零改动、零额外延迟);致命语义不变(当前页无该元素且无新窗口时两分支各自超时 reject,race 以先 reject 者结束,沿用顶层 catch 出错截图);新窗口真开出来后既有 `context.on('page')` 照常更新 `activePage`,故等待步骤解决后后续步骤自然在新页继续。旧宏零迁移、无类型/UI/preload 改动。验证:`MACRO_HEADLESS=1 node scripts/verify-popup-wait.mjs`(page1 有 target=_blank 链接、page2 延迟 1.5s 插入 `#target`,断言 `ok=true`;日志实测复现「第 3 步执行中→检测到弹窗→2s 后完成」)+ `verify-pagination`/`verify-merge` 回归通过
- 启动加载 UI(消除白屏):主进程 `main.ts` 的 `BrowserWindow` 加 `backgroundColor:'#f3f4f6'`(同 body 背景)+`show:false`,`ready-to-show` 事件里再 `show()`,避免内容绘制前露白;`index.html` 在 `<body>` 内加内联加载遮罩 `#boot-overlay`(全屏覆盖+居中旋转圈+「正在启动…」,旋转动画复用已有 `@keyframes ai-spin`,随首帧立即出现);`renderer.ts` 的 `init()` 改 `async`,把原 `void loadAiProfiles()/loadBrowserConfig()` 改 `await Promise.allSettled([...])`(任一失败也继续),完成后 `hideBootOverlay()` 加 `.hidden` 类淡出并 `setTimeout` 移除节点,调用处 `void init()`
- 打包浏览器缓存复用(避免每次重下 ~690MB Chromium):`scripts/copy-playwright-browser.mjs` 原**无条件 `rmSync` 后重装**→每打必下。改为版本标记幂等:`build/ms-playwright/.pw-version` 存已下的 `pwVersion`,**快路径**(标记==当前版本且 `chromium-*/chrome-win64/chrome.exe` 在位)直接 `exit 0` 跳过;**慢路径**(首次/版本变更)才清空+`playwright install chromium`+写标记。`build/ms-playwright/` 已 gitignore 但保留在本地磁盘供复用;删该目录或 `.pw-version` 可强制重下。electron/nsis 等由 electron-builder 自带缓存(`%LOCALAPPDATA%\electron-builder\Cache`),无需处理
- 后处理器注册表 + **热插拔插件列表**(回放产出后的定制后处理,轻量可插拔扩展点,非通用插件框架):根因是项目原本无任何 hook/pipeline,mode 分发是 `extractor.ts` 硬编码 if/else;为「list-action 下载 zip→解压取内部 excel→堆叠合并成一张总表」这类**定制非通用**需求,既不宜搭重型插件系统(过度设计),也不宜写死埋进 mode 分支(难找难扩)。**做法**:①`macro-types.ts` 加 `Macro.postProcess?:PostProcessSpec[]`(`{type,options?}`)+`RunResult.postProcessed?:PostProcessResult[]`(`{type,output?,message}`)+`PostProcessorManifest`(`{type,label,description}`,驱动 UI);②新目录 `src/core/post-processors/`——`index.ts` 是 `type→{manifest,handler}` 的 Map 注册表(`registerPostProcessor(manifest,handler)`/`runPostProcessors`/`listPostProcessors`,单个失败不致命、不依赖 Electron),末尾 `import './merge-zip-excel'` 触发自注册(CommonJS 循环引用安全:`registerPostProcessor` 在底部 require 前已挂 exports);③首个 handler `merge-zip-excel.ts`(**多格式**)——`adm-zip` 解压(从 entry 直接取 Buffer 不落临时文件);读取用 **SheetJS(`xlsx` 库)** 通吃 **csv/xls/xlsx**(`readTable`:csv 走 `XLSX.read(text,{type:'string'})`、二进制走 `{type:'buffer'}`,取首个 sheet,`sheet_to_json({defval:'',raw:false})` 得 `Record<string,string>[]`);**csv 编码 UTF-8 主 + GBK 兜底**(`decodeCsv`:UTF-8 解出含 U+FFFD 替换符则 `iconv-lite` 按 gbk 重解,去 BOM);**既认 zip 内条目、也认直接下载的裸 csv/xls/xlsx**;+ **复用现有 `exportToExcel`(exceljs)** 写盘(union 列天然兼容不同表头)堆叠成一张 `merged-<时间戳>.xlsx` 落 `exports/`;`options.addSourceColumn` 为真时加「来源文件」列(默认关,无 UI 入口、留作未来)。**SheetJS 装官方已修复版**(`npm i https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz`,避开 npm 上 0.18.5 旧版安全告警)。实测真实 payment zip(内含 UTF-8/BOM csv):20 个 zip 合并 4506 行、表头与中文「支付已通知」完好。④主进程 `main.ts` 的 `run-macro` 在 `runner.run()` 成功后,若 `macro.postProcess` 非空则 `runPostProcessors`(传 `downloads`(仅本次新下载)/`downloadDir`/`exportsDir`/`timestamp()`),`shell.showItemInFolder` 优先定位合并产物;另加 IPC `list-plugins`→`listPostProcessors()`,`preload` 暴露 `listPlugins()`。⑤UI——**注册表驱动的可选插件列表(热插拔)**:侧栏 collapsible 面板 `#plugin-panel`(青绿 + 🧩 徽标),面板体 `#plugin-list` 由 `loadPlugins()`(`init` 内随 `loadAiProfiles` 一起 `allSettled`)拉 `listPlugins()` **动态渲染**每个插件一行(`.plugin-row` = 左侧复选框+label,右侧「直接运行」按钮;下方 `.plugin-desc` 说明)。**两种触发**:(a) 勾选后**随主「运行」**执行——勾选项由 `selectedPluginTypes()` 读出,`buildMacro` 写 `macro.postProcess=picked.map(t=>({type:t}))`(随宏保存),结果走公共 `reportRunResult`;加载宏 `refreshPluginSelection(macro.postProcess)` 回显;(b)「直接运行」`runPluginDirect(type)`——**不跑宏**,经 IPC `run-plugin`(`main.ts`)弹**文件多选**(`openFile`+`multiSelections`,filters 限 `zip/csv/xls/xlsx/xlsm`,默认定位 downloads/),所选文件路径作 `downloads` 传给 `runPostProcessors([{type}], …)`,定位产物(可直接挑 zip 压缩包,与插件「解压 zip…」简介一致)。**选择器属提取规则(既有机制),插件只对文件/`downloads` 通用**。**热插拔**:以后在 `post-processors/` 加一个自注册带 manifest 的新插件模块 + `index.ts` 末尾 `import`,它就自动出现在列表(含「直接运行」),前端零改动。**只处理本次 `result.downloads`,不扫历史旧文件;不做跨表去重**。验证:`npm run build && node scripts/verify-merge.mjs`(造 3 xlsx 打 zip,断言合并 6 行、列并集 `[名称,价格,产地]`,已过)
- 已知限制:`waitForSelector` 不自动录制,可手动加入 JSON;回放靠 Playwright auto-wait 兜底;localStorage 注入仅覆盖录制 webview「当前页 origin」(跨多 origin 不迁移),复杂多域登录靠持久 profile 兜底

## 常用命令

- `npm install` — 安装依赖(postinstall 自动下载 Playwright Chromium)
- `npm run build` — 编译 TS + 拷贝静态资源到 dist
- `npm run dev` / `npm start` — 启动桌面程序(经 `scripts/start.mjs` 启动,自动剥离 ELECTRON_RUN_AS_NODE)
- `MACRO_HEADLESS=1 node scripts/verify-core.mjs` — 无头自检:实跑 demo 宏并导出 Excel
- `MACRO_HEADLESS=1 node scripts/verify-pagination.mjs` — 翻页健壮性自检:脆弱选择器 `.pager li a`(quotes.toscrape.com page2 命中 2)+ 语义指纹 anchor 重定位,实跑采满 3 页 30 行(需网络)
- `node scripts/test-ai.mjs [profileId] [需求]` — AI 自检:对接 openclaw agent 出规则(需先 `npm run build` 且 Gateway 运行),如 `node scripts/test-ai.mjs webextract`
- `node scripts/test-selector-fix.mjs [profileId]` — 选择器校正自检:对接 selector-fix agent 把脆弱选择器校正为稳定选择器(需先 `npm run build`、Gateway 运行、已建 selector-fix agent)
- `node scripts/verify-captures.mjs` — 旁车上下文读写离线自检(不需网络,需先 `npm run build`)
- `MACRO_HEADLESS=1 node scripts/verify-stepurls.mjs` — 回放回填来源 URL 自检:断言 `RunResult.stepUrls` 精确捕获链接跳转后的新页面 URL(救旧宏「按 URL 分组」;需网络,需先 `npm run build`)
- `MACRO_HEADLESS=1 node scripts/verify-cancel.mjs` — 运行中停止离线自检:卡在 30s 超时 waitForSelector 上,800ms 后 `runner.cancel()`,断言远早于超时返回且 `cancelled=true`(不需网络,需先 `npm run build`)
- `node scripts/verify-merge.mjs` — 合并后处理器离线自检:本地造 xlsx 打 zip,跑 merge-zip-excel,断言行数/列并集(需先 `npm run build`,不需网络)
- `node scripts/verify-bank-integrate.mjs` — 银行整合端到端自检:**5 代号(1/2/3/5/6)各真跑打包 exe**,拷 xlsxIntgration `data/input/N` 现成样本→桥接 spawn 对应 exe→断言 exports 产出 xlsx 可读、≥1 工作表(需先 `npm run build` + 该 exe 已打包;某代号 exe/样本缺失则跳过非失败)
- `MACRO_HEADLESS=1 node scripts/verify-replay-intercept.mjs` — 回放端请求改写 E2E:本地 echo 服务 + 真实 MacroRunner 跑 route 改写,断言服务端实收 `{page:1,size:100,injected:"yes"}`(需先 `npm run build`,不需网络;本机缺 headless_shell 时前置 `PLAYWRIGHT_BROWSERS_PATH=<repo>/build/ms-playwright` 走捆绑 Chromium)
- `node scripts/verify-timeline-recorder.mjs` — 请求时间线记录器纯逻辑离线自检:断言 JSONL 两行同 id / matches / **1MB body 不截断** / append / 懒建目录 / 熔断(需先 `npm run build`,不需网络)
- `MACRO_HEADLESS=1 node scripts/verify-timeline-replay.mjs` — 回放端只记录支路 E2E:本地 echo 服务 + 仅开 `record.enabled`(改写关),断言 `timeline-replay-*.jsonl` 含 `/echo` 请求+响应两行(需先 `npm run build`,不需网络;缺 headless_shell 时前置 `PLAYWRIGHT_BROWSERS_PATH=<repo>/build/ms-playwright`)
- `node scripts/verify-timeline-record.mjs` — 录制端只记录支路真跑 Electron E2E:webview 发 POST + 仅开 `record.enabled`,断言 `timeline-record-*.jsonl` 含请求+响应两行(真跑通 CDP Network 链路;需先 `npm run build`,需能以应用方式启动 Electron)
- `MACRO_HEADLESS=1 node scripts/verify-hotreload.mjs` — 回放端运行中热更新 E2E:初始改写+记录全关,回放跑到一半 `runner.updateRequestRules` 切全开,断言前段未改写/未记录、后段已改写/已记录(需先 `npm run build`,不需网络;缺 headless_shell 前置 `PLAYWRIGHT_BROWSERS_PATH=<repo>/build/ms-playwright`)
- `MACRO_HEADLESS=1 node scripts/verify-resend.mjs` — 回放端「重发型」拦截 E2E:命中触发请求后延时改参重发新请求,断言原始 1 条 + 重发 ≥1 条(带 `x-macro-resend` 头、已改参、延时 ≈1500ms、防递归不增长)(需先 `npm run build`,不需网络;缺 headless_shell 前置 `PLAYWRIGHT_BROWSERS_PATH=<repo>/build/ms-playwright` 或 `npx playwright install chromium-headless-shell`)
- `node scripts/verify-response-rewrite.mjs` — 响应头条件改写纯逻辑离线自检:断言 `when` 条件(AND/大小写不敏感/缺省恒真)、`setHeaders` 覆盖不产生重复键、`removeHeaders` 大小写不敏感、保留不在名单内的重复头(两条 set-cookie)、无动作/条件不满足返回 null(需先 `npm run build`,不需网络。23 断言全过)
- `MACRO_HEADLESS=1 node scripts/verify-response-header-replay.mjs` — 回放端响应头改写 E2E:本地服务返 `xx:1,cc:0,x-drop:yes`,规则 `when xx=1 → setHeaders cc=1 + removeHeaders x-drop`,断言页面读到改写后响应头 `cc=1 且无 x-drop`(`#done` 出现)且 `/api/data` 命中(需先 `npm run build`;缺 headless_shell 前置 `PLAYWRIGHT_BROWSERS_PATH=<repo>/build/ms-playwright` 或 `npx playwright install chromium-headless-shell`)
- `MACRO_HEADLESS=1 node scripts/verify-request-header-replay.mjs` — 回放端**请求头**改写 E2E:同源服务存收到的 `req.headers`,session `requestHeaderRules:[{urlPattern,when,setHeaders,removeHeaders}]` + `rules:[submit body 改写]`,三用例断言服务端收到的请求头:①组合 a(GET)set/remove/保留 + `when` 命中;②`when` 不满足不改;③组合 b(POST)body 与请求头一次 `continue` 合并(且 body 改长后仍完整 → 证 content-length 剥离)(需先 `npm run build`;缺 headless_shell 前置 `PLAYWRIGHT_BROWSERS_PATH=<repo>/build/ms-playwright`)
- `MACRO_HEADLESS=1 node scripts/verify-block-intercept.mjs` — 回放端「真拦截(硬阻断)」E2E:本地服务同源测试页并发 `fetch('/allowed')`+`fetch('/blocked')`,session `enabled:true, blocks:[{urlPattern:'*/blocked*'}]`,跑 `[goto,waitForSelector #done]`,断言**服务端收到 /allowed 但从未收到 /blocked**(被 route.abort 硬阻断、未发出)且 result.ok(需先 `npm run build`;缺 headless_shell 前置 `PLAYWRIGHT_BROWSERS_PATH=<repo>/build/ms-playwright` 或走系统缓存 `%LOCALAPPDATA%\ms-playwright`)
- `MACRO_HEADLESS=1 node scripts/verify-dump-body.mjs` — 回放端「请求体落盘」CDP Fetch E2E:**两用例**(页面发 `Blob` body 模拟 File 上传 + `Uint8Array` body),768 字节全字节值二进制,session `enabled:true, dumps:[{urlPattern:'*/upload*',extension:'mp4'}]`,`MacroRunner` 第 7 参传临时 dumpsDir,断言各落 1 个 `dump-*.mp4` 且与原始 Buffer **逐字节相等**(Blob 用例证 CDP `postDataEntries` 抓到了 postDataBuffer 抓不到的 File/Blob 上传体)(需先 `npm run build`;缺 headless_shell 前置 `PLAYWRIGHT_BROWSERS_PATH=<repo>/build/ms-playwright` 或 `npx playwright install chromium-headless-shell`)
- `MACRO_HEADLESS=1 node scripts/verify-body-replace.mjs` — 回放端「请求体整体替换(拦截替换)」CDP Fetch E2E:页面发 **Blob** body 内容 A(256 字节)、替换文件 B(600 字节、不同内容/大小),session `enabled:true, bodyReplaces:[{urlPattern:'*/upload*',replaceWithFile:B}]`,断言**服务端实收 == B 逐字节相等**、!= A、`content-length` 头 = 600(大小变化时长度正确重算);证 CDP `Fetch.continueRequest({postData})` 整体替换 Blob 上传体(需先 `npm run build`;缺 headless_shell 前置 `PLAYWRIGHT_BROWSERS_PATH=<repo>/build/ms-playwright` 或 `npx playwright install chromium-headless-shell`)
- `MACRO_HEADLESS=1 node scripts/verify-resend-headers.mjs` — 回放端「重发型」改重发请求头 E2E:命中触发后延时重发,`setHeaders` 覆盖/新增头、`removeHeaders` 删除继承头,断言重发头含 `x-custom`、不含 `x-inherited`、保留 `x-keep` 与标记头 `x-macro-resend`,且原始请求不受影响(需先 `npm run build`;缺 headless_shell 前置 `PLAYWRIGHT_BROWSERS_PATH=<repo>/build/ms-playwright` 或走系统缓存 `%LOCALAPPDATA%\ms-playwright`)
- `MACRO_HEADLESS=1 node scripts/verify-resend-replace.mjs` — 回放端「重发 + 本地文件整体替换 body」E2E:页面只发 1 个 `POST /trigger`(JSON),替换文件 B(600 字节全字节值),session `enabled:true, rules:[], resends:[{urlPattern:'*/trigger*',delayMs:800,replaceWithFile:B}]`,宏 `[goto,pause]`+onPause 保活;断言恰 1 原始(字节 != B)+ ≥1 重发且**重发体 == B 逐字节相等**(证 resend 用文件字节整体作 body、base64→页面内 `Uint8Array` 重建、能发任意二进制)+ 延时 ≥500ms(需先 `npm run build`;缺 headless_shell 前置 `PLAYWRIGHT_BROWSERS_PATH=<repo>/build/ms-playwright` 或走系统缓存 `%LOCALAPPDATA%\ms-playwright`)
- `MACRO_HEADLESS=1 node scripts/verify-popup-wait.mjs` — 新窗口等待竞态离线自检:本地造 target=_blank 两页(page2 延迟 1.5s 插入 `#target`),断言点击开新窗后 waitForSelector 跟随新窗口 `ok=true`(需先 `npm run build`,不需网络)
- 注:环境若设置了 `ELECTRON_RUN_AS_NODE=1`,务必用 `npm start`(而非 `electron .`)启动
- `MACRO_TIMEOUT=60000`(毫秒,默认 60000)— 回放全局默认超时,影响 click/goto/waitForSelector 等动作;慢加载站点可调大,如 `MACRO_TIMEOUT=90000 npm run dev`
